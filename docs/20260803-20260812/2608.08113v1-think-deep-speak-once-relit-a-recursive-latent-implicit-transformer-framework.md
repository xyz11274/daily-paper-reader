---
title: "Think Deep, Speak Once: Relit, A Recursive Latent Implicit Transformer Framework"
authors: "Abhishek Panwar, Maheep Singh, Saksham Bansal"
date: 2026-08-08
pdf: "https://arxiv.org/pdf/2608.08113v1"
tags: ["query:llm-trends"]
score: 9.0
evidence: 面向思维链的潜在推理框架，直接涉及LLM中的思维链推理
tldr: 思维链提示虽能激发LLM推理，但强制模型将中间步骤外部化为离散令牌，带来大量计算开销。潜在推理方法试图在连续隐藏状态内化该过程，但现有递归模型在自然语言语义连贯性上不足。本文提出ReLIT，一种混合框架，将深层递归推理嵌入基础模型的丰富语义表示中，在保持语义连贯的同时减少外部化步骤。实验表明ReLIT在推理任务上性能优异，同时降低计算开销，为高效LLM推理提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.08113v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1299, \"height\": 645, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08113v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1528, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08113v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 781, \"height\": 558, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08113v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1435, \"height\": 529, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.08113v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1163, \"height\": 586, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.08113v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 717, \"height\": 246, \"label\": \"Table\"}]"
motivation: 面向思维链的潜在推理框架，直接涉及LLM中的思维链推理。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
思维链（Chain-of-Thought, CoT）提示已经成为激发大语言模型（Large Language Models, LLMs）推理能力的主流范式，但它迫使模型将中间推理步骤外化为离散 tokens，由此产生了巨大的计算开销。近期的潜空间推理方法尝试将这一过程内化到连续隐藏状态中。作为潜空间推理领域的最新进展之一，微型递归模型（Tiny Recursive Models, TRMs）擅长符号推理，但难以在自然语言场景中保持语义一致性。为填补这一缺口，我们提出ReLIT（Recursive Latent Implicit Transformer，递归潜隐式Transformer），这是一种将深度递归推理建立在基础模型丰富语义表示之上的混合框架。ReLIT为冻结的大语言模型骨干网络（TinyLlama-1.1B）增加了一个轻量可训练的递归块，该块在生成最终输出之前迭代优化其潜思考（z），从结构上分离算法处理中的语言直觉需求，并通过梯度隔离的循环回路实现"深度思考"，同时不会产生显式token生成带来的延迟。实验表明，ReLIT在GLoRE逻辑推理基准测试上实现了极高的参数效率，尽管仅使用极少监督，它在ProofWriter和RuleTaker等挑战性任务上的表现仍可匹配甚至优于参数量大得多的模型。这些结果证明，推理能力可以通过递归深度而非参数宽度实现高效扩展，为基于语义的隐式推理提供了一个原则性框架。

## 速览
**TLDR**：当前思维链提示诱导大语言模型推理时，需要输出离散中间 tokens 带来大量计算开销，现有隐式推理的Tiny递归模型擅长符号推理却难以保持自然语言语义连贯。本文提出ReLIT递归隐式Transformer框架，为冻结的基础大模型添加轻量可训练递归块，在隐空间迭代优化推理过程。该方法在多个推理基准上取得优异性能，证明可通过循环深度而非参数宽度高效拓展推理能力。 \
**Motivation**：思维链推理需输出中间离散 tokens 带来大量计算开销，现有隐式递归推理模型难以保持自然语言的语义连贯性。 \
**Method**：ReLIT为冻结的TinyLlama-1.1B大模型主干添加轻量可训练递归块，在隐空间迭代优化推理后生成最终输出。 \
**Result**：在GLoRE逻辑推理基准实现高参数效率，仅用少量监督就在ProofWriter、RuleTaker上匹配或超越更大模型。 \
**Conclusion**：推理能力可通过循环深度而非参数宽度高效缩放，为语义接地的隐式推理提供了规范的框架。

---

## Abstract
Chain-of-Thought (CoT) prompting has become the dominant paradigm for eliciting reasoning in Large Language Models (LLMs), yet it creates substantial computational overhead by forcing models to externalize intermediate reasoning steps as discrete tokens. Recent latent reasoning approaches attempt to internalize this process within continuous hidden states. One of the latest advancements in the field of latent reasoning, Tiny Recursive Models (TRMs) excel at symbolic reasoning but struggle to preserve semantic coherence in natural language settings. To bridge this gap, we introduce ReLIT (Recursive Latent Implicit Transformer), a hybrid framework that grounds deep recursive reasoning within the rich semantic representations of a foundational model. ReLIT augments a frozen LLM backbone (TinyLlama-1.1B) with a lightweight, trainable recursive block that iteratively refines its latent thinking (z) before committing to a final output, structurally solving linguistic intuition from algorithmic processing and enabling "deep thinking" via gradient-isolated recurrent loops without the latency of explicit token generation. Empirically, ReLIT achieves high parameter efficiency on the GLoRE logical reasoning benchmark, matching or outperforming significantly larger models on challenging tasks such as ProofWriter and RuleTaker despite minimal supervision. These results demonstrate that reasoning capability can be scaled efficiently through recurrent depth rather than parameter width, offering a principled framework for semantically grounded implicit reasoning.

---

## 论文详细总结（自动生成）

# 论文详细总结
## 1. 核心问题与研究背景
- 主流的思维链（CoT）推理范式需要大语言模型将中间推理步骤外化为离散token输出，不仅带来高额计算延迟与内存开销，还存在单步错误导致整体推理失败的问题，同时将高维隐思想压缩为低维文本也会造成信息损失
- 新兴的潜空间推理范式尝试将推理过程内化到连续隐藏状态中提升效率，但领域最新的微型递归模型（TRM）仅擅长符号推理，缺乏自然语言理解能力，无法在自然语言场景中保持语义连贯性
- 现有架构无法兼顾高效递归隐式推理与自然语言语义一致性，本文针对该缺口提出新的推理框架。

## 2. 提出的方法论
### 核心思想
构建"三明治"混合架构：将深度递归推理过程放在冻结预训练大模型的隐空间内，结构上分离语义理解与逻辑推理两个模块，在隐空间完成多步推理后仅输出最终结果，既避免了显式中间token生成的计算开销，也解决了纯递归模型的语义缺失问题。

### 关键技术细节
- **三态交互设计**：定义三类向量分工完成推理：
  1. 语义锚`x`：由冻结预训练大模型输出的静态输入语义表示，全程保持不变防止语义漂移
  2. 潜思考暂存`z`：专门的内部工作记忆，可自由探索抽象逻辑路径，初始化为可学习高斯分布
  3. 答案状态`y`：动态演化的当前推理假设，初始化为语义锚`x`，每轮迭代优化，最终投影为模型输出
- **残差状态更新策略**：不同于传统递归模型从头生成新状态，ReLIT每步仅计算逻辑增量（delta）叠加到原有状态上，既保证了训练过程的梯度流畅，也防止长推理周期中语义信息流失，推理过程等价于在隐空间中做几何导航逐步收敛到正确解
- **Recall-Then-Learn训练协议**：为了实现深度递归同时避免传统BPTT的高内存开销，将训练分为两个阶段：先做T-1次无梯度递归更新让系统收敛到稳定推理路径（预热阶段），仅最后一步做梯度更新（优化阶段），通过梯度隔离大幅降低内存占用
- **自适应停止机制**：添加停止头预测推理收敛置信度，模型可根据任务复杂度动态停止推理，优化计算预算分配
- **模块化输出头**：支持两种配置：冻结原有大模型的投影头保证全局语义一致性，或是使用可训练投影头适配特定下游任务。

## 3. 实验设计
### 基准与数据集
在**GLoRE通用逻辑推理基准**上开展测试，覆盖不同推理类型、不同数据规模的场景，共使用5个数据集：
1. ProofWriter：演绎逻辑推理任务，训练集5000样本，测试集3000样本
2. RuleTaker：规则推理任务，训练集5000样本，测试集3000样本
3. HELP：可解释规划推理数据集，训练集800样本，测试集200样本
4. NaN-NLI：否定类自然语言推理数据集，极端低资源场景，仅200训练样本、50测试样本
5. TaxiNLI：自然语言推理数据集，训练集2000样本，测试集800样本

### 对比方法
对比了不同参数规模、不同类型的主流大模型，包括：RoBERTa、LLaMA、Falcon、Mixtral-8x7B、ChatGPT、GPT-4、o1-mini、DeepSeek R1、QwQ-32B。

## 4. 资源与算力
文中未明确提及实验使用的GPU型号、数量、训练时长等算力相关信息。

## 5. 实验数量与充分性
- 共在5个不同特性的推理数据集上开展实验，覆盖了演绎推理、规则推理、低资源推理、自然语言推理等多种场景，测试了从200样本到5000样本的不同监督规模，额外做了收敛性分析、推理轨迹可视化验证方法的合理性，对比了从百万级到百亿级参数量的多款主流模型，整体实验覆盖度较好，结论客观公平
- 不足：文中未提供模块级消融实验，无法验证各核心设计（残差更新、Recall-Then-Learn等）的独立贡献，实验完整性略有欠缺。

## 6. 主要结论与发现
- ReLIT参数效率极高：仅训练轻量递归块，在ProofWriter（98.6%准确率）、RuleTaker（97.6%准确率）任务上大幅领先参数量远大于自身的主流大模型，在HELP数据集上性能超过ChatGPT和GPT-4，在低资源NaN-NLI数据集上性能与ChatGPT相当
- 模型可动态适配任务难度：平均推理停止深度随任务复杂度提升，简单的ProofWriter平均仅需3.6步，复杂的RuleTaker需要5.2步，低资源NaN-NLI需要7.8步，符合设计预期
- 验证了推理能力可通过递归深度而非参数宽度高效扩展，仅用少量监督就能达到远超同参数规模模型的推理性能。

## 7. 优点
- 架构设计巧妙：分离语义理解与逻辑推理，冻结大模型主干仅训练轻量递归块，整体训练、推理成本极低，参数效率远优于传统全参数微调方案
- 同时解决了两个现有方案的痛点：既弥补了纯递归隐式模型的语义缺失问题，也保留了隐式推理无中间token开销的优势，推理延迟远低于CoT
- 自适应停止机制可动态分配计算资源，计算利用率更高
- 低数据适应性好：依托冻结大模型的预训练语义能力，仅用少量训练样本就能取得不错的推理性能。

## 8. 不足与局限
- 理论上存在固有语义瓶颈：根据数据处理不等式，递归推理无法恢复冻结主干提取语义锚时丢失的输入信息，推理能力上限受主干模型的表示质量限制
- 当前仅支持单次输出任务（"深度思考，一次输出"），尚未拓展到多token自回归生成场景，适用场景有限
- 未在数学推理、多步开放域问答等更多类型的推理任务上验证泛化性，也未做模块消融验证各组件贡献
- 未公开算力消耗细节，可复现性信息略有缺失。

（完）
