---
title: "Closing the Consistency Gap: Self-Evolving Agents That Learn to Stay on Course"
title_zh: 弥合一致性差距：学会保持正轨的自进化智能体
authors: "Evelyn Duesterwald, Benjamin Elder, Lilian Ngweta, Shashanka Ubaru, Malgorzata Zimon"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08832v1"
tags: ["query:agent-evolve"]
score: 9.0
evidence: 自进化智能体框架，将不稳定步骤转为情景记忆
tldr: "LLM智能体平均准确率尚可，但多次运行常不一致：ReAct在AppWorld上用GPT-4.1五次全对率仅53%，单次通过率却达77%，形成24个百分点的一致性差距。本文提出自演化智能体框架，用一致性分析器定位轨迹中易翻转步骤，再由指南生成器转为记忆中的针对性指南，注入未来相似任务。实验将五次全对率在同任务评估提升16个百分点，相似任务泛化提升13个百分点。该工作把一致性差距视为可信部署前提，并用记忆化自我演化加以缓解。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.08832v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 2594, \"height\": 944}]"
motivation: LLM智能体平均准确率尚可，但同一任务多次运行结果不稳定，生产可靠性不足；作者将其定义为一致性差距，并视作可信部署的前提。
method: 提出自演化框架：一致性分析器定位易翻转低一致性步骤，指南生成器将诊断转为针对性指南，写入 episodic memory 并注入未来相似任务。
result: 在 AppWorld 的 ReAct/GPT-4.1 设置下，五次全部成功任务比例在同任务评估提升16个百分点，相似任务泛化提升13个百分点。
conclusion: 表明将低一致性步骤诊断并记忆化为指南，可显著缩小一致性差距，为LLM智能体可信部署提供自演化路径。
---

## 摘要
由大语言模型（LLM）驱动的智能体平均而言可以很准确，但在生产环境中却可能不可靠；这一差异已被观察到，但仍在很大程度上未得到解决。在 AppWorld 基准上，使用 GPT-4.1 的 ReAct 智能体在五次运行同一任务时，仅在 53% 的情况下五次全部成功，尽管其单次运行的平均通过率为 77%。我们将这 24 个百分点的缺口称为一致性差距，并认为解决该问题是可信 AI 智能体部署的前提条件。我们提出一种自进化智能体框架，通过识别智能体轨迹中不稳定、低一致性的步骤，并将其转化为智能体在未来运行中可以借鉴的情节记忆，来缩小这一差距。其核心是一个一致性分析器（Consistency Analyzer），用于精确定位轨迹在多次执行中可能发生翻转的位置及其原因；以及一个指南生成器（Guideline Generator），用于将诊断结果转化为有针对性的指南，提交至记忆，并在未来类似任务的智能体执行中注入。在 AppWorld 上，使用 ReAct/GPT-4.1 时，我们的框架在同一任务评估中将五次运行全部成功的任务比例提高了 16 个百分点，在相似任务泛化中提高了 13 个百分点。

## Abstract
Large language model (LLM)-powered agents can be accurate on average yet unreliable in production, a discrepancy that has been observed but remains largely unaddressed. When given the same task five times, a ReAct agent on the AppWorld benchmark using GPT-4.1 succeeds in all five runs only 53% of the time, even though its per-run pass rate averages 77%. We call this 24-point shortfall the consistency gap, and we argue that addressing it is a precondition for trustworthy AI agent deployment. We present a self-evolving agent framework that reduces this gap by identifying unstable, low-consistency steps in agent trajectories and converting them into episodic memory the agent can draw on in future runs. At its core is a Consistency Analyzer that pinpoints where and why a trajectory is likely to flip across executions, and a Guideline Generator that converts the diagnosis into targeted guidelines, committed to memory and injected into future agent executions on similar tasks. On AppWorld with ReAct/GPT-4.1, our framework raises the fraction of tasks that succeed in all five runs by +16 points on same-task evaluation and +13 points on similar-task generalization.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义
- **研究动机**：LLM 智能体在平均准确率上可能表现不错，但在生产环境中多次执行同一任务时结果不稳定。论文将这种“平均准确率高、重复运行可靠性低”的差异称为**一致性差距（consistency gap）**。
- **典型证据**：在 AppWorld 基准上，ReAct + GPT-4.1 的单次平均通过率 Mean@5 为 77%，但同一任务五次运行全部成功的比例 Pass^5 仅 53%，差距达 24 个百分点。
- **整体含义**：作者认为，一致性不是准确率的附属品，而是可信部署的前提。若智能体不能稳定重复成功，平均准确率会掩盖用户实际体验中的失败风险。

### 2. 方法论
- **核心思想**：识别智能体轨迹中“容易翻转”的低一致性步骤，将其诊断结果转化为自然语言指南，写入情景记忆，并在未来相似任务中注入提示词，从而稳定决策。
- **关键指标**：
  - Pass@k：k 次中至少成功一次，衡量能力上限。
  - Mean@k：k 次平均通过率，常规准确率。
  - Pass^5 / Pass^k：k 次全部成功，衡量一致性。
  - 一致性差距：Gap@k = Mean@k − Pass^k。
  - 归一化一致性：Consistency@k = Pass^k / Mean@k，用于剥离模型能力上限的影响。
- **两阶段离线流水线**：
  - **Detect：一致性分析器**。对轨迹中每个推理步骤，将记录下来的 prompt 重新发给同一 LLM 端点 N 次，默认 N=30、温度约 0.5，形成响应分布。分析器是黑盒的，不需要模型
