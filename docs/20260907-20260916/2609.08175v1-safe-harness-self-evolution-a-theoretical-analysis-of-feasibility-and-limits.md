---
title: "Safe Harness Self-Evolution: A Theoretical Analysis of Feasibility and Limits"
title_zh: 安全 Harness 自演化：可行性与局限的理论分析
authors: "Qianshu Cai, Yonggang Zhang, Jun Nie, Maohao Ran, Huajiang Zheng, Jun Song, Xinmei Tian, Yike Guo, Wei Xue"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08175v1"
tags: ["query:agent-evolve"]
score: 9.0
evidence: 对智能体安全修改自身提示、工具与代码的理论分析
tldr: 当底层语言模型冻结时，智能体可通过修改提示、工具、代码或编排并跨任务保留来自我进化。本文在固定用户任务分布下，系统分析安全自进化的可行性与极限，统一建模修改生成、有限数据认证与选择、安全采纳及更新后行为。核心结论是生成与认证约束不同：当前任务表现不决定合格修改生成概率，评估受限时增加候选未必提升成功保证，识别真实改进的最坏评估成本在奖励趋近上界时发散。该研究为诊断瓶颈和设计更安全的自进化机制提供理论基础。
source: arxiv
selection_source: fresh_fetch
motivation: 安全自进化需在冻结语言模型下持续修改并保留变更，但其可行性与安全边界缺乏统一理论刻画。
method: 在固定用户任务分布下，统一建模修改生成、有限数据认证与选择、安全采纳及更新后行为，并推导期望奖励提升条件与有限数据界。
result: 生成与认证约束分离：当前表现不决定合格修改生成率，评估受限时增加候选未必提升成功保证，奖励趋近上界时识别真实改进的评估成本发散。
conclusion: 认证改进保证可随多次更新累积，但单次成功不保证后续仍可提升；该框架可用于诊断瓶颈并设计更安全的自进化机制。
---

## 摘要
Harness 自演化是指智能体在保持底层语言模型冻结的情况下，根据任务反馈修改其提示、工具、代码或编排，且修改会在后续任务中持续保留的过程。我们对安全 Harness 自演化的可行性与局限进行了系统的理论分析，将修改生成、有限数据认证与选择、安全采纳以及更新后的行为联系起来。在固定的用户任务分布下，我们建立了保证整体期望回报提升、同时控制保留任务上变化的条件，刻画了生成合格修改的概率，并推导了安全选择与采纳的有限数据界。我们的分析表明，生成与认证施加了不同的约束：当前任务性能并不决定生成合格修改的概率，并且当评估受限时，生成更多候选并不一定能提高成功更新的保证。因此，即使改进机会仍然存在，也可能出现停滞。我们进一步表明，当期望回报趋近其上界时，识别真实改进的最坏情况评估成本会发散。在连续更新中，经过认证的改进保证会在有限运行过程中累积，但一次成功更新本身并不保证进一步的改进仍然可能。这些结果为诊断瓶颈和设计更安全的自演化机制提供了基础。

## Abstract
Harness self-evolution is the process by which an agent modifies its prompts, tools, code, or orchestration in response to task feedback while keeping the underlying language model frozen, with changes persisting across subsequent tasks. We provide a systematic theoretical analysis of the feasibility and limits of safe harness self-evolution, connecting modification generation, finite-data certification and selection, safe adoption, and behavior after an update. Under a fixed user-task distribution, we establish conditions guaranteeing overall expected-reward improvement while controlling changes on retained tasks, characterize the probability of generating qualified modifications, and derive finite-data bounds for safe selection and adoption. Our analysis shows that generation and certification impose distinct constraints: current task performance does not determine the probability of generating qualified modifications, and generating more candidates need not improve the guarantee of a successful update when evaluation is limiting. Stagnation may therefore arise even when improvement opportunities remain. We further show that worst-case evaluation cost for recognizing genuine improvements diverges as expected reward approaches its upper bound. Across successive updates, certified improvement guarantees accumulate over a finite run, but a successful update does not by itself guarantee that further improvement remains possible. These results provide a basis for diagnosing bottlenecks and designing safer self-evolution mechanisms.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究背景**：Harness 自演化指智能体在**冻结底层 LLM** 的前提下，根据任务反馈修改自身的提示、工具、代码或编排逻辑，且修改会跨任务持久保留。已有工作如 Gödel Agent、SICA、DGM、HGM、MOSS、HarnessX、Self-Harness、TTHE 等，展示了智能体自我修改的可行性，但缺乏统一理论刻画：何时能安全改进？改进机会如何转化为可靠更新？停滞来自生成不足还是认证不足？
- **核心问题**：论文围绕 Harness 自演化闭环提出四个问题：
  1. 智能体能否生成满足改进与保留要求的“合格修改”？
  2. 有限评估能否安全认证并采纳合格修改？
  3. 安全自演化能推进多远？
  4. 一次成功更新后，后续是否还能继续改进？
- **整体含义**：论文在固定用户任务分布下，将修改生成、有限数据认证与选择、安全采纳、更新后行为统一建模。核心观点是：**生成机会与认证采纳面临不同约束**；当前任务表现不决定生成合格修改的概率；评估受限时增加候选生成未必提升成功更新保证；即使仍有改进机会，也可能因采纳规则而停滞；当期望回报接近上界时，识别真实改进的最坏评估成本会发散。

## 2. 方法论：核心思想与关键技术细节

- **形式化设定**：
  - 智能体 \(A_k=(M,C_k)\)，其中 \(M\) 为冻结 LLM，\(C_k\) 为可演化 harness：提示、代码、工具、编排。
  - 修改 \(\Delta C\in\mathcal M\)，候选智能体 \(\tilde A=A_k\oplus\Delta C\)。
  - 奖励 \(r(t,o)\in[0,1]\)，任务期望奖励 \(V(A,t)\)，用户分布 \(D_{\text{user}}\) 下期望奖励 \(J_{\text{user}}(A)\)。
  - 失败检测器 \(\phi(t,o)\)，失败概率 \(\psi_A(t)\)，失败率 \(Z_k\)；定义失败任务分布 \(D_{F,k}\) 与保留任务分布 \(D_{R,k}\)。

- **合格修改与目标**：
  - 目标 \(T=(\lambda,\delta)\)：要求失败任务加权改进 \(L_k(c)\ge\lambda\)，且保留任务平均绝对变化 \(D_k(c)\le\delta\)。
  - 定理 1：若 \(D_R(\tilde A;A_k)\le\delta\)，则
    \[
    J_{\text{user}}(\tilde A)-J_{\text{user}}(A_k)\ge L_{A_k}(\tilde A)-(1-Z_k)\delta.
    \]
  - 校准目标：\(\lambda_k(\gamma,\delta)=\gamma+(1-Z_k)\delta\)，保证整体期望回报至少提升 \(\gamma\)。

- **可达性（Reachability）**：
  - 定义 \(P_k(T\mid H_k,\xi)\)：在当前历史与生成上下文下，生成过程产出合格修改的概率。
  - 候选池上界：从 \(N\) 个 i.i.d. 候选中选择，任何选择规则满足
    \[
    R_{k,N}(T)\le 1-(1-P_k(T))^N\le NP_k(T).
    \]
    即选择最多实现“池中至少存在一个合格修改”的命中概率。
  - 必要条件：正可达性要求 \(Z_k\bar a_k\ge\lambda\)，等价地 \(J_{\text{user}}(A_k)\le 1-\lambda\)。若当前状态无法提供足够失败任务改进空间，任何生成或评估预算都无法产生合格修改。

- **有限数据认证与安全采纳**：
  - 对每个候选估计 \(L_k(c)\)、\(D_k(c)\) 的置信区间；精确目标门控在
    \[
    \hat L_i-e^l_{L,i}\ge\lambda,\quad \hat D_i+e^u_{D,i}\le\delta
    \]
    时认证。
  - 定义“内集” \(P^+\)：具有足够边际的合格修改。定理 3 给出认证选择概率下界与候选池上界；候选池增大可提高 any-hit 概率，但会通过 \(\log N\) 加宽同时置信区间，未必提升认证下界。
  - 单修改更新协议 Two-Gate：收集生成失败批、独立评估失败批与存储样本，估计 \(\hat L_k,\hat D_R,\hat Z_k\)；若
    \[
    \hat L_k\ge\tau,\quad \hat D_R\le\delta
    \]
    且运行时检查
    \[
    \Delta_k=\tau-\varepsilon_L-(1-\hat Z_k+\varepsilon_Z)(\delta+\varepsilon_D)>0,
    \]
    则采纳。定理 4 给出单步错误采纳概率上界。
  - Measured-margin 规则：使用单侧下降 \(D^-_R\)，接受条件为 \(\hat D^-_{R,k}\le\delta^-\) 且
    \[
    \hat M_k=\hat L_k-\varepsilon_L-(1-\hat Z_k+\varepsilon_Z)(\hat D^-_{R,k}+\varepsilon^-_D)>0.
    \]
    该规则可利用修改自身保留任务损失，在 Two-Gate 阈值之外认证正改进。

- **极限与多步分析**：
  - 定义可采纳区域 \(R\)，给出 \(\delta_{\max}\)；若声明容差过大，则无法通过调整阈值或增加生成解决。
  - 当期望回报接近上界时，维持认证错误控制与检测能力所需的最坏评估成本发散：在硬实例族上，任意 level-\(\beta\) 认证器满足
    \[
    \mathbb E^-[N_{\text{eval}}]\ge \frac{Z_k}{10u_k},
    \]
    其中 \(u_k=1-J_{\text{user}}(A_k)\)。
  - 多步定理 7：在每步假设成立且运行级风险分配下，被接受步骤的认证改进下界可累加：
    \[
    J_{\text{user}}(A_K)-J_{\text{user}}(A_0)\ge\sum_{k\in\mathcal A}\Delta_k.
    \]
  - 命题 4：在无限制更新类中，可构造第一步以概率 1 生成合格修改并认证改进，但后续所有步骤均无额外改进。因此**当前安全改进不保证后续仍可改进**。

- **诊断框架**：
  - 区分生成概率 \(P_k\)、认证概率、选择概率 \(R\)。
  - 定理 6 给出对所有目标同时成立的可达性均匀置信界：
    \[
    \max\{0,\hat P_{\text{in}}(T)-r_{\text{VC}}\}\le P_k(T)\le \min\{1,\hat P_{\text{out}}(T)+r_{\text{VC}}\}.
    \]
  - 区间宽度由“评估矩形未判定资格的比例”与“有限生成样本误差”两部分组成，分别对应评估精度与生成样本量，不能相互替代。

## 3. 实验设计：数据集、场景与对比

- **DS-1000 单步研究**：
  - 对象：持久化 Python solver-harness 源码修改，LLM 冻结。
  - 生成过程：独立生成、无标签修订、结果反馈修订。
  - 规模：4 个冻结上下文 × 3 个生成过程 × 6 个独立候选池 × 每池 4 个终端输出 = 288 个终端输出，其中 2 个无效。
  - 任务划分：每个上下文包含 8 个生成任务、8 个低预算评估任务、48 个高预算评估任务、56 个独立审计任务；生成、认证、审计任务不相交。
  - 目标：主目标 \(T_0=(0.05,0.50)\)，更严格目标 \(T_1=(0.10,0.25)\)，并预注册目标网格。
  - 对比：三种生成过程；低/高预算认证器；审计点估计 vs 认证决策；采纳 vs 保留。
  - 结果：审计点估计下，各生成过程 \( \hat P_{\text{pt}} \) 约 0.812–0.885，且每个池至少有一个修改满足目标；但高预算门控仅认证 2 个独立生成修改，修订修改全部未认证；\( \hat Q,\hat H,\hat R \) 多数为 0 或极低。说明“审计估计满足目标”不等于“有限评估能认证”。

- **DS-1000 跨步研究**
