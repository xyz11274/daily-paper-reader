---
title: "SkillZip: Evaluation-Free Skill Compression for Self-Evolving Agents by Discovering Reusable Structure"
title_zh: SkillZip：通过发现可复用结构实现面向自进化智能体的无评估技能压缩
authors: "Xiaofan Bai, Hongqiang Lin, Chao Liu, Yantao Zhang, Xuan Jin, Xipeng Cao, Yuhong Li"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.11079v1"
tags: ["query:ase"]
score: 10.0
evidence: 专门面向自进化智能体研究技能压缩方法
tldr: 自进化智能体在积累可复用技能的过程中，容易产生大量冗余重复内容，导致技能注入成本升高、维护难度变大，现有压缩方案均存在适配缺陷。本文提出无评估的技能压缩方法SkillZip，通过提取共享可复用结构压缩技能，实验验证该方法在压缩性能、泛化性上表现更优，可为自进化智能体技能维护提供高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.11079v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 898, \"height\": 542, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11079v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 775, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11079v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1660, \"height\": 1016, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11079v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 835, \"height\": 286, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11079v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 741, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11079v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1507, \"height\": 386, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.11079v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 918, \"height\": 597, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.11079v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 919, \"height\": 570, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.11079v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 742, \"height\": 301, \"label\": \"Table\"}]"
motivation: 自进化技能积累会产生大量冗余重复内容，导致技能注入成本高、维护难，现有通用压缩、评估引导压缩都存在明显局限。
method: SkillZip采用无评估压缩思路，基于带硬覆盖约束的结构化最小描述长度目标，提取共享可复用结构保留必要例外。
result: 综合实验表明，SkillZip在压缩性能、泛化能力上均优于现有方法，额外开销低，支持高效本地增量更新。
conclusion: SkillZip无需依赖评估集，适配自进化智能体技能压缩需求，支持一次性压缩和持续写入式压缩两种模式。
---

## 摘要
自进化智能体通过追加成功流程与故障修复积累可复用技能。随着时间推移，相同需求往往会在多个分支、示例和提示中被重复表述，公共动作序列也多是被复制而非复用，最终得到的技能注入成本高昂，且难以维护。通用提示压缩并不适配该场景，因为技能并非扁平文本：技能的名称与描述定义了其适用场景，工作流控制执行过程，工具与输出契约约束了有效性，即便没有采样任务触发，罕见异常仍可能是必不可少的内容。评估引导的压缩可以测试这些行为，但它会引入轨迹推演、额外成本，且依赖压缩阶段的评估集。我们提出SkillZip，这是一种无评估方法，通过寻找技能最短的忠实结构解释完成压缩。核心直觉为：一次解释，多处引用——在对应适用范围将重复规则陈述一次，把重复动作序列提取为共享流程，仅将差异保留为显式异常。我们将该直觉形式化为作用于技能契约与残差的带类型最小描述长度目标，同时对所有提取的触发条件、工作流边、工具需求、约束义务与输出字段施加严格覆盖约束。该表述自带简单的共享阈值，通过构造即可保留唯一的稀有规则，且支持高效局部更新。SkillZip包含两种模式：仅需一次结构化抽取调用与确定性优化的单次模式，以及可在不重放任务、不重新解析完整历史的情况下集成每一次自进化补丁的持续写入时压缩（Zip-on-Write）模式。通过全面实验评估，我们验证了SkillZip在压缩性能、泛化能力与成本开销方面的有效性和优越性。

## Abstract
Self-evolving agents accumulate reusable skills by appending successful procedures and failure fixes. Over time, the same requirement is often restated in several branches, examples, and warnings, while common action sequences are copied rather than reused. The resulting skill becomes expensive to inject and difficult to maintain. Generic prompt compression is ill-suited to this setting because a skill is not a flat passage: its name and description define when it applies, its workflow controls execution, its tool and output contracts constrain validity, and rare exceptions may remain essential even when no sampled task activates them. Evaluation-guided compression can test these behaviors, but it introduces rollouts, cost, and dependence on the compression-time evaluation set. We present SkillZip, an evaluation-free method that compresses a skill by finding its shortest faithful structural explanation. The intuition is explain once, reference many: state a repeated rule once at the scope where it applies, factor a repeated action sequence into a shared procedure, and keep only the differences as explicit exceptions. We formalize this intuition as a typed minimum description-length objective over a skill contract and a residual, subject to a hard coverage constraint for every extracted trigger, workflow edge, tool requirement, obligation, and output field. The formulation provides simple sharing thresholds, preserves unique rare rules by construction, and supports efficient local updates. SkillZip has a one-shot mode with one structured extraction call and deterministic optimization, and a continual Zip-on-Write mode that integrates each self-evolution patch without replaying tasks or reparsing the full history. Through comprehensive experimental evaluations, we demonstrate the effectiveness and superiority of SkillZip in compression performance, generalizability, and cost overhead.

---

## 论文详细总结（自动生成）

# SkillZip论文详细总结

## 1. 核心问题与研究背景
- 自进化智能体通过追加成功流程、故障修复来积累可复用技能，每次更新为本地追加而非全局重构，长期积累后会产生严重的技能膨胀问题：相同规则、动作序列被重复复制到多个分支，文本长度持续增长，而真正新增的过程知识早已饱和。
- 膨胀的技能会占用更多上下文空间，提升每次调用的预填成本，还容易掩盖关键执行指令，增加维护难度。
- 现有方法存在明显局限：
  - 通用提示压缩针对当前查询筛选重要token，不适合需要适配所有未来任务的可复用技能，且无法识别技能的结构化语义，容易误删稀有但关键的异常规则。
  - 评估引导压缩（如SkillReducer）需要依赖压缩阶段的任务集做验证，不仅带来大量轨迹推演的额外成本，还容易过拟合到压缩时的评估集，漏掉未被采样的关键规则。

## 2. 提出的方法论
### 核心思想
遵循“一次解释，多处引用”的原则，将技能建模为带类型的结构化契约，在不接触任何下游任务、评估数据的前提下，寻找技能的最短忠实结构解释：提取共享可复用结构，仅保留真正差异作为显式异常，保证压缩的同时不丢失任何原技能的要求。

### 关键技术细节
1. **结构化技能契约建模**：将输入技能解析为带类型的结构化契约 `C(S) = ⟨I, G, T, C, O, E⟩`，各部分分别为：接口（名称、触发条件等）、工作流（动作、分支、终止条件等）、工具协议（工具要求、参数约束等）、带作用域的规则（必须/禁止行为，带适用范围）、输出契约（输出格式、必填字段、验证规则）、支撑证据（关联到对应契约元素的示例、原理）。解析不确定的内容直接锁入残差，原封不动保留，避免误删。
2. **形式化优化目标**：基于最小描述长度（MDL）原则，优化目标为：找共享单元库 `K` 和残差 `R`，最小化 `L(K) + L(R|K)`（即共享结构长度加残差长度），约束为**所有原技能提取的必需契约单元都必须被覆盖**，从构造上保证任何规则都不会被随意删除，稀有规则的保留不依赖任务采样频率。
3. **四类结构复用规则**：仅当共享后的总长度短于原重复表示时才会启用共享：
   - 等价需求合并：将复述相同要求的内容合并为一个共享规则，仅保留差异作为残差
   - 跨作用域规则提升：将所有子分支都重复的规则移动到最近公共父作用域，冲突的分支行为保留为显式异常
   - 重复工作流提取：将多次出现的相同动作序列提取为共享过程，原位置替换为调用
   - 变体规则整合：将多个带守卫的相近规则整理为一个通用规则加显式异常
4. **两种运行模式**：
   - 一次性压缩：适配已完成进化的成熟技能，流程为：扫描原技能→结构化提取契约→生成类型兼容的复用候选→确定性选择最短满足覆盖约束的表示→渲染压缩文本→结构审计补全缺失内容
   - Zip-on-Write持续压缩：适配增量自进化流程，每次新补丁仅在受影响的局部作用域做优化，统计复用频率，达到阈值后再对当前紧凑契约做全局重打包，不需要重放历史任务、重解析完整历史，效率高。

## 3. 实验设计
- 测试基准/数据集：共3个覆盖不同场景的基准：
  1. BFCL-v4 Web Search：评估多步网页检索与推理能力
  2. LiveMathematicianBench：评估带精确约束的数学推理能力
  3. SpreadsheetBench：评估真实场景的电子表格操作能力
- 测试模型骨干：覆盖不同能力层级与不同模型家族，共3种：Qwen3.7-Max、Qwen3.6-Plus、Kimi K2.6
- 对比基线：共5种：
  1. 无技能：骨干模型不加载任务特定技能
  2. 人工技能：原始人工编写的种子技能
  3. 未压缩进化技能：自进化后得到的完整未压缩技能
  4. SkillReducer：当前最接近的现有技能压缩方法，作为核心对比基线
- 核心研究方向：共5个，分别验证技能增长规律、压缩保真度、压缩成本、跨模型泛化能力、持续压缩效果。

## 4. 资源与算力
文中未明确提及具体的GPU型号、使用数量、训练/压缩的硬件时长等算力细节，仅给出了不同压缩方法的端到端总时间开销对比。

## 5. 实验数量与充分性
- 核心任务性能测试：共覆盖 3个基准 × 3种模型骨干，共9组对照实验
- 额外拓展实验：包含压缩效率对比、跨模型泛化测试、持续压缩不同激活时机的消融对比，共3组额外分析
- 实验设计的客观性：自进化的训练集与最终测试集完全隔离，压缩过程不接触任何测试数据，评估公平无偏差
- 整体来看，实验覆盖了压缩率、保真度、效率、泛化、增量场景等核心维度，实验设计充分、结论可靠。

## 6. 主要结论与发现
1. 自进化智能体的技能膨胀问题真实存在：技能平均会增长到初始长度的5倍以上，即使真正新增的过程知识已经饱和，文本长度仍会持续增加，压缩是自进化智能体技能维护的必要环节。
2. SkillZip可实现平均31.2%的压缩率，压缩后任务性能不低于甚至略高于未压缩技能（平均得分0.577 vs 未压缩0.570），大幅优于现有基线SkillReducer（压缩率31.2% vs 9.2%，平均性能0.577 vs 0.544）。
3. 压缩效率远高于评估引导方法：平均端到端速度比SkillReducer快3.5倍，且完全不需要任务推演（rollout），额外开销极低。
4. 跨模型泛化能力更好：压缩后保留了清晰的结构化规则，跨模型迁移的性能保留率高于现有方法。
5. Zip-on-Write持续压缩效果显著：从自进化第一轮就启用压缩可将最终技能长度控制在初始长度的1.6~1.9倍（无压缩为2.5~3.7倍），且不会牺牲任务精度，后期再激活压缩只能部分缓解已经产生的膨胀。

## 7. 方法与实验的亮点
- 首次提出完全无评估的技能压缩框架，避免了评估引导压缩的过拟合风险与高成本问题，完美适配自进化智能体的技能维护场景。
- 通过结构化契约建模与硬覆盖约束，从构造上保证稀有关键规则不会被误删，保真度远高于通用提示压缩方法。
- 同时支持一次性离线压缩与增量持续压缩两种模式，增量更新仅需局部优化，不需要重放全历史，效率高。
- 输出为人类可读的普通文本技能，不依赖特定模型，方便版本管理、检查与跨 backbone 复用。
- 采用保守策略：解析不确定的内容直接原封保留，搭配后置结构审计，尽可能避免静默删除关键内容的问题。

## 8. 不足与局限
- 压缩的保真度依赖结构解析的质量，保证仅针对解析出的契约单元，无法保证对任意模糊自然语言的解析都完全正确，但方法采用保守策略，风险为欠压缩而非误删。
- 方法更适配知识密集、冗余来自重复结构的自进化技能，对于混杂大量无关背景内容的通用公开技能，适配性不如评估引导的压缩方法。
- 尚未测试大规模社区级技能库（上万技能规模）的扩展性能，也未适配已经编译为可执行格式的技能压缩场景。

（完）
