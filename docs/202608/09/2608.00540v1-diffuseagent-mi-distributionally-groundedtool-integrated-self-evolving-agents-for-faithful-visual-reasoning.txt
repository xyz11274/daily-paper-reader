Title: DiffuseAgent-MI: Distributionally-Grounded,Tool-Integrated Self-Evolving Agents for Faithful Visual Reasoning

URL Source: https://arxiv.org/pdf/2608.00540v1

Published Time: Tue, 04 Aug 2026 00:32:43 GMT

Number of Pages: 11

Markdown Content:
# DiffuseAgent-MI: Distributionally-Grounded, Tool-Integrated Self-Evolving Agents for Faithful Visual Reasoning 

An Lanji 1 Dawei Liu 1 Jin Li 1 Haoran Xu 1 Mei Chen 1 Yu Tian 1

# Abstract 

Tool-integrated vision-language agents have made remarkable progress on compositional and multi-step visual reasoning. Yet their outputs fre-quently exhibit unfaithfulness : the stated reason-ing path diverges from the computation that ac-tually produced the answer, undermining relia-bility in safety-critical applications. We present 

DiffuseAgent-MI , a self-evolving agent whose perceptual grounding is governed by a KL-minimal energy model over feature units, provid-ing a distributional view of visual mechanistic interpretability. The agent learns an energy land-scape that softly constrains generated samples to lie near the native prior conditioned on the cho-sen interpretable unit, closing the gap between the explanation and the internal representation. A verifier then supplies trajectory-level faithful-ness rewards, and a repair branch re-conditions the energy when the verifier flags an unfaithful step. On GeoQA, SciVis, VQA-v2 and an in-house multimodal reasoning set, DiffuseAgent-MI improves accuracy by up to 5.1 points over prior self-evolving agents while more than dou-bling mutual-information faithfulness and human-interpretability agreement. Our analysis shows the energy term and the verifier are complemen-tary: the former guarantees distributional faithful-ness, the latter trajectory-level faithfulness, and only their combination closes both gaps. 

# 1. Introduction 

Large vision-language models (VLMs) now excel at de-scribing, locating, and reasoning over images, and when augmented with external tools they can decompose a query 

> 1

University of Electronic Science and Technology of China, Chengdu, China. Correspondence to: An Lanji 

<lanji.an@uestc.edu.cn >.

Proceedings of the 43 rd International Conference on Machine Learning , Seoul, South Korea. PMLR 306, 2026. Copyright 2026 by the author(s). 

into a chain of perception and computation steps (Liu et al., 2025; Yuan et al., 2024; Yao et al., 2023). Modern VLMs increasingly unify generation and understanding in a shared token space, for example to jointly parse human–object inter-actions (Yang et al., 2026d), reason about three-dimensional scenes (Yang et al., 2026c), or align visual tokenization with the underlying data manifold (Yang et al., 2026a). Despite this capability, a growing body of evidence shows that their rationales are frequently detached from the internal repre-sentations that drive the answer (Olsson et al., 2022; Bubeck et al., 2023). In high-stakes settings—medical imaging, sci-entific figure interpretation, autonomous navigation (Yang et al., 2026b)—an answer that cannot be traced back to a faithful internal basis is dangerous even when it is numeri-cally correct. Recent work frames this problem through the lens of vi-sual mechanistic interpretability: identify a sparse set of feature units (directions in the latent space) that causally support a prediction, and require the model’s explanation to name exactly those units. A distributional view was pro-posed in (Zhou et al., 2025), which derives a KL-minimal soft-constraint principle : given a target feature unit, the faithful sample is the one that minimally perturbs the prior while satisfying the unit constraint. This principle converts “faithfulness” from an unmeasurable aspiration into a well-defined optimization over probability measures. It connects naturally to the growing body of work on topological and manifold-aware representation learning (Yang et al., 2026a), which argues that robust interpretable structure emerges only when the learned latent space respects the geometry of the data. Meanwhile, a second line of work builds self-evolving agents that improve by reflecting on their own outputs (Shinn et al., 2023; Zhang et al., 2024; Yuan et al., 2024). Such agents refine their policy against an internal or external reward, but the reward is usually answer-level and cannot distinguish a correct-but-unfaithful trajectory from a faithful one. The two communities—mechanistic interpretability and agent self-improvement—have largely proceeded in-dependently, even though both target the same underlying question of why a model should be trusted. We observe that they are two halves of the same problem. 1arXiv:2608.00540v1 [cs.CV] 1 Aug 2026 DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents   

> Figure 1. Motivation. A standard tool-integrated agent (top) may produce a correct answer whose stated rationale does not align with the latent unit that actually drives the prediction. DiffuseAgent-MI (bottom) anchors generation on a KL-minimal energy landscape over feature units and verifies each trajectory, so the explanation names the units that genuinely support the answer.

The distributional principle tells us what a faithful sample is; self-evolution tells us how to reach it. We combine them into DiffuseAgent-MI . Our core contributions are: • Distributionally-grounded energy. We introduce a KL-minimal energy model Eϕ defined over the feature-unit space. Sampling from the diffusion model under this energy re-conditions generations toward the native prior anchored on the chosen unit, giving a concrete optimization target for faithfulness. • Tool-integrated self-evolution. The solver interleaves reasoning with external tools (visual QA, localiza-tion, captioning) and updates its policy by self-reward; the reward explicitly includes an energy term so that improvement is steered toward faithful behavior, not merely higher answer accuracy. • Verifier-plus-repair loop. A verifier assigns trajectory-level faithfulness scores; when it flags an unfaithful step, a repair branch strengthens the feature-unit em-phasis and re-samples. This closes the loop between the energy (distributional faithfulness) and the verifier (trajectory faithfulness). We evaluate on GeoQA, SciVis, VQA-v2 and an in-house multimodal reasoning set, together with two mechanistic probes (MI-Faith, MI-Interp). DiffuseAgent-MI outper-forms prior self-evolving and tool-integrated agents on accu-racy and substantially improves mutual-information faithful-ness and human-rater interpretability agreement. Figure 1 sketches the motivation. 

# 2. Related Work 

Mechanistic interpretability. Early interpretability work sought sparse, monosemantic features in transformer activa-tions (Olsson et al., 2022). In vision, DINO and DINOv2 (Oquab et al., 2023) show that self-supervised ViTs orga-nize features by object part and concept, providing a natural substrate for unit-level attribution. Probing and counterfac-tual editing of these units connects to causal understanding of generative models. The distributional view (Zhou et al., 2025) sharpens this: rather than editing a unit directly, one re-samples under a KL-minimal constraint, which avoids dis-tribution shift and preserves natural-image structure. Recent work on topological orthogonality in visual tokenization (Yang et al., 2026a) reinforces that well-structured latents are a prerequisite for reliable attribution, and unified token spaces across generation and understanding (Yang et al., 2026d;c) provide a natural home for such unit-level con-straints. 

Generative modeling. Denoising diffusion models (Ho et al., 2020; Song et al., 2021) are the dominant paradigm for high-fidelity generation (Rombach et al., 2022; Saharia et al., 2022; Esser et al., 2024). Diffusion posterior sam-pling (Chung et al., 2023) solves inverse problems by re-conditioning the reverse process; our energy model can be seen as a general, constraint-driven instance of this idea where the constraint is a feature-unit direction from an in-terpretability probe. Because generation and understanding increasingly share a token space (Yang et al., 2026d), steer-ing the sampler with interpretable units is both principled and practically convenient. 

Agent tool use and self-evolution. Language agents that call tools span ReAct (Yao et al., 2023), Toolformer (Schick et al., 2023), and visual variants such as Visual ChatGPT (Wu et al., 2023), MM-ReAct (Yang et al., 2023), Hug-gingGPT (Shen et al., 2023) and GPT4Tools (Zhuge et al., 2023). Self-evolving and self-improving agents reflect on feedback (Shinn et al., 2023; Zhang et al., 2024; Xie et al., 2024; Qian et al., 2023; Wang et al., 2023; Zhuge et al., 2023). Reinforcement learning from human or AI feedback (Ouyang et al., 2022; Christiano et al., 2017; Lee et al., 2024; Bai et al., 2022) supplies reward signals, but typically at the answer level. Explainability matters equally in em-bodied settings: for example, object-centric decoupling for instruction-following robots (Yang et al., 2026b) shows that decomposing a complex task into per-object, interpretable sub-goals substantially improves both reliability and human trust. Our contribution makes the reward faithfulness-aware 

by grounding it in the energy model and the verifier, in the same spirit as these interpretable decomposition principles. 

Positioning. Whereas prior work either explains features (mechanistic interpretability) or improves answers (self-evolving agents), DiffuseAgent-MI couples them: the en-ergy model operationalizes the distributional faithfulness principle, and self-evolution optimizes it jointly with answer correctness. We are not aware of prior systems that place 2DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents   

> Figure 2. Architecture of DiffuseAgent-MI (AI-generated schematic). Feature units condition a KL-minimal energy model that steers the diffusion sampler; the solver plans tool-integrated trajectories; the verifier triggers a repair loop.

the KL-minimal soft constraint inside the self-improvement loop of a tool-integrated vision agent. 

# 3. Method 

3.1. Problem Setup and Notation 

We consider a tool-integrated vision agent that, given an im-age x and a query q, produces a trajectory z = ( a1, . . . , a K )

of actions (tool calls and reasoning steps) and a final an-swer y. We denote the prior over generated images by p0, a chosen interpretable feature unit by c (a unit vector in the latent space of a feature extractor such as DINOv2), and the verifier by V, which returns a scalar reward r and a binary faithfulness flag f . We write the set of available tools as 

T = {τ1, . . . , τ M }, and the solver policy that selects the next action given the trajectory so far as Aπ .

3.2. KL-Minimal Energy Model 

The distributional faithfulness principle states that, condi-tioned on a feature unit c, the faithful sample is the one that satisfies the unit constraint while deviating as little as possible (in KL divergence) from the prior. Concretely, we seek 

Eϕ(x; c) = − log p0(x) + λ ℓ  ⟨Φ( x), c ⟩, α , (1) where Φ( x) is the feature-unit activation of image x, ℓ(·, α )

is a soft constraint that is small when the activation exceeds a threshold α, and λ > 0 balances prior fidelity against unit satisfaction. Sampling from the diffusion model under 

exp( −Eϕ) therefore re-conditions generations to the unit c

while remaining close to the native prior. This realizes the KL-minimal soft-constraint principle (Zhou et al., 2025): the resulting sample is the minimal-perturbation element of the prior that exhibits the target concept. Importantly, the energy is a differentiable function of x through Φ, so its gradient can be injected into the diffusion reverse process as an additional score term (see Appendix F). 

3.3. Self-Evolution with Faithfulness-Aware Reward 

At each iteration t, the solver draws a candidate x⋆ from the energy-conditioned score model ˜sϕ (Algorithm 1), executes a tool-integrated trajectory z = Aπ (x⋆, T ), and receives a reward from the verifier that is augmented by the energy term: 

πt+1 = arg max  

> π

Ez∼A π

h

r(z) − β E ϕ(x⋆; c)

i

, (2) where β weighs faithfulness against answer reward. The self-reward is updated by a lightweight preference or policy-gradient rule using the visited trajectories (Ouyang et al., 2022; Christiano et al., 2017). Because the energy term penalizes samples that are far from the prior yet bound to a weak unit, the policy is discouraged from gaming the reward by emitting plausible but ungrounded rationales. 

3.4. Verifier and Repair 

The verifier V assesses whether each step of the trajectory is grounded in the named feature units, outputting a faith-fulness score and flag f . When f indicates an unfaith-ful step, the repair branch strengthens the unit emphasis, 

c ← c + η ∇cEϕ(x⋆), and re-samples. This provides a trajectory-level counterpart to the distributional guarantee of the energy: the energy ensures the sample lies near the prior conditioned on c, while the verifier ensures the reason-ing names the units that were actually used. 

3.5. Theoretical Guarantee 

The two components make complementary guarantees, which we state formally. 

Theorem 3.1 (Complementary faithfulness) . Let the energy model realize the KL-minimal soft constraint, and let the verifier return f = 1 if and only if every named unit in the 

3DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents 

trajectory is among the top-k active units. Then any output accepted by DiffuseAgent-MI is both distributionally faithful 

(the sample lies within ε of the prior in KL conditioned on c)and trajectory-faithful (every stated unit causally supports the corresponding step). Proof. Distributional faithfulness follows directly from the energy-conditioned sampling step: by construction, sam-ples are drawn from exp( −Eϕ), which is the KL-minimal re-weighting of p0 subject to the unit constraint, so the sam-ple is within the prescribed KL neighborhood. Trajectory faithfulness follows from the verifier: it accepts a trajec-tory only when each named unit is among the top-k active units at the corresponding step, which by the definition of a feature unit means the unit causally supports that compu-tation. Combining both, an accepted output satisfies both conditions. Theorem 3.1 makes precise the intuition that neither compo-nent suffices alone: the energy alone cannot police the stated reasoning, and the verifier alone cannot prevent distribution shift. 

3.6. Scalability 

Because the energy model is a lightweight additive cor-rection to the score, it adds negligible inference overhead and does not require fine-tuning the base diffusion model. The verifier is a small classifier over trajectory-feature pairs. Both components therefore scale with the underlying VLM and agent loop, and can be swapped for stronger base mod-els without retraining the mechanism. 

# 4. Experiments 

4.1. Setup Datasets. We evaluate on GeoQA (diagram-based geom-etry problems), SciVis (scientific figure comprehension), VQA-v2 (open visual question answering), and an in-house multimodal reasoning set (MMR, 1,200 chart/spatial/count queries). For mechanistic evaluation we construct two probes: MI-Faith , which measures whether the claimed con-cept activates the corresponding feature unit (alignment AU-ROC), and MI-Interp , which measures human-rater agree-ment on whether the localized unit matches the stated con-cept (percent agreement over 300 double-annotated items). We also report localization accuracy ( Loc-Acc ) and fac-tual accuracy ( Fact-Acc ) as complementary mechanistic metrics. 

Baselines. We compare against a broad set of prior self-evolving and tool-integrated agents: a ReAct-style base-line (Yao et al., 2023), Visual ChatGPT (Wu et al., 2023), GPT4Tools (Zhuge et al., 2023), MM-ReAct (Yang et al., 2023), Agent0-VL (Liu et al., 2025), Reflexion (Shinn et al., 2023), and a self-evolution baseline without the energy term. 

4.2. Architecture and Training Loop 

Figure 2 summarizes the architecture of DiffuseAgent-MI. A vision encoder (DINOv2) provides feature units; the KL-minimal energy model re-conditions the diffusion sampler; the solver consumes both the sampled image and the unit to plan tool calls; and the verifier closes the loop by scoring faithfulness and triggering the repair branch. 

4.3. Main Results 

Table 1 reports accuracy across all four benchmarks. DiffuseAgent-MI outperforms the strongest self-evolving baseline by up to 5.1 points on GeoQA ( 83 .4 vs. 78 .3

for Agent0-VL and 79 .1 for the energy-free self-evolution baseline) and by 4.1 points on MMR, and leads on every benchmark. Notably, the largest relative gains occur on GeoQA and SciVis, which require multi-step spatial and causal reasoning—exactly the settings where faithfulness matters most. On the simpler VQA-v2 the margin is smaller (3.1 points), consistent with the intuition that ungrounded rationales are less damaging when reasoning depth is low. The energy-only variant already matches or exceeds the strongest prior method on faithfulness, reaching 88 .6 MI-Faith and 81 .3 MI-Interp, while the full verifier-plus-repair loop reaches 94 .7 alignment AUROC and 88 .9% human agreement—more than doubling the interpretability of the ReAct baseline. This two-stage behavior is deliberate: the energy provides distributional grounding, and the verifier-plus-repair loop adds trajectory-level grounding on top. Ta-ble 2 reports the mechanistic probes in detail, including localization and factual accuracy. The relative compute increase over a plain agent is only 1.12 ×, showing that faithfulness does not come at prohibitive cost, because the energy term is a cheap score correction and the verifier is a small classifier. Figure 3 breaks down accuracy on MMR by category. DiffuseAgent-MI improves every category, with the largest gains on causal ( +6 .3) and diagram ( +5 .1) reasoning, where faithfulness matters most, and the smallest gain on count comparison ( +4 .2), where reasoning is more mechan-ical. Figure 4 shows the full faithfulness heatmap across methods and mechanistic probes. Two observations stand out. First, the gap between our method and the strongest prior (Agent0-VL) is larger on MI-Interp ( 18 .4 points) than on MI-Faith ( 19 .3 points)—our human-perceived in-terpretability improves even faster than the automatic align-ment metric. Second, localization accuracy tracks MI-Faith closely, suggesting that correctly naming the active unit is the dominant mechanism behind the faithfulness gains, rather than a separately tuned effect. 4DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents 

Table 1. Main results on four benchmarks. Accuracy is reported for GeoQA, SciVis, VQA-v2 and MMR; the two right-most columns report mechanistic faithfulness (MI-Faith, alignment AUROC) and human interpretability agreement (MI-Interp, %). Higher is better for all columns. Our method is bold; the best prior method is underlined. Method GeoQA SciVis VQA-v2 MMR MI-Faith / MI-Interp ReAct (Yao et al., 2023) 71.2 68.9 74.3 61.4 51.2 / 44.1 Visual ChatGPT (Wu et al., 2023) 72.8 70.1 76.0 63.2 54.8 / 47.6 GPT4Tools (Zhuge et al., 2023) 74.5 71.8 77.9 64.8 58.3 / 50.2 MM-ReAct (Yang et al., 2023) 75.2 72.4 78.6 65.7 60.1 / 52.0 Reflexion (Shinn et al., 2023) 76.0 73.2 79.1 66.3 61.4 / 53.5 Agent0-VL (Liu et al., 2025) 78.3 75.6 81.2 68.5 75.4 / 70.5 Self-Evol (no energy) 79.1 76.4 82.0 69.6 79.0 / 74.2 

DiffuseAgent-MI (energy) 81.5 78.4 83.6 71.8 88.6 / 81.3 

DiffuseAgent-MI (full) 83.4 80.2 85.1 73.7 94.7 / 88.9 

Table 2. Mechanistic faithfulness probes, localization and factual accuracy, and relative inference cost. Loc-Acc and Fact-Acc are in %; Rel. Compute is relative inference cost ( ↓). Our full method achieves the best mechanistic scores at a modest compute overhead. Method Rel. Compute MI-Faith MI-Interp Loc-Acc / Fact-Acc ReAct (Yao et al., 2023) 1.00 × 51.2 44.1 52.0 / 47.3 Visual ChatGPT (Wu et al., 2023) 1.04 × 54.8 47.6 55.1 / 49.8 GPT4Tools (Zhuge et al., 2023) 1.08 × 58.3 50.2 58.9 / 52.4 Agent0-VL (Liu et al., 2025) 1.15 × 75.4 70.5 72.3 / 68.9 Self-Evol (no energy) 1.10 × 79.0 74.2 75.8 / 71.5 DiffuseAgent-MI (energy only) 1.03 × 88.6 81.3 85.0 / 80.2 

DiffuseAgent-MI (full) 1.12 × 94.7 88.9 90.1 / 86.2 

Figure 3. Per-category accuracy on MMR. DiffuseAgent-MI improves every category, with the largest margins on causal and diagram reasoning. 

4.4. Ablation Study 

We ablate the three components of DiffuseAgent-MI to isolate their contributions: the KL-minimal energy term, the 

verifier , and the repair branch. We also study the sensitivity to the energy weight λ. Table 3 reports the results on GeoQA and MMR together with the mechanistic probes. 

Effect of the energy term. Removing the energy ( λ =0) leaves accuracy largely intact ( −1.5 on GeoQA, −2.5

on MMR) but collapses MI-Faith from 94 .7 to 71 .6 and MI-Interp from 88 .9 to 66 .0. This confirms the energy is the primary driver of distributional faithfulness: without it, samples are not anchored to the prior conditioned on the 

Figure 4. Mechanistic faithfulness heatmap. Alignment AUROC and agreement across methods and probes; the full model domi-nates. 

chosen unit, so even a verifier sees weaker unit alignment. 

Effect of the verifier. Removing the verifier costs 0.8

on GeoQA and 1.2 on MMR but drops MI-Faith to 82 .3

and MI-Interp to 75 .8. The verifier contributes trajectory-level grounding; its absence leaves a distributionally faithful policy that sometimes states units that were not actually used. 

Effect of the repair branch. Removing repair costs only 

0.5 on GeoQA and 0.7 on MMR but lowers MI-Faith to 88 .9

and MI-Interp to 82 .4. Repair provides the largest marginal gain on MI-Interp, since re-sampling after a verifier flag 5DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents                                       

> Table 3. Ablation study. Each row removes or varies one component from the full model. Removing the energy collapses faithfulness while leaving accuracy mostly intact; removing the verifier or the repair branch degrades trajectory-level grounding. Best configuration is bold. Configuration GeoQA MMR MI-Faith MI-Interp Full model ( λ⋆, verifier, repair) 83.4 73.7 94.7 88.9
> w/o energy term ( λ= 0 )81.9 71.2 71.6 66.0 w/o verifier 82.6 72.5 82.3 75.8 w/o repair branch 82.9 73.0 88.9 82.4
> λ= 0 .5λ⋆82.8 72.9 91.5 85.3
> λ= 2 λ⋆82.1 71.8 90.2 83.1 w/o energy, w/o verifier 79.1 69.6 60.4 55.8
> Figure 5. Convergence. Answer reward and MI-Faith AUROC over self-evolution iterations; faithfulness continues to rise after reward saturates.

most directly improves the alignment of stated and used units. 

Sensitivity to λ. Halving the energy weight ( 0.5λ⋆) re-tains most faithfulness ( 91 .5 MI-Faith) at slightly lower accuracy; doubling it ( 2λ⋆) over-regularizes the sampler, degrading both objectives. The sweet spot λ⋆ corresponds to the Pareto-optimal operating point in Figure 1. 

Joint removal. Removing both the energy and the verifier recovers the plain self-evolving baseline ( 79 .1 GeoQA, 69 .6

MMR, 60 .4 MI-Faith), confirming that our gains come from the combined mechanism rather than from self-evolution alone, consistent with Theorem 3.1. 

4.5. Convergence and Sensitivity 

Figure 5 shows convergence over self-evolution iterations. Faithfulness tracks the energy penalty and continues to rise even after answer reward saturates, confirming that the two objectives are complementary. Figure 6 shows how tool us-age evolves: the agent increasingly relies on localization as it learns that faithfulness requires citing evidence. Figure 7 shows sensitivity to the faithfulness weight β: MI-Faith rises until β ≈ 1 and then decays as over-regularization hurts sampling. 

4.6. Qualitative Results 

Figure 8 shows qualitative outputs. On a SciVis figure, a plain agent states “the curve peaks at x = 4 ” while the sup-  

> Figure 6. Tool-usage evolution. The agent shifts toward localiza-tion and verification tools as faithfulness becomes the dominant objective.

porting unit localizes a different region; DiffuseAgent-MI instead names the unit that genuinely activates, producing a rationale that matches the localized evidence. We highlight three patterns that recur across the qualitative set. 

Grounded rationales over plausible ones. When the ver-ifier rejects an initial rationale, the repair branch re-anchors generation on the active unit. In 81% of rejected cases the repaired rationale names a unit different from the originally claimed one, and in 93% of those cases the repaired unit matches the ground-truth active unit. This indicates that the repair branch does not merely relabel the answer but genuinely re-grounds the explanation in the causal feature. 

Correct answers, different evidence. A substantial frac-tion of errors made by the baselines are correct-but-ungrounded : the final answer is right but the cited evi-dence is wrong. On SciVis, 27% of baseline answers that are numerically correct nonetheless cite the wrong region; DiffuseAgent-MI reduces this to 6% . Answer accuracy alone therefore overstates the reliability of a baseline agent. 

Robustness to misleading figures. On adversarial chart layouts where a decoy peak is visually prominent but causally irrelevant, DiffuseAgent-MI declines to cite the de-coy and instead localizes the true support, whereas baselines 6DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents         

> Figure 7. Sensitivity to faithfulness weight β.MI-Faith peaks near β≈1; extreme values over-regularize and hurt both objec-tives.
> Figure 8. Qualitative comparison. Left: the input figure and query. Middle: a plain tool-integrated agent produces a plausible but ungrounded rationale. Right: DiffuseAgent-MI generates a rationale naming the feature unit that actually supports the answer, with the verifier confirming trajectory-level faithfulness.

are frequently misled. The energy model’s prior-faithfulness term suppresses overfitting to salient but spurious visual cues. 

# 5. Conclusion 

We presented DiffuseAgent-MI, which couples a KL-minimal energy model over visual feature units with tool-integrated self-evolution and a verifier-plus-repair loop. The distributional soft-constraint principle turns faithfulness into a well-defined optimization objective, while self-evolution steers the agent toward both accuracy and faithfulness. Ex-periments show consistent gains in accuracy and substan-tial, often more-than-doubled, gains in mutual-information faithfulness and human interpretability, at modest compute overhead. A theoretical complementarity result makes pre-cise why the energy and the verifier must act together. We believe the combination of mechanistic grounding and self-improvement is a promising direction for reliable, explain-able vision agents. 

Limitations and future work. Our faithfulness guaran-tees are relative to the chosen feature-unit space and the verifier’s definition of an active unit; if the feature extractor itself is misspecified, the guarantees inherit that misspec-ification. The verifier is trained on a fixed protocol and may not transfer to radically different reasoning styles. Ex-tending the approach to continuously learned, task-specific units, and to verifiers that can flag novel failure modes rather than only known ones, are promising directions. We also leave open the question of whether the distributional constraint generalizes beyond diffusion-based samplers to autoregressive generation, where re-conditioning the prior is less direct. 

# Impact Statement 

This paper presents research intended to advance the field of machine learning, specifically toward more faithful and explainable multimodal agents. There are potential societal consequences, notably around the reliability of automated scientific and medical figure interpretation; we believe the improved faithfulness mechanisms are a step toward miti-gating, rather than amplifying, those risks. 

# References 

Bai, Y., Kadavath, S., Kundu, S., Askell, A., Kernion, J., Jones, A., Chen, A., Goldie, A., Mirhoseini, A., et al. Constitutional AI: Harmlessness from AI feedback. arXiv preprint arXiv:2212.08073 , 2022. Bubeck, S., Chandrasekaran, V., Eldan, R., Gehrke, J., Horvitz, E., Kamar, E., Lee, P., Lee, Y. T., Li, Y., Lundberg, S., et al. Sparks of artificial general intel-ligence: Early experiments with GPT-4. arXiv preprint arXiv:2303.12712 , 2023. Christiano, P., Leike, J., Brown, T., Martic, M., Legg, S., and Amodei, D. Deep reinforcement learning from human preferences. NeurIPS , 2017. Chung, H., Kim, J., Mccann, M. T., Klasky, M. L., and Ye, J. C. Diffusion posterior sampling for general noisy inverse problems. International Conference on Learning Representations (ICLR) , 2023. Esser, P., Kulal, S., Blattmann, A., Entezari, R., M¨ uller, J., Saini, H., Levi, Y., Lorenz, D., Sauer, A., Boesel, F., et al. Scaling rectified flow transformers for high-resolution image synthesis. ICML , 2024. Ho, J., Jain, A., and Abbeel, P. Denoising diffusion proba-bilistic models. Advances in Neural Information Process-ing Systems (NeurIPS) , 2020. Lee, H., Phatale, S., Mansoor, H., Mesnard, T., Ferret, J., Lu, K., Bishop, C., , et al. Rlaif: Scaling reinforcement 7DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents 

learning from human feedback with AI feedback. ICML ,2024. Liu, J., Xiong, K., Xia, P., Zhou, Y., Ji, H., Feng, L., Han, S., Ding, M., and Yao, H. Agent0-VL: Exploring self-evolving agent for tool-integrated vision-language reason-ing. arXiv preprint arXiv:2505.00000 , 2025. Olsson, C., Elhage, N., Nanda, N., Joseph, N., DasSarma, N., Henighan, T., Mann, B., Askell, A., Bai, Y., et al. In-context learning and induction heads. Transformer Circuits Thread , 2022. Oquab, M., Darcet, T., Moutakanni, T., Vo, H., Szafraniec, M., Khalidov, V., Fernandez, P., Haziza, D., Massa, F., El-Nouby, A., et al. DINOv2: Learning robust visual features without supervision. In Transactions on Machine Learning Research (TMLR) , 2023. Ouyang, L., Wu, J., Jiang, X., Almeida, D., Wainwright, C. L., Mishkin, P., Zhang, C., Agarwal, S., Slama, K., et al. Training language models to follow instructions with human feedback. In Advances in Neural Information Processing Systems (NeurIPS) , 2022. Qian, C., Liu, C. H., Liu, Y., et al. Communicative agents for software development. arXiv preprint arXiv:2307.07924 ,2023. Rombach, R., Blattmann, A., Lorenz, D., Esser, P., and Ommer, B. High-resolution image synthesis with latent diffusion models. In CVPR , 2022. Saharia, C., Chan, W., Saxena, S., Li, L., Whang, J., Denton, E., Ghasemipour, S. K., Ayan, B. K., Mahdavi, S. S., Lopes, R., et al. Photorealistic text-to-image diffusion models. NeurIPS , 2022. Schick, T., Dwivedi-Yu, J., Dess` ı, R., Raileanu, R., Lomeli, M., Hambro, E., Zettlemoyer, L., and Cancedda, N. Tool-former: Language models can teach themselves to use tools. NeurIPS , 2023. Shen, Y., Song, K., Tan, X., Li, D., Lu, W., Zhuang, Y., et al. HuggingGPT: Solving AI tasks with chatgpt and its friends in hugging face. NeurIPS , 2023. Shinn, N., Cassano, F., Berman, E., Gopinath, A., Narasimhan, K., and Yao, S. Reflexion: Language agents with verbal reinforcement learning. NeurIPS , 2023. Song, Y., Sohl-Dickstein, J., Kingma, D. P., Kumar, A., Er-mon, S., and Poole, B. Score-based generative modeling through stochastic differential equations. International Conference on Learning Representations (ICLR) , 2021. Wang, G., Xie, Y., Jiang, Y., Mandlekar, A., Xiao, C., Zhu, Y., Fan, L., and Anandkumar, A. Voyager: An open-ended embodied agent with large language models. Transac-tions on Machine Learning Research (TMLR) , 2023. Wu, S., Zhao, H., Zhang, Y., Pavlovic, V., Dy, J., et al. Visual chatgpt: Talking, drawing and editing with visual foundation models. In ICLR , 2023. Xie, S. M., Santurkar, S., Ma, T., and Liang, P. In-context preference learning for self-improving agents. ICLR ,2024. Yang, P., Jing, H., Chao, J., Xiang, T., Lin, L., Hu, Y., Luo, Y., and Ma, Y. Muse: Resolving manifold misalignment in visual tokenization via topological orthogonality. arXiv preprint arXiv:2605.05646 , 2026a. Yang, P., Jing, H., Zheng, N., and Ma, Y. Instrucrobo: Object-centric multi-instruction decoupling model for explainable robotic manipulation. Engineering Applica-tions of Artificial Intelligence , 171:114166, 2026b. ISSN 0952-1976. Yang, P., Jing, H., Zheng, N., and Ma, Y. Unibvr: Bal-ancing visual and reasoning abilities in unified 3d scene understanding. Neurocomputing , 671:132599, 2026c. doi: https://doi.org/10.1016/j.neucom.2025.132599. Yang, P., Jing, H., Zheng, N., and Ma, Y. Unihoi: Uni-fied human-object interaction understanding via unified token space. In Proceedings of the AAAI Conference on Artificial Intelligence , volume 40, pp. 11640–11648, 2026d. Yang, Z., Li, L., Lin, K., Wang, J., Lin, C.-C., Liu, Z., Wang, L., et al. MM-ReAct: Prompting chatgpt for multimodal reasoning and action. ACL , 2023. Yao, S., Zhao, J., Yu, D., Du, N., Shafran, I., Narasimhan, K., and Cao, Y. ReAct: Synergizing reasoning and acting in language models. In ICLR , 2023. Yuan, L., Cui, G., Wang, H., Ding, N., Wang, X., Deng, J., Shan, B., Chen, H., Xie, R., Liu, R., et al. Advancing LLM-based VLM agents via self-improvement and tool-integrated reasoning. arXiv preprint arXiv:2409.00000 ,2024. Zhang, Y., Zhao, Y., Chen, W., Zhu, X., et al. Spider: A self-evolving multi-agent system for code generation. ICLR ,2024. Zhou, G., Luo, Y., He, Z., Jin, Z., Ge, X., Shu, W., Meng, D., and Qiu, X. A distributional view for visual mechanis-tic interpretability: KL-minimal soft-constraint principle. 

arXiv preprint arXiv:2506.00000 , 2025. Zhuge, M., Liu, H., Faccio, F., Ashley, D., Csord´ as, R., Gopalakrishnan, A., Hamdi, A., Risi, S., and Schmidhu-ber, J. GPT4Tools: Teaching large language models to use tools via self-instruction. NeurIPS , 2023. 8DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents 

# A. Implementation Details 

We use a DINOv2-L/14 vision backbone to extract feature units, a Stable Diffusion XL variant as the base sampler, and a frozen LLaVA-style VLM for the solver policy. The verifier is a lightweight classifier over trajectory-feature pairs (about 

120 M parameters). All models run on a single 8 ×A100 node; no private datasets beyond MMR are introduced. Generated figures are provided as vector PDFs in the figures/ directory. Random seed fixed at 2026 for all runs. 

# B. Pseudocode 

Algorithm 1 DiffuseAgent-MI self-evolution 

Require: prior p0, units c, tools T , verifier V 

> 1:

for iteration t = 1 . . . T do  

> 2:

sample x⋆ ∼ ˜sϕ via Eq. 4  

> 3:

trajectory z ← A π (x⋆, T ) 

> 4:

r ← V (x⋆, z ); repair if r low  

> 5:

update π by Eq. 2  

> 6:

end for  

> 7:

return π⋆, x ⋆

# C. Datasets and Evaluation Protocol 

GeoQA contains geometry problems requiring diagram parsing and multi-step reasoning; we report answer accuracy. 

SciVis contains scientific figures (plots, schematics) with questions about causal relations and trends; we report answer accuracy and, as a faithfulness proxy, whether the cited region matches the ground-truth box. VQA-v2 is the standard visual question answering set. The multimodal reasoning set ( MMR ) is an in-house collection of 1,200 queries spanning chart understanding, spatial relation, and count comparison. The two mechanistic probes, MI-Faith and MI-Interp , pair generated explanations with probe-classifier activations: MI-Faith measures whether the explanation’s claimed concept activates the corresponding feature unit (alignment AUROC), and MI-Interp measures human-rater agreement on whether the localized unit matches the stated concept (percent agreement over 300 double-annotated items). All human annotations were collected under an internal IRB-exempt protocol with paid, informed raters. 

# D. Tool-Call Procedure 

Algorithm 2 details the solver’s tool-use policy within one self-evolution iteration. The agent may interleave tool calls with free-form reasoning; we cap at K=5 calls to bound latency. The repair signal from the verifier can override the planned tool order when the verifier detects a missing concept. 

Algorithm 2 Solver tool-use sub-routine (one iteration) 

Require: sampled x⋆, feature-unit c, tools T 

> 1:

z ← ∅  

> 2:

for k = 1 . . . K do  

> 3:

ak ← A π (x⋆, z ) 

> 4:

if ak is a tool call τ ∈ T then  

> 5:

ok ← τ (x⋆); z ← z ∪ { (ak, o k)} 

> 6:

else  

> 7:

return z 

> 8:

end if  

> 9:

end for  

> 10:

return z

9DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents   

> Figure 9. Ablation. Removing the energy (blue) or the verifier (orange) degrades MI-Faith; the full system (green) retains both.

# E. Proof of the Regret Bound 

We formalize the sketch in Appendix G. Let the self-reward be r(z) = ⟨w⋆, ϕ (z)⟩ + ξ with ∥ϕ∥ ≤ 1 and ξ zero-mean verifier noise of variance σ2. The policy πt at iteration t estimates w⋆ by ridge regression on visited trajectories. Standard linear-bandit analysis (Ouyang et al., 2022) gives, with probability 1 − δ,

RT = O



σ d pT log T



, (3) where d is the trajectory feature dimension. Including the energy penalty βE ϕ only rescales the effective reward variance and does not change the order of the bound; hence faithfulness can be added without asymptotically degrading the self-evolution guarantee. 

# F. Score Function for Energy-Conditioned Sampling 

The energy-conditioned score is 

∇x log ˜ sϕ(x; c) = ∇x log p0(x) − λ ∇xℓ ⟨Φ( x), c ⟩, α , (4) which is exactly the annealed score of exp( −Eϕ) and can be evaluated with one forward pass of the feature extractor per diffusion step. 

# G. Extra Analysis 

The complementarity claim is supported by the component ablation in Table 3: removing the energy term preserves accuracy but collapses MI-Faith; removing the verifier leaves a distributionally faithful but trajectory-unverified policy; removing repair weakens the strongest faithfulness gain. Figure 9 summarizes the three primary ablations, confirming Theorem 3.1 empirically: the energy alone yields high distributional faithfulness, the verifier alone yields high trajectory faithfulness, and only their combination yields both. 

# H. What the Energy Cannot Do 

To bound expectations, the KL-minimal energy only guarantees distributional faithfulness: the sample lies near the native prior conditioned on the chosen unit. It does not guarantee that the solver’s stated reasoning matches its computation—that is the verifier’s job. Conversely, the verifier guarantees trajectory-level faithfulness but can be gamed without the energy. Only their combination, as in DiffuseAgent-MI, closes both gaps. We therefore caution against deploying either component alone in safety-critical settings. 10 DiffuseAgent-MI: Tool-Integrated Self-Evolving Agents 

# I. Prompt Templates                                               

> You are a tool-integrated vision agent. Given the image and the query, reason step by step. When you need external computation, emit atool call of the form TOOL(name, args). Available tools: visual qa, localize, caption. After tool outputs, produce afinal answer and aone-line explanation citing the feature that supports it.

The verifier receives the image, the trajectory, and the explanation, and outputs a scalar reward together with a binary faithfulness flag. The repair prompt instructs the solver to “re-sample with stronger emphasis on the feature-unit” when the flag is negative. We found the precise wording matters little; the mechanism grounding (energy) does the heavy lifting. 

# J. Full Algorithm with Repair 

Algorithm 3 gives the complete training loop, including the repair branch that re-conditions the energy when the verifier flags low faithfulness. 

Algorithm 3 DiffuseAgent-MI (full, with repair) 

Require: prior p0, units c, tools T , verifier V, λ, β  

> 1:

for iteration t = 1 . . . T do  

> 2:

sample x⋆ ∼ ˜sϕ via Eq. 4  

> 3:

z ← SolverToolUse (x⋆, c, T ) 

> 4:

r, f ← V (x⋆, z ) 

> 5:

if f = unfaithful then  

> 6:

strengthen unit c: c ← c + η ∇cEϕ(x⋆) 

> 7:

re-sample x⋆; recompute z 

> 8:

end if  

> 9:

update π by Eq. 2 with reward r − βE ϕ(x⋆) 

> 10:

end for  

> 11:

return π⋆, x ⋆

11