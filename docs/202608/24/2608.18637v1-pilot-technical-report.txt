Title: PILOT Technical Report

URL Source: https://arxiv.org/pdf/2608.18637v1

Published Time: Thu, 20 Aug 2026 00:39:02 GMT

Number of Pages: 42

Markdown Content:
2026-8-20 

# PILOT Technical Report 

PILOT Team 

Existing agentic approaches for recommendation system optimization remain fundamentally reac-tive—they adjust parameters in response to observed metric changes but lack the ability to proactively design controlled experiments, personalize strategies at the user-segment level, or accumulate reusable experimental methodology across tasks. We present PILOT (Proactive Insight Learner for Online Tree-Experiments), an LLM-agent framework that organizes three roles within a constrained control loop where deterministic services enforce all safety, statistical, and permission boundaries: (1) an Experiment Manager that drives the full experiment lifecycle—task intake, observation governance, anomaly recovery, and postmortem—by selecting only from a rule-generated legal-command envelope; (2) a Search Planner that proposes candidate decision trees for user-segment-level personalization, invoked only when the Manager requests planning; and (3) a Memory Curator that asynchronously distills experiment outcomes into strategy-level domain knowledge and provenance-tracked methodology, failure-isolated from the main loop. The Manager makes the agent proactive, the Planner enables population-level personalization beyond global tuning, and the Curator turns every completed task into a learning opportunity for the next. Deployed on Taobao’s platform with 5 experimental buckets, PILOT is compared against ROAM (Reactive Optimization with Agent-driven Moves), a free-exploration agent without lifecycle governance or structured hypothesis testing. PILOT achieves up to +1.40% IPV, +1.60% Core IPV, +0.96% transac-tion count, and +1.50% transaction amount, improving over ROAM’s best results (+1.00% IPV, +0.90% Core IPV, +0.60% transaction count, +1.13% transaction amount) while raising search efficiency from 53.3% to 93.3% (+40pp), with no human intervention throughout the experimental cycle. IPV Core IPV Transaction Count Transaction Amount 

1.00% ▴

1.40% ▴

1.60% ▴

0.90% ▴

0.96% ▴

0.60% ▴

1.50% ▴

1.13% ▴

Search Efficiency (%) 0% 53% 93% 

+53% +40% 

Engagement Commerce Efficiency Baseline ROAM PILOT 

Figure 1 | Online A/B results for PILOT and ROAM on Taobao Homepage Guess You Like. 

> © 2026 Taobao. All rights reserved arXiv:2608.18637v1 [cs.IR] 19 Aug 2026 PILOT Technical Report

# Contents 

1 Introduction 42 The PILOT Framework 5

2.1 Experiment Manager . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 62.2 Search Planner . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 72.3 Memory Curator . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 7

3 Experiment Manager 7

3.1 Experiment Preparation . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 73.2 Observation and Advancement . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 83.3 Deterministic Services and Statistical System . . . . . . . . . . . . . . . . . . . . . . . 10 3.4 Confirmation and Delivery . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 10 

4 Search Planner 11 

4.1 Problem Formulation . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 11 4.2 Pre-screening and Exploration . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 14 4.3 Candidate Verification . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 19 

5 Memory Curator 20 

5.1 Memory Architecture and Fork-Merge . . . . . . . . . . . . . . . . . . . . . . . . . . . 20 5.2 Reconciliation and Confidence Lifecycle . . . . . . . . . . . . . . . . . . . . . . . . . . 21 5.3 Strategy Evidence . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 24 5.4 Methodology Experience . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 25 5.5 Write Governance and Data Flywheel . . . . . . . . . . . . . . . . . . . . . . . . . . . 27 

6 Experiments 27 

6.1 Experimental Setup . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 27 6.2 Search Efficiency and Task Outcome . . . . . . . . . . . . . . . . . . . . . . . . . . . 29 6.3 Ablation Study . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 30 6.4 Case Study: One Complete Task, Five Buckets . . . . . . . . . . . . . . . . . . . . . . 31 

7 Conclusion 32 References 32 

> 2PILOT Technical Report

A Contributors 35 B Implementation Details 35 

B.1 Decision Tree Protocol . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 35 

C Agent Prompts 37 

C.1 Manager — Experiment Lifecycle Orchestrator . . . . . . . . . . . . . . . . . . . . . . 37 C.2 Planner — Validation Wave Action Selector . . . . . . . . . . . . . . . . . . . . . . . . 39 C.3 Curator — Post-Settlement Memory Writer . . . . . . . . . . . . . . . . . . . . . . . . 40 

D Illustrative Memory Entries 41 

> 3PILOT Technical Report

# 1. Introduction 

Industrial recommendation platforms run online experimentation as a continuous control loop: buckets are split, strategies are deployed, and metrics are watched to decide the next adjustment. This loop has traditionally been driven by human experimenters, and as the number of concurrent experiments and user segments grows, manual design and iteration no longer scale. Agentic approaches for recommendation have started to move such optimization decisions from humans to LLM agents. Beyond agents that select items with memory and tool use (Cao et al., 2026; Chen et al., 2026; Li et al., 2026a,b; Liao et al., 2026; Nguyen et al., 2026; Tao et al., 2026; Yang and Wang, 2026; Zhang et al., 2026b,d), simulate users for training and evaluation (Bougie et al., 2026; Xie et al., 2026; Zhang et al., 2023, 2026c; Zhou et al., 2026), or converse with users (Liu et al., 2025; Tang et al., 2025; Tran et al., 2026; Xu et al., 2025), a growing line of work treats the agent as a coordinator that acts on the recommendation system itself rather than on the recommendation list (Cheng et al., 2026; Lao et al., 2026; Liu et al., 2026; Mu et al., 2026; Wang et al., 2026; Wu et al., 2026; Zhang et al., 2026a). This coordinator setting is closest to ours, yet it shares three key limitations that constrain further progress: 

L1 Reactive response. Existing coordinators act as fast responders to observed metric change or detected failure, not as experimenters that propose and validate a hypothesis before it is deployed. 

L2 Global-only strategies. Optimization is applied at the level of the whole system or bucket, with no mechanism for the agent to personalize strategies to sub-populations of users within a bucket. 

L3 No methodology accumulation. Memory and self-evolving skills are increasingly common, but what accumulates is knowledge about the content of a single task; this knowledge is not distilled into the agent’s own experimental methodology, so it does not compound into a reusable capability that makes the agent faster or better at running the next experiment. To address these limitations, we present PILOT (Proactive Insight Learner for Online Tree-Experiments), an LLM-agent framework that operates like a human experimenter rather than ametric responder : it manages experiment lifecycles , proposes population-level personalization via decision trees , and distills reusable knowledge from each experiment . PILOT organizes three LLM roles within a constrained control loop, where deterministic services enforce all safety, statistical, and permission boundaries. The architecture of PILOT embodies the following three key capabilities—one for each LLM role—that directly answer L1 –L3 above: 

⋆ Experiment Manager (§3). The Experiment Manager drives the full experiment lifecycle — task intake, playbook assembly, observation governance, anomaly recovery, scale validation, and postmortem — rather than responding to metric alerts after the fact. At each step, a deterministic Manager Guard generates a legal-command envelope from the current state and frozen policies , and the Manager selects only from this envelope ( bounded ). High-risk decisions — estimand, guardrails, budget, final confirmation — require explicit user input rather than silent inference ( safe ), and a fixed authority hierarchy ensures that frozen safety policies and user-approved specifications always override agent reasoning ( auditable ). 

⋆ Search Planner (§4). Global-only strategies apply a single strategy to an entire bucket, even though users within it can need different treatment. The Search Planner proposes candidate 

> 4PILOT Technical Report

decision trees that personalize strategies at the user-segment level , binding a distinct strat-egy bundle to each leaf so differentiation only happens where user behavior actually differs (personalized ). Candidates are drawn from three lanes — a deterministic baseline, LLM-proposed hypotheses with evidence references, and a bounded exploration slot — and every candidate must clear a deterministic validator before entering the experiment ( validated ). The Planner is invoked only when the Experiment Manager explicitly requests planning; it cannot manage lifecycle, adjust observation windows, or promote Champions ( scoped ). 

⋆ Memory Curator (§5). The Memory Curator operates asynchronously after evidence settlement, 

distilling experiment outcomes into two distinct stores : a strategy evidence store that records bundle-contrast effects with population scope and evidence-role tagging ( contrastive ), and a methodology experience store that tracks how to run experiments with source-type provenance and a governed confidence lifecycle from draft to supported to approved ( governed ). Each task forks an isolated branch from the shared knowledge base and merges back only validated entries at task end. The next task therefore starts from a richer base without cross-task contamination (compounding ). 

PILOT has been deployed on Taobao’s recommendation platform across 5 experimental buckets, compared against ROAM ( Reactive Optimization with Agent-driven Moves), an agent-based configu-ration that performs free exploration without lifecycle governance or structured hypothesis testing. 

PILOT improves the best-bucket results from +1.00% to +1.40% IPV, from +0.90% to +1.60% Core IPV, from +0.60% to +0.96% transaction count, and from +1.13% to +1.50% transaction amount, while raising search efficiency from 53.3% to 93.3% (+40 pp), requiring no human intervention throughout the experimental cycle. The remainder of this report is organized as follows. §2 overviews the PILOT framework and its constrained control loop. §3–§5 detail the Experiment Manager, Search Planner, and Memory Curator respectively. §6 reports online A/B results, ablations, and a case study of hypothesis evolution, and §7 concludes. 

# 2. The PILOT Framework 

PILOT places the LLM at three positions that most need judgment: lifecycle management, candidate proposal, and evidence distillation, and wires all three into one constrained control loop (Figure 2). Everything else in the system is deterministic. An event (a user request, a platform signal, a timer, or an executor callback) triggers the Manager Guard to work out which actions are currently allowed: a short list of legal next steps, derived purely from the state machine and frozen policy, without LLM involvement. The Experiment Manager then picks one action from that list, optionally delegating to the Search Planner. The Guard checks the pick against the current state, its parameter bounds, and any outstanding certificates; the State Committer then applies it atomically and is the only component allowed to write state. Six deterministic services carry out this loop: the Manager Guard and State Committer described above, a Statistics Engine that produces immutable decision certificates, a Contract Builder that writes down the pre-registered rules for judging an experiment’s outcome, a validator that checks any candidate the Search Planner proposes, and a builder that enumerates which actions are currently legal. LLM roles only choose among options these services already generated : they cannot invent a command, alter a statistical conclusion, or step outside a permission boundary. Beneath these services, a foundation layer (Figure 2, bottom) provides persistent storage—a Memory store for fork-merge knowledge, a StrategyHub for bundle definitions and templates, and a Knowledge Base 

> 5PILOT Technical Report

Experiment Manager 

Task intake runbook Scale-up observe & decide postmortem 

> §3

Search Planner 

Hypothesis 

> Deterministic baselines
> LLM-proposed
> (hypothesis + evidence)
> Bounded exploration
> candidate selector

Decision Tree     

> high-activity
> Bundle #12
> low-activity
> Bundle #07
> split prune hash split
> numeric split collapse update
> root
> §4

Memory Curator §5

Strategy Evidence Methodology Experience      

> Bundle #07 to Bundle #12 :+IPV (xx%), +PV (xx%)
> Pairwise
> Av.s. B
> Extreme
> push limit
> Falsify
> validate
> Shared knowledge base
> Task A
> branch
> Task B
> branch
> PR gate PR gate
> task dispatch (synchronous) task dispatch (synchronous)

PILOT’s core improvements 

reactive → PROACTIVE global-only → PERSONALIZED No accumulation → METHODOLOGY FLYWHEEL 

Figure 2 | The framework overview of PILOT . PILOT ’s three LLM roles within the constrained control loop, each addressing one limitation of existing agentic coordinators: the Experiment Manager drives proactive lifecycle control, the Search Planner enables population-level personalization via decision trees, and the Memory Curator lets experimental methodology evolve across tasks. for domain knowledge—along with an Online Runtime that handles population routing and bundle dispatch at serving time. 

2.1. Experiment Manager 

The Experiment Manager is the semantic owner of the entire experiment lifecycle: task intake, assembling an operating playbook for the task, progressing the experiment by choosing only from the actions currently allowed, observation governance, anomaly recovery, scale validation, and postmortem. It shifts the agent from reactive parameter tuning to proactive experiment management. Task intake ends with a frozen task specification agreed with the user, and an operating playbook assembled from the user’s own instructions together with methodology that has proven reliable on past tasks. Lifecycle progression then moves through four time layers that are easy to conflate but distinct in meaning: one look is a single data read paired with a state judgment, one wave is a batch of co-launched Challengers , one epoch is a window in which the Champion , its estimator, and the decision policy are frozen so every Challenger launched inside it shares one common comparison, and one search iteration is a complete plan-launch-observe-decide cycle. Observation governance tracks every look’s outcome as provisional until a result is judged final; only a final result may update Memory or unlock the next round of planning. §3 details the full lifecycle. 

> 6PILOT Technical Report

2.2. Search Planner 

The Search Planner is the candidate proposer: it reads the current Champion , the list of currently legal actions, evidence, and Memory, and returns a shortlist of legal candidates together with hypotheses and evidence references. It is invoked only when the Experiment Manager explicitly asks for a plan, and it cannot manage lifecycle, adjust observation windows, allocate traffic, or promote Champions. Every candidate it proposes is a Challenger : a variant that carries exactly one atomic action relative to the current Champion , the best confirmed tree and the platform’s search reference, itself compared against a fixed baseline bucket, B0 , that anchors the platform and stays invariant across rounds. Before a Challenger is deployed, a set of decision rules is agreed in advance and bound to it, fixing what counts as a positive, negative, inconclusive, or extended result, so the rules cannot be adjusted once the data starts coming in. §4 details the decision-tree model, the space of legal actions, and three-lane candidate generation. 

2.3. Memory Curator 

The Memory Curator is the asynchronous evidence distiller: after evidence settlement, it organizes experiment outcomes into two memory stores, one holding bundle-contrast domain knowledge with evidence-role tagging, and the other holding provenance-tracked experimental methodology with a governed confidence lifecycle. It is failure-isolated: a Memory Curator failure degrades knowledge accumulation, not the experiment itself. The first store never records a strategy’s effect on its own; every entry contrasts one strategy bundle against another and tags whether the underlying evidence supports a causal claim or only a diagnostic one. The second store tracks where a piece of methodology came from and whether it has only been tried once or has proven reliable across tasks, and only proven methodology is allowed to feed future playbooks without a human in the loop. §5 details the two memory stores, fork-merge isolation, and write governance. 

# 3. Experiment Manager 

This section details how the Experiment Manager hosts one complete experiment: converging a task into a frozen specification and playbook (§3.1), observing each round and deciding when to advance it (§3.2), and confirming and delivering the result once a round reaches its target (§3.4). 

3.1. Experiment Preparation 

Before any search activity begins, the Experiment Manager turns a raw request into three frozen artifacts: it first understands what the user wants, then assembles how the task should be run, and finally fixes the order in which these artifacts can override one another. 

Task specification. The Experiment Manager is first a dialogue agent. A user’s objective arrives as unstructured natural language, and the Manager cannot forward it to the Search Planner directly: it must converge the dialogue into a versioned task specification, denoted T , before any search activity begins. A task specification T is a versioned record covering the fields listed in Table 1. Once frozen, 

T is immutable for the remainder of the task, and no running experiment wave reads an unfrozen draft of it. Convergence proceeds through four completion states, incomplete , awaiting , ready ,

> 7PILOT Technical Report

Table 1 | Fields of a task specification T , grouped by category and confirmation tier.     

> Category Fields Tier
> Population and search Fixed baseline (

B0 ), initial Champion , search universe Standard Governance Scale validation goal and policy, notification policy, user methodology references Standard Budget and schedule Observation schedule constraints, start and deadline, and traffic, bucket, and round budget Mixed Objective and metrics Objective, minimum business effect, and estimand and primary metric Mixed Population and search Randomization and exposure spec High-risk Governance Guardrail and stop policy, approval matrix High-risk 

and frozen , each requiring the missing or ambiguous fields identified at the previous state to be resolved before advancing. For a subset of high-risk fields, the Manager must not silently infer a value: it must issue a structured request that lists the allowed values, states a recommended default, and sets a response deadline, and T cannot advance past awaiting until the user responds. 

Playbook assembly. Converging the dialogue into T tells the Manager what the task should test, but not how to run it. Before the task starts, the Manager also prepares the operating space the task will run in: it selectively pulls together the business knowledge, memory, analysis methods, strategies, and methodology relevant to this task , drawing first on whatever the user has already confirmed in the current dialogue, and filling any remaining gaps with what has proven reliable on past tasks. How this pool of methodology is sourced, governed, and kept trustworthy across tasks is the subject of §5; here it is enough to know that the outcome of this preparation is the playbook P,the operating manual the task consumes at runtime. Before P is frozen, the Manager Guard checks that it covers every decision the task will face and that nothing in it exceeds the permissions of its source. 

Authority hierarchy. Once T and P are frozen, the Manager’s choices for the remainder of the task follow a fixed order of precedence: the frozen safety and statistical policy always overrides the frozen task specification, which overrides the frozen playbook, which overrides approved methodology, which in turn overrides a provisional or unconfirmed hint, which overrides the Agent’s own reasoning. A source higher in this order can never be overridden by one lower in it, even when the Agent’s own reasoning points elsewhere. 

3.2. Observation and Advancement 

Figure 3 illustrates the full round lifecycle across three example rounds. Every Challenger is derived from the current Champion by applying exactly one atomic action (e.g., split on user_age, update_strategy leaf #3) , denoted by the Δ1 badge on each Challenger box. Within each round, the observation panel runs a four-step pipeline—collect metrics, diagnose anomalies, check data health, certificate decision—before the Manager may issue a certificate. As the figure shows, this pipeline is not always uniform: Round 1 closes cleanly, Round 2 detects a sample-ratio mismatch that extends the observation window before the round can close, and Round 3 closes cleanly after an extended window. When a round ends with a promote certificate, the winning Challenger ascends to become 

> 8PILOT Technical Report

the next round’s Champion (as in Rounds 1 and 3); when it ends with a reject , the Champion 

carries over unchanged (as in Round 2; note that the subscript in the figure tracks the round count, not whether the underlying tree changed). After the final promotion, a "Target Met?" gate checks whether the task objective in T is satisfied: if yes, the task exits the search loop into staged scale-up, independent confirmation on 𝐷 confirm , and production delivery; if no and budget remains, the dashed loop-back arrow returns the task to a new round. If no budget remains, the search halts. 

Observing a round. Once a round is running, the Experiment Manager watches it on a fixed observation plan: at each look it collects the round’s metrics, diagnoses any fluctuation or assignment anomaly (a sample-ratio mismatch, a data delay, an exposure break), and decides the round’s window, whether to keep waiting, extend, or close. These calls rest only on data-health signals such as freshness, maturity, sample size, and integrity, never on the treatment lift direction, which stays hidden until a close is actually warranted; an “almost significant” reading, a favorable date cherry-picked after the fact, or a window trimmed to flatter the result are all out of bounds. When a look shows an anomaly that looks like noise, it is marked diagnostic-only, so it does not count as evidence, and the window is extended so a clean look can still come in; a recurring anomaly, or one that breaches a hard bound, pauses the round without closing it and escalates to a human. An unresolved anomaly never gets to influence whether the Champion changes. B0 — Fixed Baseline (platform anchor, invariant across all rounds) 

Champion 0

Challenger 1a 

> Δ1action

Challenger 1b 

> Δ1action
> split at user_age update leaf #3

Round 1    

> collect diagnose check certificate

Success  

> Observation PROMOTE
> Challenger 1a Win

Champion 1a 

Challenger 2a 

> Δ1action

Challenger 2b 

> Δ1action
> Update leaf #2 update leaf #5

Round 2    

> collect diagnose check certificate

Block   

> Observation REJECT
> All Challengers LOSE

Champion 1a 

Challenger 3a 

> Δ1action
> Update leaf #1

Round 3    

> collect diagnose check certificate

Reach the target  

> Observation PROMOTE
> Challenger 3a WIN
> compare against B0 compare against B0 compare against B0
> SCALE UP
> User gate

Figure 3 | Champion–Challenger round lifecycle. Each round pits the current Champion against one or more Challengers under a fixed observation window, ending in a promote , reject , or 

continue certificate. A promoted Challenger becomes the next round’s Champion ; a rejected one leaves the Champion unchanged. The fixed baseline B0 anchors all comparisons across rounds. 

Round certificates. When the signals clear, the Manager closes the round with one of three certifi-cates. A continue certificate keeps the round running and schedules the next look. A promote cer-

> 9PILOT Technical Report

tificate ends the round in the Challenger’s favor: the Challenger becomes the new Champion ,and the task opens the next round, or moves on to confirmation if its target is already met (§3.4). A reject certificate fires when the result is negative or inconclusive. The Champion stays in place, and the task opens a new round if budget remains, or halts. Not every certificate is a real choice. A guardrail breach or an unresolved integrity failure leaves only one legal response. The deterministic services then mark that response as forced, and the Manager’s job narrows to confirming it or escalating. The facts a real decision would unlock stay withheld, since there is nothing left for them to decide. 

Advancing to a new round. A new round opens only after the previous round’s effect judgment completes and the task still needs to search: the target is not yet met, and budget remains in T .Opening it follows a fixed order. The Manager first writes the analysis specs that tell the deterministic services what to compute about the current search space. It then requests a plan from the Search Planner (§4); the Planner proposes candidates, and the Manager selects among them, never inventing candidates of its own. Each selected candidate becomes a Challenger , a tree carrying one atomic action relative to the current Champion . The Manager then launches the Challengers , and the platform compiles and deploys them for the next observation window. Every round consumes traffic and round budget from T , and the Manager tracks the remainder before opening the next. While a round runs, the Manager schedules the next look, marks a look diagnostic-only, extends the observation window, notifies stakeholders, and escalates on demand. It does not deploy a candidate or expand traffic without approval, alter a statistical conclusion, act on an action the deterministic services did not generate, or promote a Champion on search-period data alone. 

Failure and stopping semantics. The Manager does not always return a valid certificate. If its output is invalid or late, the system retries a bounded number of times and then falls back to a safe default, typically holding the round where it is or escalating to a human. Anything hard to reverse (deploying a candidate, expanding the traffic or round budget, or editing T ) always needs human approval; the Manager is never left to guess across that line. When a reject leaves no budget in T ,the search stops. This means only that the task’s rounds and traffic have run out—not that no further improvement is possible. 

3.3. Deterministic Services and Statistical System 

The lifecycle described in §3.2 relies on the Experiment Manager to make judgment calls—which certificate to issue, when to escalate, how to interpret an anomaly. But every such call is bounded by a layer of deterministic services that the Manager cannot override (see the "Deterministic Services & Statistical System" strip in Figure 2). Six services form this layer, each with a narrow, auditable responsibility. Table 2 summarizes each service, the artifact it produces, and the invariant it enforces. 

The key separation: LLM roles hold judgment authority (choose among admissible options); deterministic services hold enforcement authority (generate the option space, validate every choice, commit state). 3.4. Confirmation and Delivery Staged scale-up. A promote certificate that also meets the task’s target ends the search. From this point the Experiment Manager stops proposing new rounds and instead manages a staged handover from search traffic to production traffic. Scale-up proceeds through a sequence of stages. Each stage 

> 10 PILOT Technical Report

Table 2 | Deterministic services bounding LLM judgment.    

> Service Artifact Role
> Manager Guard

Control Envelope  

> Constructs admissible command set from state machine and frozen policy; validates the Manager’s pick before commit State Committer

State Transition Sole state writer; atomically applies Guard-approved commands Statistics Engine Decision Certificate Produces immutable per-look certificates; Manager may act on them but cannot alter conclusions Contract Builder Learning Contract Freezes outcome criteria and execution plan before data collection begins Candidate Validator Validation Report Per-action schema, envelope, and evidence checks on every Planner Proposal  

> Action Enumerator

Action Envelope Lists all legal atomic actions for the current Champion 

> tree; Planner cannot invent actions outside it

sets a traffic share, larger than the one before it, and a duration window with a minimum and a maximum. A stage waits at least its minimum before it can advance, and is re-evaluated by its maximum. At that point a certificate again resolves to hold , advance , or rollback , using the same observation signals as in §3.2. 

Independent confirmation and postmortem. Scale-up alone does not confirm the Champion .The data collected while searching, 𝐷 search , shaped the choice of Champion , so it cannot also serve as proof. Final confirmation evaluates the Champion against a single pre-registered test on 𝐷 confirm ,a freshly collected, held-out sample gathered only after the Champion was fixed. The test runs once. A result that is not significant on 𝐷 confirm cannot be rescued by looking again or by folding in 𝐷 search .Once confirmed, the Champion is delivered to production, and the task enters a postmortem. The Manager assembles a retrospective of what worked, what needed a retry or an escalation, and what the playbook P should record differently next time. This retrospective becomes a methodology patch consumed by the Memory Curator (§5). The task is not considered complete until the postmortem is resolved. 

# 4. Search Planner 

This section details the Search Planner: how the search problem is formulated (§4.1), how candidates are pre-screened, explored, and ranked before committing experiment budget (§4.2), and how the top-ranked candidates are verified through whole-bucket A/B testing in a multi-round loop (§4.3). 

4.1. Problem Formulation 

Table 3 collects the notation used throughout this section and subsequent sections. 

PolicyTree. A PolicyTree 𝑇 is a rooted tree that maps the entire user population to strategy bundles (Figure 4). Every internal node routes users by a single pre-treatment registered feature 

> 11 PILOT Technical Report

Table 3 | Notation used in the Search Planner and related sections. 

Symbol Name Description  

> 𝑇

PolicyTree A complete, executable mapping from the user population to strategy bundles  

> 𝑇 0

Fixed baseline ( B0 ) Platform-default tree, invariant across all rounds; anchors Δtotal  

> 𝑇 ∗

Champion Current best confirmed tree; search reference within a reference epoch  

> 𝑇 ∗
> 𝑎

Challenger Champion plus exactly one atomic action 𝑎 ; the unit of search 

A ( 𝑇 ∗) ActionEnvelope Finite set of legal atomic actions for the current Champion , enumerated deterministically  

> 𝑎

Atomic action One of five kinds: split , prune , hashExpand , collapse , updateStrategy  

> ℓ

Leaf Terminal node of a PolicyTree; every user hits exactly one leaf 

> 𝑏

(ℓ) Bundle binding Strategy bundle assigned to leaf ℓ

S SearchUniverse Registered features F , registered bundles, tree constraints, and search budget 

F Feature set Pre-treatment registered features available for splits 

Y ( 𝑇 ) Primary online metric The observed value of the primary metric (e.g., IPV) under tree 𝑇 in the A/B platform 

> 𝑤

(ℓ) Global leaf share |users in ℓ| / | total users | 

> Δtotal

(𝑇 ) Total treatment effect 𝔼 [Y ( 𝑇 )] − 𝔼 [Y ( 𝑇 0)] ; measured against the fixed baseline  

> Δstep

(𝑎 ) Step treatment effect 𝔼 [Y ( 𝑇 ∗ 

> 𝑎

)] − 𝔼 [Y ( 𝑇 ∗)] ; measured against the current Champion 

> 𝜌

(𝑎 ) Resolvability Whether action 𝑎 can be conclusively evaluated within the remaining budget 

𝑓 ∈ F ; every leaf ℓ binds exactly one strategy bundle 𝑏 (ℓ). Formally, for a tree with leaves {ℓ1, . . . , ℓ 𝐿 },the mapping can be written as 

𝑇 (𝑢 ) = 𝑏 (ℓ𝑗 ), where 𝑗 = arg ℓ

𝑢 ∈ path (ℓ), (1) i.e., user 𝑢 is routed to the unique leaf whose path predicate it satisfies, and receives the strategy bundle bound to that leaf. Two structural invariants hold for every valid tree: 1. Completeness and mutual exclusion : for any user 𝑢 in the population, there exists exactly one leaf ℓ such that 𝑢 satisfies the path predicate from root to ℓ. The leaf predicates partition the population into disjoint, exhaustive segments—no user falls through, and no user matches two leaves. 2. Canonical identity : semantically equivalent trees—those that route every user to the same bundle—produce the same canonical hash, regardless of node ordering or syntactic differences. This property ensures that the system can detect when two structurally different trees encode the same policy, preventing redundant experiments. Internal nodes come in three types that differ in how they partition users: • split : categorical feature match—routes users by an enumerated set of feature values (e.g., 

city ∈ {Shanghai , Beijing , Hangzhou }), with a default child for unmatched values. • numericSplit : numeric feature threshold or range—routes users by ordered comparisons ( <, ≤,

>, ≥, between ), supporting both single-threshold and multi-interval boundaries. • hashSplit : stable user-level hashing—computes a deterministic hash of a user identifier (via Mur-murHash3) to map users to percentage buckets, enabling stable sub-population experimentation where the same user always lands in the same branch. 

> 12 PILOT Technical Report

Figure 4 | Structure of a PolicyTree .

SearchUniverse. Before any search begins, the SearchUniverse S defines the boundaries within which all trees must live. It contains: ❶ the set of registered features F , each with its data type, pre-treatment availability certification, missing-value semantics, and allowed values or boundaries; 

❷ the set of registered strategy bundles, each a versioned configuration that the online runtime can execute; ❸ an initial tree 𝑇 (0) that seeds the search; and ❹ the hard tree constraints and search budget. The tree constraints are: • 𝑑 max : maximum tree depth—limits the length of any root-to-leaf path, preventing overly specific population segments that lack statistical power. • Lmax : maximum leaf count—caps the total number of distinct strategy assignments, controlling deployment complexity. • 𝑤 min : minimum global leaf share—ensures every leaf receives enough traffic for the Statistics Engine to produce a conclusive DecisionCertificate within the budget. • Fmax : maximum distinct features—limits the number of different features used for splits across the entire tree, reducing the risk of overfitting to noise in high-dimensional feature spaces. The search budget specifies the maximum number of rounds Rmax , the total bucket-days available, the number of experiment buckets K, and the maximum concurrent Challengers per round. All these values are drawn from S and are not adjustable by the LLM; they are set by the user during task specification (§3.1) and frozen before the search starts. 

Search problem. The search begins from an initial tree 𝑇 (0) specified in the SearchUniverse S

and seeks a sequence of trees 𝑇 (0) , 𝑇 (1) , . . . , 𝑇 ( R ) , where each 𝑇 (𝑟 +1) is obtained from 𝑇 (𝑟 ) by applying exactly one atomic action 𝑎 (𝑟 ) ∈ A ( 𝑇 (𝑟 ) ). Let Y ( 𝑇 ) denote the primary online metric (e.g., IPV) observed under the A/B platform when tree 𝑇 is deployed to its assigned traffic bucket. The 

> 13 PILOT Technical Report

optimization objective is: max  

> 𝑎 (0),...,𝑎 (R− 1)
> Δtotal

 𝑇 ( R )  = 𝔼 Y  𝑇 ( R )  − 𝔼 Y ( 𝑇 0)

s.t. 



depth (𝑇 (𝑟 ) ) ≤ 𝑑 max ,

leaves (𝑇 (𝑟 ) ) ≤ L max ,𝑤 (ℓ) ≥ 𝑤 min ∀ ℓ ∈ leaves (𝑇 (𝑟 ) ),

{ 𝑓 : 𝑓 used in 𝑇 (𝑟 ) } ≤ F max ,

(2) for all 𝑟 = 0, . . . , R. These bounds—maximum tree depth 𝑑 max , maximum leaf count Lmax , minimum global leaf share 𝑤 min , and maximum distinct features Fmax —are drawn from S and are not adjustable by the LLM. They guarantee that every tree in the sequence is deployable online and that every leaf retains enough traffic for statistical resolution. 

B0, Champion and Challenger. Three trees coexist during any round (Figure 3). The fixed baseline  

> 𝑇 0

( B0 ) runs the platform’s default behavior throughout the experiment and never changes; it anchors Δtotal . The Champion 𝑇 ∗ is the current best confirmed tree and serves as the search reference within a reference epoch; every Challenger is measured against it to obtain the step-level effect  

> Δstep

(𝑎 ) = 𝔼 Y ( 𝑇 ∗ 

> 𝑎

) − 𝔼 Y ( 𝑇 ∗) . (3) The Champion is frozen within an epoch so that all Challengers launched inside it share a common comparison. A promote certificate (see §3.2) replaces the Champion with the winning 

Challenger ; the old search data then serves only as historical evidence, not as the basis for promotion. 

ActionEnvelope. Given the current Champion 𝑇 ∗, the remaining depth budget, and the constraints in S, the Action Enumerator (§3.3) deterministically constructs the ActionEnvelope A ( 𝑇 ∗): the finite, auditable set of all atomic actions that can legally be applied to 𝑇 ∗. Each action 𝑎 ∈ A ( 𝑇 ∗)

specifies a target leaf ℓ, an action kind, and the resulting candidate tree 𝑇 ∗ 

> 𝑎

. The five action kinds are: 1. split : partition a leaf on a categorical or numeric feature, creating child nodes with new bundle bindings. 2. prune : merge an existing split, collapsing its children back into the parent leaf. 3. hashExpand : add a hashSplit node for stable user-level sub-population experimentation. 4. collapse : undo a structural expansion, merging an expanded subtree back. 5. updateStrategy : change the bundle binding 𝑏 (ℓ) on an existing leaf without altering tree structure. The Search Planner may only reference action IDs already present in A ( 𝑇 ∗); it cannot invent actions outside the envelope. Actions not shortlisted by the Planner retain audit trails and remain eligible for the bounded exploration lane (§4.2.1). 

4.2. Pre-screening and Exploration 

The search is organized into two temporal phases (Figure 5). Round 0 completes pre-analysis and exploration traffic: it diagnoses feature quality, segments the population, runs small-scale within-population comparisons, and produces a priority-ranked candidate list. Rounds 1– 𝑁 then verify 

> 14 PILOT Technical Report

Figure 5 | Search Planner pipeline: pre-analysis and candidate recall (Stage 1), optional exploration traffic via hashSplit probing (Stage 2), and hypothesis-driven proposal generation (Stage 3). candidates through whole-bucket A/B testing in an iterative loop (§4.3). Round 0 results determine only the order in which candidates are verified; whether a candidate is ultimately adopted depends entirely on its whole-bucket A/B outcome. 

4.2.1. Pre-analysis and Candidate Recall 

Before any exploration traffic is allocated, the Search Planner runs a structured screening pipeline over the registered features in F . The pipeline proceeds in three stages: quality diagnosis, boundary generation, and historical de-prioritization. 

Feature-level quality diagnosis. For each registered feature 𝑓 ∈ F , the Planner checks four proper-ties: ❶ pre-treatment availability—whether 𝑓 can be observed before exposure, so that conditioning on it does not introduce post-treatment bias; ❷ missing rate—features with excessive missing values produce unstable splits and reduce effective sample size; ❸ temporal stability—whether the feature’s distribution shifts significantly across days, which would confound repeated-measurement designs; and ❹ coverage—whether the feature’s non-missing population is large enough for each resulting subgroup to meet the minimum leaf share 𝑤 min . Features that fail any hard threshold on these checks are excluded from the candidate set; those that pass enter the next stage with a quality score that influences their priority. 

Candidate boundary generation and population segmentation. For numeric features that pass quality screening, the Planner generates candidate split boundaries from a deterministic set: distribu-tion quantiles, known business-rule thresholds (e.g., membership tiers, purchase-count brackets), and 

> 15 PILOT Technical Report

historical points where strategy effects showed a change. For categorical features, the candidate set is the registered value enumeration. Each candidate boundary partitions the population into subgroups, and the Planner measures the exposure-weighted posterior variance—a proxy for how differently subgroups behave under the current policy: 

V ( 𝑓 , 𝑐 ) =∑︁ 

> 𝑔
> 𝑤 𝑔

 ¯𝑦 𝑔 − ¯𝑦 2, (4) where 𝑔 indexes the subgroups created by splitting feature 𝑓 at boundary 𝑐 , 𝑤 𝑔 is the exposure share of subgroup 𝑔 , ¯ 𝑦 𝑔 is the subgroup posterior mean of the primary metric, and ¯ 𝑦 is the overall posterior mean. A high V ( 𝑓 , 𝑐 ) indicates that subgroups under this split respond differently, making it a promising axis for personalization. Strongly correlated or near-duplicate features are identified and collapsed so that the downstream exploration budget is not spent on redundant comparisons. 

Historical de-prioritization. The Planner cross-references the surviving feature–boundary pairs against historical evidence from Memory (§5). Feature–strategy combinations that failed to produce whole-bucket gain in prior experiments are deprioritized, and combinations whose mechanism has been explicitly falsified are dropped entirely. Conversely, combinations with positive but inconclusive prior signal are promoted, since they represent the lowest-cost opportunity for a decisive test. The output of this stage is an ordered shortlist of feature–boundary–strategy triples, ranked by a composite of population variance, quality score, and historical prior, ready for exploration traffic allocation. 

4.2.2. Exploration Traffic and Candidate Ranking 

This stage is not always executed. The Planner activates it only when two conditions hold simultane-ously: ❶ the remaining experiment cycle in T is long enough to absorb an extra probing round without squeezing the verification budget in Rounds 1– 𝑁 , and ❷ the number of surviving feature–boundary pairs from §4.2.1 exceeds a threshold at which offline pre-analysis alone cannot reliably distinguish the most promising candidates. When the experiment cycle is short or the candidate set is already small, the Planner skips directly to three-lane candidate generation (§4.2.3), using the pre-analysis ranking as the sole priority signal. 

Probing via expansion actions. When exploration is activated, the Planner deploys lightweight probing experiments by inserting hashSplit expansion nodes into the current Champion tree. The 

hashExpand action from the ActionEnvelope serves this purpose: it inserts a hashSplit node that deterministically maps each user to a fixed branch via stable hashing. The same user always sees the same strategy across visits, enabling measurement of both immediate and cumulative effects (e.g., shifts in purchase frequency over a week). Because the assignment is user-level, each user contributes one independent observation per day, so sample-size accumulation tracks the number of exposed users rather than page views. 

Candidate ranking. After the probing window closes, the Planner ranks candidates by their per-population gain contribution:  

> 𝑔 pop

(𝑎, 𝑔 ) = 𝑤 𝑔 ·  ¯𝑦 cand  

> 𝑔

− ¯𝑦 ctrl 

> 𝑔

, (5) where ¯ 𝑦 cand  

> 𝑔

and ¯ 𝑦 ctrl  

> 𝑔

are the per-user posterior metrics under the candidate and control strategies in subgroup 𝑔 , and 𝑤 𝑔 is the exposure share of subgroup 𝑔 . Only results that meet two thresholds advance to the candidate list: ❶ the accumulated sample size reaches the minimum required for a 

> 16 PILOT Technical Report

Table 4 | Bottom-up trajectory summarization vs. top-down hypothesis testing.                    

> Bottom-up (trajectory →summary) Top-down (hypothesis →evidence)
> Knowledge source LLM post-hoc narrative over full trajectory Pre-registered falsifiable hypothesis Direction control Determined by LLM attention; uncontrol-lable Determined by Planner proposal; au-ditable Confidence No built-in measure; single-run and multi-run indistinguishable Grounded in statistical certificate; confi-dence = design power Negative results Typically discarded or vaguely noted Explicitly close a hypothesis branch; pre-vent re-exploration Hallucination risk High—narrative coherence ≠empirical va-lidity Low—every claim has a provenance chain

conclusive evaluation, and ❷ the estimated effect is stable across at least the required number of consecutive observation days. Failing either gate disqualifies the result from influencing candidate priority, though it may still inform the Planner’s understanding through the segment-diagnostic evidence role (§4.3). 

Global-share weighting. The per-population gains are aggregated into a single global contribution score for action 𝑎 by summing across all affected subgroups: 

> 𝑔

(𝑎 ) =∑︁  

> 𝑔
> 𝑔 pop

(𝑎, 𝑔 ) . (6) This weighting ensures that a subgroup with extreme local lift but negligible population share does not dominate the ranking. The final candidate priority is determined by 𝑔 (𝑎 ), so actions that improve large subgroups rank above those that produce large but narrow effects. 

4.2.3. Hypothesis-driven Candidate Proposal 

Motivation. Most existing LLM-driven optimization frameworks accumulate experience through a bottom-up pattern: the system collects a trajectory of experimental actions and outcomes, then asks the LLM to summarize the trajectory into reusable heuristics or rules Lao et al. (2026); Mu et al. (2026); Wu et al. (2026). While straightforward, this paradigm has three structural weaknesses: 1. High stochasticity. The summarization step relies entirely on the LLM’s post-hoc reasoning over noisy, high-dimensional trajectories. Different summarization runs on the same trajectory can yield contradictory conclusions. 2. Uncontrollable accumulation direction. Because the LLM decides after the fact which aspects of the trajectory matter, the system has no control over what knowledge is retained; experience grows along whichever axis the LLM happens to attend to. 3. No built-in confidence. A heuristic distilled from a single lucky run is indistinguishable from one confirmed across dozens of experiments, creating a fertile ground for hallucinated insights that propagate through subsequent decisions. PILOT replaces this bottom-up pattern with a top-down, hypothesis-first mechanism. Before any ex-periment budget is spent, the Search Planner is required to commit a falsifiable hypothesis—specifying the target population, the strategy change, the expected causal mechanism, and the criteria under 

> 17 PILOT Technical Report

which the hypothesis would be considered refuted. Evidence is then collected specifically to test that hypothesis through pre-registered A/B comparison. A hypothesis that survives verification is persisted as a high-confidence experience record with its supporting evidence; one that is refuted is persisted as a negative result that closes a branch of the search space. In both cases, the experience carries an auditable provenance chain (hypothesis → contract → data → certificate), and its confidence is determined by the statistical design rather than by the LLM’s narrative coherence. Table 4 summarizes the comparison. 

Case: A PlannerProposal in Round 2 Context. Round 1 applied split on purchase_count at boundary ≥ 3, producing two leaves. The whole-bucket A/B result showed a positive Δstep that met the promotion threshold; the Challenger 

was promoted to Champion 1 . Segment-diagnostic evidence revealed that within the high-purchase leaf ( purchase_count ≥ 3), users with visit_frequency ≥ 5/week responded 2 .3× more strongly than others, but this observation carries only diagnostic weight and cannot trigger promotion on its own. 

ActionId : split_leaf_03_on_visit_frequency_ge5 

Ordinal Priority : 1 (exploit) 

Hypothesis : “Splitting the high-purchase leaf ( purchase_count ≥ 3) by visit_frequency at threshold 5 and assigning the enhanced ranking formula S2 to the high-frequency subgroup will improve whole-bucket IPV. Reject if : the whole-bucket Δstep is non-positive after the full observation window, or if the GMV guardrail declines by more than 0.5%.” 

Evidence Refs : [round_1_certificate, segment_diag_visit_freq_heterogeneity] 

Expected Mechanism : High-frequency visitors in the high-purchase segment have more exposure opportunities per unit time; a ranking formula optimized for repeat engagement ( S2 ) should convert these exposures more efficiently than the generic formula ( S0 ). 

Outcome for Next Decision :

If confirmed → The split on visit_frequency is validated. Next round: explore whether a further numericSplit on avg_session_duration within the high-frequency subgroup can capture an additional effect. Persist: “ visit_frequency ≥ 5 × S2 is positive for high-purchase users.” 

If refuted → Close the visit_frequency branch for this population. The segment-diagnostic heterogeneity did not survive whole-bucket validation. Persist: “ visit_frequency split does not produce whole-bucket gain in the high-purchase segment; do not re-prioritize.” Next round: investigate the second-ranked hypothesis (alternative strategy S3 on the same leaf without further splitting). 

Hypothesis formulation. When the Experiment Manager issues a RequestPlan , the Planner reads the current Champion , the ActionEnvelope A ( 𝑇 ∗), all accumulated evidence (pre-analysis results, exploration signals if available, and outcomes of prior verification rounds), and relevant entries from Memory (§5). From this input, the Planner performs four reasoning steps: ❶ review the previous round’s hypothesis outcome—supported, refuted, or still undetermined; ❷ identify the uncertainty that most limits the next decision; ❸ select actions from A ( 𝑇 ∗) that either exploit a known positive signal, explore an under-tested region, or falsify a standing assumption; and ❹ for each selected action, specify how different experimental outcomes would change subsequent search direction. The exploit–explore–falsify trichotomy ensures that the Planner’s proposals are not uniformly greedy: falsification proposals are valued precisely because a negative result permanently prunes a hypothesis branch, accelerating convergence even when no positive gain is found. 

> 18 PILOT Technical Report

PlannerProposal. The Planner’s sole formal output is a PlannerProposal . Each proposed action carries six fields: • ActionId : must exist in the current A ( 𝑇 ∗).• Ordinal Priority : the Planner’s ranking within its own shortlist. • Hypothesis : a falsifiable statement specifying the target population, the strategy change, the expected direction, and the conditions under which the hypothesis would be rejected. • Evidence Refs : pointers to evidence entries that support the hypothesis. • Expected Mechanism : a causal sketch of how the action produces its effect. • Outcome for Next Decision : a conditional plan—if the hypothesis is confirmed, which follow-up action to pursue; if refuted, which hypothesis branch to close and what alternative to investigate. The proposal must not contain treatment-effect estimates, standard errors, significance levels, posterior probabilities, traffic-allocation recommendations, promotion decisions, observation-window adjustments, or early-stop conclusions—these belong to the Statistics Engine and the Experiment Manager, not to the Planner. This separation ensures that the LLM’s contribution is limited to what to test and why , while whether the test succeeded is determined entirely by the deterministic statistical machinery. 

4.3. Candidate Verification 

Once pre-screening produces a ranked candidate list, the search enters an iterative verification loop (Rounds 1– 𝑁 ). Each round converts one or more top-ranked candidates into Challenger 

trees—each differing from the current Champion by exactly one atomic action—and evaluates them through whole-bucket A/B testing. The key design principle is that no candidate is adopted based on pre-screening evidence alone ; adoption requires a statistically conclusive whole-bucket comparison against the Champion .

Pre-registered decision criteria. Before a Challenger is deployed, the system freezes the decision criteria for that round: the hypothesis to be tested, the primary metric, the minimum effect size worth detecting, guardrail thresholds, and the rules that map each possible statistical outcome (positive, negative, inconclusive, or extend observation) to a concrete next action. Freezing these criteria before data collection prevents the common pitfall of post-hoc rationalization—adjusting what counts as “success” after seeing the results. The Planner specifies what to test; the deterministic statistical machinery decides whether it passed. 

Verification loop. Each round proceeds as follows. The Challengers are compiled into executable policy trees and deployed to the A/B platform alongside the Champion . During the observation window, the Experiment Manager collects metrics and monitors data health (§3.2). At the close of observation, the statistical system produces a verdict: promote (the Challenger becomes the new Champion ), reject (the Champion is retained), or continue (the observation window is extended). When multiple Challenger s run in parallel within the same round, statistical thresholds are jointly calibrated across the multi-arm comparison to control the overall error rate. After each verdict, the outcome is persisted as a strategy experience record—including the hypothesis, the target population, the metric result, and whether the whole-bucket threshold was met. Confirmed hypotheses become high-confidence positive experience; refuted hypotheses become negative experience that 

> 19 PILOT Technical Report

prevents future re-exploration of the same candidate. Both feed into the Memory Curator (§5), ensuring that subsequent rounds—and future experiments on the same search universe—benefit from accumulated evidence rather than starting from scratch. 

# 5. Memory Curator 

The Memory Curator transforms completed experiments and subsequent user feedback into reusable, cross-experiment knowledge. It maintains two complementary memory stores. The first, Strategy Evidence , organizes evidence around strategy–population bundles. Each entry associates a strategy with its target population, the resulting online metrics, and the observed positive or negative outcomes. The second store, Methodology Experience , captures operational knowledge about how experiments should be conducted. It records concise summaries of actions such as waiting, retrying, preserving traffic or sample volume, extending observation, ramping up traffic, and rolling back an intervention, together with their execution context, outcomes, and relevant user feedback. When new feedback is received, the Memory Curator reconciles operational records across experiments, analyzes the likely causes of recurring successes or failures, and uses the resulting evidence to refine the corresponding operational skills. A key design boundary separates task-level statistical conclusions from cross-task memory confidence .A terminal certificate issued by the Statistics Engine— promote , reject , or inconclusive —determines the next action for the current task: replacing the Champion , retaining it, or extending the experimental round. However, even a decisive certificate can create only a draft memory entry. No single experiment is sufficient to establish reusable cross-task knowledge. Advancing an entry from 

draft to supported and ultimately to approved requires consistent evidence from independent tasks, as assessed through the reconciliation process (§5.2). Memory remains advisory at every confidence level. It may influence strategy recall, candidate prioritization, experimental planning, and the selection or refinement of operational methods, but it never substitutes for a live DecisionCertificate . In particular, memory cannot directly justify a promotion, generate statistical evidence, or create or expand control permissions. Table 5 summarizes the two memory stores. Section 5.1 introduces their shared fork–merge architecture, while §5.2 defines the reconciliation procedure and confidence lifecycle used by both stores. Sections 5.3 and 5.4 describe Strategy Evidence and Methodology Experience, respectively. Finally, §5.5 specifies the write-governance rules and the self-improvement flywheel enabled by this architecture. 

5.1. Memory Architecture and Fork-Merge 

The two stores serve complementary roles within the experiment loop. Strategy Evidence (§5.3) records what experiments discovered : every entry contrasts one strategy bundle against another on a defined population, tagged with whether the observation supports a causal claim or only a diagnostic one. Methodology Experience (§5.4) records how to run experiments : observation windows, retry policies, scale-up ladders, rollback procedures, and monitoring cadences, each tracked with source provenance and a governed confidence level. When a new task begins, the Curator forks an isolated branch from the main knowledge base. The branch spans both stores: all EvidenceItem s, ScopedClaim s, and MethodologyPatch es produced during the task are deposited into this branch and remain invisible to other concurrent tasks. If two tasks run in parallel, each writes to its own branch; their results converge only at merge 

> 20 PILOT Technical Report

Table 5 | Comparison of the two memory stores maintained by the Memory Curator.   

> Strategy Evidence Methodology Experience
> Scope Domain knowledge: which bundle outper-forms which comparator, on which popu-lation Meta-knowledge: how to run experiments well under specific operational scenarios Retrieval key

(from_bundle_id, to_bundle_id, population_scope) (operation_type, scenario_predicate) 

> Data model

EvidenceItem (immutable per-round observations) aggregated into 

ScopedClaim (cross-round conclusions) 

PostmortemRecord (immutable per-task retrospectives) distilled into 

MethodologyEntry (evolving opera-tional recommendations) Written when A round closes with a promote ,

reject , or inconclusive certificate The task postmortem produces a method-ology patch Consumed by Search Planner: candidate recall and his-torical de-prioritization (§4.2.1) Experiment Manager: playbook assembly and runtime decisions (§3.1) Confidence driver Consistent ScopedClaim conclusions across independent tasks Consistent operational outcomes across in-dependent tasks 

time, never during execution. Figure 6 illustrates this lifecycle. On the left, the main memory—containing both Strategy Evidence and Methodology Experience—serves as the shared knowledge base. Each new task (A, B, C, . . . ) forks a local branch from this base; the branches run concurrently and in isolation, with no cross-visibility during execution. When a task’s evidence reaches terminal settlement, the Memory Curator Agent takes over: it verifies that the evidence is settled, reconciles the branch entries against the main store using the retrieval-key matching and three-case logic detailed in §5.2, and merges the approved updates back into the main memory. The confidence lifecycle ( draft → supported →

approved ) advances only through this cross-task reconciliation, and the updated main memory is reused by the next task, closing the compounding loop. The branch enters the merge process only after the task completes and its evidence reaches terminal settlement (§3.4). At that point, the Curator applies the reconciliation logic described in §5.2 to integrate the branch entries into the main stores. Should the merge itself fail—due to a reconciliation conflict the Curator cannot resolve, an LLM timeout, or any other curation error— the already-settled evidence, the Manager’s current decision, and the next Planning request all remain valid. Failure degrades knowledge accumulation for future tasks; it does not compromise the correctness of the task that produced the evidence. 

5.2. Reconciliation and Confidence Lifecycle 

The Memory Curator assigns each memory entry one of three confidence states: draft , supported ,or approved . These states quantify the degree of cross-task validation received by a claim, rather than the statistical strength of the experiment that initially produced it. • draft denotes a task-local observation that has been admitted to the shared memory but has not yet been independently replicated. Every newly created entry begins in this state, regardless 

> 21 PILOT Technical Report Strategy Evidence
> Methodology Experience

Main Memory  

> (shared Knowledge Base)
> Task C
> Task A
> Task B Fork
> (shared knowledge base)
> Memory Curator Agent Memory Curator Agent Memory Curator Agent
> Enriched Memory Next Tasks reconcile supported approved draft

Figure 6 | Memory fork–merge lifecycle in PILOT . The Curator forks an isolated branch from the shared main memory for each task, covering both Strategy Evidence and Methodology Experience. Task branches remain invisible to other concurrent tasks during execution. After terminal settlement, the Memory Curator Agent reconciles the branch with the main store, advances claim confidence through the governed lifecycle, and merges approved updates back into main memory for use by subsequent tasks. of the sample size or statistical significance of its originating experiment. • supported denotes a claim that has received at least one consistent confirmation from an inde-pendent task under the same retrieval scope. Such an entry represents a recurring observation, but the available evidence remains insufficient for unrestricted reuse. • approved denotes a claim whose supporting evidence has exceeded a governed cross-task confirmation threshold without unresolved material contradictions. Approved entries may be used autonomously for advisory functions such as strategy recall, candidate prioritization, playbook assembly, and operational-method selection. The nominal confidence progression is 

draft −→ supported −→ approved .

Confidence advances only when a claim is consistently confirmed by independent tasks. Repeated observations or evaluations derived from the same task count as a single source of evidence, and duplicate records provide no additional support. When contradictory evidence arises, the Curator re-evaluates the claim and may suspend its advancement or mark it as superseded if the opposing evidence becomes sufficiently strong. Every confidence transition and supersession is recorded together with its evidence and rationale; existing claims are never silently overwritten. 

Reconciliation procedure. Each task writes new observations to an isolated memory branch. After the task reaches terminal settlement, the Curator reconciles each branch entry against the correspond-

> 22 PILOT Technical Report

ing main store. It retrieves potentially related entries using a store-specific key: (from_bundle_id, to_bundle_id, population_scope) for Strategy Evidence, and (operation_type, scenari o_predicate) for Methodology Experience. For Strategy Evidence, the reconciled conclusion de-scribes the online effect of a strategy transition for a specified population. For Methodology Experience, it describes the observed utility or failure mode of an operational method in a specified scenario, incorporating experimental outcomes and relevant user feedback. For each incoming entry, reconciliation produces one of three outcomes: 1. Consistent evidence. If the incoming conclusion agrees with an existing claim under the same retrieval key, the Curator appends the task as a supporting reference. The first qualifying inde-pendent confirmation advances the entry from draft to supported . Once the accumulated independent confirmations satisfy the governed approval criterion, the entry advances from 

supported to approved . Further consistent observations strengthen the evidence base of an already-approved entry without changing its state. 2. Contradictory evidence. If the incoming conclusion conflicts with an existing claim under the same key, the Curator records it as a contradicting reference and re-evaluates the claim’s confidence. An isolated contradiction does not automatically erase or reverse the existing entry. Instead, the Curator preserves both the supporting and contradicting evidence, prevents unsupported confidence advancement, and applies the governed conflict-resolution rule. If independent contradictory evidence becomes sufficiently strong to overturn the existing claim, the original entry is explicitly marked as superseded, together with the evidence and rationale for that decision. The competing claim is materialized as a new draft entry and must independently progress through the same cross-task validation lifecycle. Thus, even strong counter-evidence cannot immediately create an approved replacement. 3. No matching claim. If no existing entry shares the retrieval key, the incoming entry is inserted into the main store as draft . It remains a provisional observation until subsequent independent tasks provide supporting or contradicting evidence. 

Cross-task confidence versus within-task significance. The confidence lifecycle is deliberately sep-arated from task-level statistical inference. The Statistics Engine determines whether an observed effect is sufficiently supported within the current experiment and issues a terminal DecisionCertificate .The Memory Curator instead determines whether the resulting claim is reliable across experiments. Consequently, a single task can produce only a draft entry, even when its sample size is large and its statistical conclusion is decisive. This separation prevents a one-off result from being prematurely promoted into generally reusable knowledge. A terminal inconclusive result is treated as a distinct semantic outcome rather than as negative evidence. It indicates that the task lacked sufficient information or statistical power to resolve the claim. The Curator may retain the associated execution record for diagnostic purposes, but it neither counts the result as a contradiction nor changes the confidence of an existing claim. 

Scope compatibility. Confidence transitions require exact compatibility of the retrieval scope. In Strategy Evidence, this includes the strategy transition and population predicate; in Methodology Experience, it includes the operation type and scenario predicate. Entries with overlapping but non-identical scopes may be retrieved as contextual evidence, but they do not directly drive confidence transitions. For example, evidence obtained for purchase_count ≥ 3 AND visit_freq ≥ 5 may inform 

> 23 PILOT Technical Report

the interpretation of a claim for purchase_count ≥ 3, but it cannot independently confirm or contradict that broader claim. This restriction prevents evidence from a narrow subpopulation or operational scenario from being incorrectly generalized to a broader scope. Where recurring scope-specific differences are observed, the Curator retains separate entries rather than forcing them into a single aggregate claim. 

5.3. Strategy Evidence 

Strategy Evidence records cross-task knowledge about the relative effectiveness of strategy bundles: which bundle performs better than which baseline, for which population, and with what confidence. After an experiment round terminates with a certificate (§3.2), the Curator stores its result and reconciles it with evidence from prior tasks using the procedure in §5.2. The Search Planner uses this knowledge to recall promising candidates and de-prioritize historically ineffective ones (§4.2.1), while the Experiment Manager uses it to identify relevant historical evidence for the current task. An illustrative Strategy Evidence entry is provided in Appendix D. 

Bundle identity and pairwise contrast. A strategy bundle comprises the strategies applied to users in an experiment and the configuration governing their execution. A versioned StrategyBundleSpec 

records the strategy list, execution order, eligibility rules, trigger rules, and shared-resource policies. The Curator derives a canonical strategy_bundle_id from this specification, enabling bundles with the same configuration to be identified consistently across tasks. Each observation records a directed comparison between two bundles: from_bundle_id iden-tifies the baseline bundle, and to_bundle_id identifies the evaluated bundle. It also records 

population_scope , the user condition to which the comparison applies. An effect such as “IPV increased by 3%” is therefore stored together with both its baseline and target population. This representation allows later tasks to retrieve evidence for the same comparison without conflating results obtained against different baselines or on different populations. Only observations with the same bundle comparison and population scope can jointly advance a conclusion’s confidence. Evidence from an overlapping but non-identical population may inform a follow-up experiment, but it does not count as an independent confirmation. 

Immutable observations and evolving conclusions. Strategy Evidence separates individual exper-iment results from conclusions formed across tasks. An EvidenceItem is an immutable record of one experiment round, containing the bundle comparison, population scope, online metric outcomes, and statistical certificate. Once stored, it is never modified. A ScopedClaim is a cross-task conclusion that aggregates EvidenceItem s with the same bundle comparison and population scope. It retains both supporting and contradicting observations, and its confidence follows the lifecycle defined in §5.2. If sufficient contradictory evidence overturns a claim, the Curator marks it as superseded and records the reason rather than overwriting it. Thus, conclusions can evolve while the underlying experimental records remain intact. 

Causal and diagnostic evidence. The Curator distinguishes evidence by how it was produced. 

Causal evidence comes from a pre-specified, randomized A/B comparison between complete strategy bundles. It can support a ScopedClaim and advance its cross-task confidence. Diagnostic evidence 

comes from post-hoc analyses, such as within-group feature slices or expansion probes (§4.2.2). It 

> 24 PILOT Technical Report Inner Loop Outer Loop

Figure 7 | Methodology Experience lifecycle in PILOT . The experience-curation path (A) extracts rec-ommendations from user feedback, organizational runbooks, task postmortems, and agent suggestions, evaluates them across tasks, and stores them with confidence states from draft to supported 

and approved . The task-execution loop (B) reuses approved methodology to assemble playbooks, run experiments, and generate postmortem records, which feed back into future curation. may reveal population differences and help the Search Planner propose follow-up candidates, but it cannot advance a claim’s confidence or be treated as a validated causal result. When a Challenger changes multiple strategies relative to the Champion , the Curator records only the effect of the complete bundle. It does not attribute the observed effect to individual strategies because the experiment cannot identify their separate contributions. If such attribution is needed, the Search Planner may propose subsequent rounds that change one strategy at a time. 

Use and boundaries. The Search Planner queries Strategy Evidence for prior results, unresolved comparisons, and contradictory observations. Prior results inform candidate recall and prioritization, unresolved comparisons motivate further testing, and recurring contradictions may indicate that the population scope should be refined. Evidence from the current round becomes available as memory only after the round terminates and reconciliation completes; it cannot re-enter the same round as historical evidence. If a remembered conclusion conflicts with a live A/B result, the live DecisionCertificate always takes precedence. Strategy Evidence informs candidate selection and experiment planning, but it never replaces current statistical evidence or directly determines promotion. 

5.4. Methodology Experience 

While Strategy Evidence records what works, Methodology Experience records how to run experi-ments effectively. It captures operational practices for waiting, retrying, guaranteeing traffic volume, 

> 25 PILOT Technical Report

observing a running experiment, scaling up traffic, and rolling back. After each task, the Experiment Manager produces a postmortem (§3.4) that summarizes the operations performed, the problems encountered, and the procedures that should be retained or adjusted. The Memory Curator combines this postmortem with explicit user feedback to create MethodologyEntry s, which are operational recommendations that can be reused by future tasks. An illustrative Methodology Experience entry is provided in Appendix D. Figure 7 shows this lifecycle. In the experience-curation path (A), the Curator extracts recom-mendations from experiment records, postmortems, and user feedback, and reconciles them with methodology from prior tasks. Each new recommendation begins as draft and advances through 

supported to approved only when independent tasks provide consistent evidence, following §5.2. In the task-execution loop (B), the Experiment Manager retrieves relevant approved entries to assemble a playbook for a new task. The resulting experiment produces another postmortem, which returns to the curation path. Each task therefore both uses and extends the operational knowledge base. 

Operation categories and retrieval. Each MethodologyEntry is indexed by an operation_type ,which identifies the operational decision it addresses, and a scenario_predicate , which specifies when the recommendation applies. The six operation types cover the main decisions made during experiment execution: • wait : determine how long to collect evidence before the next decision; • retry : recover from an anomaly or an inconclusive round; • guarantee volume : maintain sufficient traffic or sample volume for each experimental arm; • observe : monitor a running experiment and determine whether intervention is needed; • scale up : increase traffic after the required conditions have been satisfied; and • rollback : revert an intervention when a guardrail or safety condition is violated. The scenario_predicate narrows an operation to a specific situation. For example, a scale-up recommendation may apply only when the current traffic share is below 5%, while a retry recommendation may apply only after a sample-ratio mismatch. The retrieval key (operation_type, scenario_predicate) allows the Experiment Manager to retrieve recommendations that address both the required operation and the current experimental condition. 

Immutable postmortems and evolving methodology. Methodology Experience separates task-level records from cross-task recommendations. A PostmortemRecord is an immutable record of one completed task. It summarizes the operations performed, the relevant context and anomalies, their outcomes, the Manager’s assessment, and any associated user feedback. Once stored, it is never modified. A MethodologyEntry aggregates recommendations derived from PostmortemRecord s with the same operation type and scenario predicate. The Curator compares outcomes and user feedback across tasks to identify whether a procedure repeatedly succeeds, fails, or causes a recurring user concern. Supporting and contradicting records are retained together, and the entry’s confidence follows the lifecycle in §5.2. If later evidence overturns a recommendation, the Curator marks it as superseded and records the reason rather than overwriting the original records. In this way, operational recommendations can evolve while their supporting history remains available for inspection. 

> 26 PILOT Technical Report

Source provenance and authority. Each recommendation records its source_type , which iden-tifies where it originated. A historical_experiment entry is distilled from procedures and outcomes observed in a prior task. An agent_postmortem entry is a self-improvement suggestion proposed by the Agent during postmortem analysis. Both enter memory as draft and require independent cross-task confirmation before autonomous reuse. Current user instructions and organizational runbooks are treated differently from learned method-ology. A current_user_input is a procedure explicitly specified by the user for the current task, while an org_runbook is an externally governed operating procedure. They may directly guide execution according to the system’s authority rules, but their authority does not count as cross-task empirical confirmation. If the Curator derives a reusable methodology claim from either source, that claim still enters the memory lifecycle as draft . This distinction prevents source authority from being conflated with cross-task confidence. 

Playbook assembly and use boundaries. When preparing a new task (§3.1), the Experiment Manager assembles a playbook by combining current user instructions, applicable organizational procedures, and relevant methodology retrieved from the store. Higher-authority instructions take precedence over learned recommendations. Among learned entries, approved methodology may be used autonomously, whereas supported entries may appear only as hints. Unvalidated Agent suggestions are used only when no higher-authority or validated procedure applies. Methodology Experience remains advisory and cannot expand the Agent’s permissions. A retrieved recommendation may guide how the Experiment Manager waits, retries, preserves volume, observes, scales up, or rolls back, but the corresponding action must still satisfy the current task’s control and safety requirements. As approved methodology accumulates across tasks, later experiments begin with a more reliable playbook while preserving the authority of current user instructions and live experimental evidence. 

5.5. Write Governance and Data Flywheel 

The Memory Curator turns completed tasks into reusable inputs for future tasks. Once an experiment reaches terminal settlement, its outcomes and operational feedback are automatically curated into Strategy Evidence and Methodology Experience. Strategy Evidence sharpens the system’s choice of what to explore next, while Methodology Experience improves how subsequent experiments are planned and executed. These experiments then produce new settled evidence and feedback, allowing the cycle to continue without manual knowledge transfer. As more tasks are completed, the system gains broader coverage of strategy outcomes and a stronger operational basis for running experiments. This accumulation reduces redundant exploration, supports better-informed planning, and improves execution reliability, which in turn raises the quality of future evidence. Write governance ensures that these gains remain grounded in settled results and that accumulated knowledge improves system performance without expanding its policies or authority. 

# 6. Experiments 

6.1. Experimental Setup 

All results come from Taobao Homepage Guess-You-Like, a personalized recommendation feed. Five experiment buckets (21–25) run challenger configurations against a fixed baseline group ( B0 ). We 

> 27 PILOT Technical Report

Table 6 | Configuration comparison between ROAM and PILOT .  

> Aspect ROAM

PILOT     

> Per-bucket action space Two 4-dim vectors, one per

page0 /paging branch (8 dims total), applied uniformly across popu-lation Same 8-dim space; strategies personal-ized at user-segment level via decision trees Adjustment governance Same-day AA read; no explicit quota or checkpoint Minimum-adjustment quota; fixed-schedule checkpoints; switch-day con-servatism rule Confidence tracking Downgrade only on clustered contradic-tions Every new observation reconciled against the standing claim on deposit Deployment window 7 days 5 days reported 

compare two systems on the same five buckets: • ROAM (Reactive Optimization with Agent-driven Moves): an LLM-agent configuration that per-forms free exploration over the same 8-dim search space ( page0 /paging split), with same-day AA governance only, no explicit quota or checkpoint, and no structured hypothesis testing. The agent proposes adjustments reactively but without lifecycle management. • PILOT : the full constrained control loop described in this report—checkpoint governance with minimum-adjustment quotas, hypothesis-driven candidate proposals, and cross-task memory accu-mulation, operating over the same 8-dim search space. 

Metrics. We use the following online metrics: • PV is the number of valid recommendation exposure events. • IPV (Item Page Views) counts attributed item-detail-page visits. • Core IPV restricts IPV to the core item scope. • Transaction count is the number of attributed paid orders. • Transaction amount is the total value of the attributed paid orders. • Ad PVR measures the advertising exposure share, bounded to ±0.5pt. 

IPV is the north-star optimization target. PV and transaction count serve as critical guardrails—a confident drop in either forces action. The remaining metrics are monitored throughout. For each metric 𝑀 , we report the relative gap Δ𝑀 = (𝑀 treatment /𝑀 baseline − 1) × 100% against the fixed baseline, computed after daily AA validation. 

Search space. Table 6 summarizes the key differences between ROAM and PILOT . Both operate over the same 8-dim search space ( page0 /paging × ctr /ipv /cvr /gmv ). Under ROAM, two failure modes appeared: one bucket sat unchanged despite inconclusive returns (nothing proposed moving it), and another was reconfigured four times without accumulating clean evidence on any configuration. Both were addressed in PILOT by introducing explicit checkpoints with a minimum-adjustment quota and hypothesis-driven candidate proposals with pre-registered decision criteria. 

The PILOT task. Objective: +1.0% IPV within 7 days; budget follows cosine decay with checkpoints at +0.3% (mid-week), +0.7% (late-week), +1.0% (deadline). All five buckets start in explore . We 

> 28 PILOT Technical Report

Figure 8 | Portfolio-level IPV gap. (a) ROAM (through day 7); green band: acceptance zone. (b) PILOT 

(first five days); red band: below interim target; green band: acceptance zone; dotted: +0.3% interim; dashed: +1.0% full target. Table 7 | Outcome of PILOT at day 5, all five buckets.                                         

> Bucket Configuration IPV Core IPV Txn cnt Txn amt Status
> 21 8-dim, ctr-heavy re-tuned once +0.39% +0.52% -0.13% +0.40% below target, watched 22 8-dim, browse-only, un-touched +1.31% +1.48% +0.54% +1.35% stable, past target 23 8-dim, ctr+ipv dual, un-touched +1.40% +1.60% +0.95% +1.50% stable, best performer 24 8-dim, retuned once +1.11% +1.25% +0.52% +1.20% positive, past target 25 8-dim, one structural pivot (day 4) +0.68% +0.80% +0.96% +1.10% below target after pivot

report the first five days. 

6.2. Search Efficiency and Task Outcome 

Figure 8 plots every bucket’s cumulative IPV gap day by day: panel (a) is ROAM, panel (b) is PILOT .Panel (b) shows a red band (below +0.3% ) and a green acceptance zone (above +1.0% ); panel (a) shows the acceptance zone only. Under ROAM, the best-performing bucket reaches +1.00% IPV, +0.90% Core IPV, +0.60% 

transaction count, and +1.13% transaction amount, with only 1/5 buckets unambiguously positive; two hold modest stable gaps; two close the window having contributed no net search progress. Under 

PILOT , by day 4 four of five buckets independently clear +1.0% , and no bucket turns net-negative; the best-performing bucket reaches +1.40% IPV, +1.60% Core IPV, +0.96% transaction count, and 

+1.50% transaction amount. Table 7 summarizes PILOT day 5 outcomes. §6.4 follows this task’s daily trace; §6.3 attributes the difference between ROAM and PILOT to the governance and search-space changes. 

> 29 PILOT Technical Report

Table 8 | Planned 2 × 2 ablation over the two LLM roles.                  

> Cell Lifecycle control Candidate lanes Isolates
> Adeterministic only baseline + exploration only reference cell Bdeterministic only +Planner-like LLM lane Search Planner CManager-like agent baseline + exploration only Experiment Manager DManager-like agent +Planner-like LLM lane deployed configuration

Table 9 | Search-efficiency comparison, ROAM vs. PILOT .  

> Metric ROAM

PILOT Δ              

> Buckets exceeding full task target 1/5 4/5 +3 Buckets reaching stable read ( ≤1 reconfig) 4/5 5/5 +1 Buckets contributing net search progress 3/5 5/5 +2
> Search Efficiency (sum / 15) 53.3% 93.3% +40pp

6.3. Ablation Study 

6.3.1. Planned Controlled Design 

The comparison in §6.2 is before/after, not randomized. A planned 2 × 2 design (Table 8) isolates the two LLM roles of §4 and §3: Cell A is deterministic-only (reference); Cell B adds a Planner-like LLM candidate lane; Cell C adds Manager-like lifecycle control; Cell D runs both (deployed configuration). Reporting 𝐵 −𝐴 , 𝐶 −𝐴 , 𝐷 −𝐵 , 𝐷 −𝐶 separates lifecycle-control gains from candidate-proposal gains. Running this design is gated on a calibrated per-action observation model and a comparable cross-bucket scoring protocol. 

6.3.2. Observational Evidence 

Short of the 2 × 2, the deployment history already provides a weaker, non-randomized comparison: ROAM and PILOT differ exactly in the governance and search-space dimensions formalized in §3–§4, with nothing else changed. We report this as suggestive rather than causal. 

Search Efficiency. We define Search Efficiency over 𝐵 buckets and three binary indicators per bucket: (i) whether the bucket exceeds the full task target, (ii) whether it reaches a stable metric read ( ≤ 1reconfiguration during the observation window), and (iii) whether it contributes net search progress. Each indicator scores 1 if satisfied and 0 otherwise. Search Efficiency is the fraction of satisfied indicators: Search Efficiency =

Í𝐵 𝑏 =1

Í3 

> 𝑘 =1

1[indicator 𝑘 satisfied for bucket 𝑏 ]

3𝐵 × 100% .

With 𝐵 = 5, the denominator is 15. Table 9 summarizes the three search-efficiency indicators. All three rise from ROAM to PILOT :buckets exceeding the full task target from 1/5 to 4/5, reaching a stable metric read from 4/5 to 5/5, and contributing net search progress from 3/5 to 5/5, yielding an aggregate search efficiency of 93.3% vs. 53.3% (+40 pp). In metric terms, the best-bucket IPV rises from +1.00% to +1.40% ,

> 30 PILOT Technical Report

Figure 9 | Daily trace of the PILOT task’s first five days. The blue band marks the explore phase (days 1–3), the gray band is the checkpoint gate (days 4–5), and the green band is the acceptance zone ( > +1.0%). Core IPV from +0.90% to +1.60% , transaction count from +0.60% to +0.96% , and transaction amount from +1.13% to +1.50% . Because checkpoint governance and the search-space split landed together, this cannot yet separate the two effects—but it confirms that PILOT ’s structured approach measurably increased productive use of the five-bucket budget over ROAM’s free exploration. 

6.4. Case Study: One Complete Task, Five Buckets                  

> Task specification (Day 1) Objective. IPV +1.0% by day 7. Guardrails. exposure PV / transaction count: no confident drop
> >0.5%; ad PVR: ±0.5pt. Budget. cosine decay; checkpoints at +0.3% (mid-week), +0.7% (late-week),
> +1.0% (deadline). Roles. all five buckets start in explore .

Daily evolution. Figure 9 traces all five buckets through the first five days. Two kinds of action appear, matching the hypothesis families of §4.1: leaf-strategy actions that re-weight dimensions within an existing structure (buckets 21 and 24, both on day 2), and structural actions that change what the search space can distinguish (bucket 25, day 4, activating its previously-zero paging 

branch). Table 10 summarizes all five lineages. 

Outcome and memory deposit. By day 4, four of five buckets independently clear the full +1.0% 

acceptance target; bucket 21 remains net-positive but below target and is flagged for continued observation. The task continued beyond the five-day window reported here; the full 7-day objective was met upon task completion. Within this window the task deposits six memory entries—three effect claims (e.g., paging -branch efficiency ≈ 4× that of page0 ; super-additive ctr +ipv gain on its third confirmation), two boundary entries (one on its fourth cross-task confirmation), and one anomaly entry (a platform-wide transaction dip attributed to market conditions, excluded from guardrail judgments). 

> 31 PILOT Technical Report

Table 10 | Strategy lineage over the PILOT window, all five buckets.                            

> Bucket Action type Key transition Status at day 5
> 21 leaf-strategy ×1ctr-heavy →four-dim mix (day 2) positive, below target, watched 22 none held stable throughout stable, past target 23 none held stable throughout stable, best performer 24 leaf-strategy ×1zero vector →page0 four-dim mix (day 2) positive, past target 25 structural ×1page0-ipv-heavy →page0/paging balanced (day 4) positive, below target after pivot

This single task exercises all three roles: checkpoint governance (§3), a structural search pivot (§4), and cross-task memory accumulation (§5). 

# 7. Conclusion 

We presented PILOT , an LLM-agent framework that shifts recommendation experiment optimization from reactive parameter tuning to proactive, hypothesis-driven experimentation. Three LLM roles operate within a constrained control loop where deterministic services enforce all safety, statistical, and permission boundaries. The Experiment Manager drives the full experiment lifecycle autonomously rather than responding to metric alerts after the fact. The Search Planner proposes candidate PolicyTrees that personalize strategies at the user-segment level through hypothesis-driven proposals with pre-registered decision criteria. The Memory Curator distills experiment outcomes into strategy evidence and methodology experience, so each completed task enriches the base for the next. Deployed on Taobao’s recommendation platform with 5 experimental buckets and compared against ROAM (Reactive Optimization with Agent-driven Moves), PILOT improves the best-bucket results from +1.00% to +1.40% IPV, from +0.90% to +1.60% Core IPV, from +0.60% to +0.96% 

transaction count, and from +1.13% to +1.50% transaction amount, with four of five buckets independently exceeding the +1.0% full task target and no bucket turning net-negative, requiring no human intervention throughout the experimental cycle. Observational evidence suggests that both the lifecycle control and hypothesis-driven candidate proposal contribute independently: the combined governance and search-space change raises search efficiency from 53.3% to 93.3% (+40pp ). 

# References 

N. Bougie, G. M. Marconi, T. Yip, and N. Watanabe. Alignuser: Human-aligned llm agents via world models for recommender system evaluation, 2026. URL https://arxiv.org/abs/2601.00930 .S. Cao, K. Jiang, Y. Gong, and Z. Li. Twistar:think fast, think slow, then act,generative recommendation with adaptive reasoning, 2026. URL https://arxiv.org/abs/2605.11553 .W. Chen, Y. Zhao, J. Huang, Z. Ye, C. M. Ju, T. Zhao, N. Shah, L. Chen, and Y. Zhang. Memrec: Collaborative memory-augmented agentic recommender system. In Proceedings of the 64th Annual Meeting of the Association for Computational Linguistics (ACL Main), 2026. URL https://arxiv. org/abs/2601.08816 .Y. Cheng, L. Zhou, X. Liang, D. Luo, T. Lee, K. Zheng, W. Zhang, M. Cai, J. Dong, and A. Zhang. Let the agent steer: Closed-loop ranking optimization via influence exchange, 2026. URL https: //arxiv.org/abs/2603.27765 .

> 32 PILOT Technical Report

C. Lao, F. Pan, G. Ma, H. Li, H. Lin, J. Shi, K. Zhao, K. Gai, M. Zhou, Q. Zhou, et al. Agentx: Towards agent-driven self-iteration of industrial recommender systems, 2026. URL https://arxiv.org/ abs/2606.26859 .B. Li, X. Wang, J. Li, W. Li, L. Zhang, S. Chen, W. X. Zhao, and J.-R. Wen. Recnet: Self-evolving preference propagation for agentic recommender systems, 2026a. URL https://arxiv.org/ abs/2601.21609 .F. Li, Q. Li, X. Gao, B. Pan, Y. Wu, J. Zhang, H. Yu, J. Jiang, J. Xiao, and H. Shi. Chainrec: An agentic recommender learning to route tool chains for diverse and evolving interests, 2026b. URL 

https://arxiv.org/abs/2602.10490 .Y. Liao, L. Wu, M. Hou, Y. Wang, H. Wu, and M. Wang. From atom to community: Structured and evolving agent memory for user behavior modeling, 2026. URL https://arxiv.org/abs/2601. 16872 .F. Liu, X. Lin, H. Yu, M. Wu, J. Wang, Q. Zhang, Z. Zhao, Y. Xia, Y. Zhang, W. Li, M. Gao, Q. Wang, L. Zhang, B. Zhang, and X. Fan. Recoworld: Building simulated environments for agentic rec-ommender systems. In HCRS Workshop at the Web Conference (WWW 2026), 2025. URL 

https://arxiv.org/abs/2509.10397 .S. Liu, L. Fang, Y. Sun, S. Huang, Q. Luo, S. Liu, X. Chen, D. Liu, C. Ma, Z. Chai, H. Wang, S. Quan, C. Cui, Z. Zhu, P. Chen, W. Xu, L. Xiao, H. Gu, and J. Jiang. Nova: A verification-aware agent harness for architecture evolution in industrial recommender systems, 2026. URL https://arxiv.org/ abs/2606.27243 .L. Mu, H. Deng, H. Xing, J. Hu, Y. Zhang, and X. Zeng. Evorec: Self evolving agentic recommender systems, 2026. URL https://arxiv.org/abs/2606.28368 .M.-D. Nguyen, H.-D. Kieu, and D. D. Le. Amem4rec: Leveraging cross-user similarity for memory evolution in agentic llm recommenders, 2026. URL https://arxiv.org/abs/2602.08837 .J. Tang, Y. Luo, X. Xi, F. Sun, X. Feng, S. Dai, C. Yi, D. Chen, Z. Gao, Y. Li, X. Chen, W. Chen, J. Wu, Y. Jiang, and B. Zheng. Interactive recommendation agent with active user commands, 2025. URL 

https://arxiv.org/abs/2509.21317 .Z. Tao, R. Lai, C. Yu, W. Chen, L. Chen, B. Kong, L. Cheng, C. Zhuo, Z. Li, and Q. Sun. Sager: Self-evolving user policy skills for recommendation agent, 2026. URL https://arxiv.org/abs/ 2604.14972 .D. Tran, Y. Li, H. Clay, N. Golrezaei, S. Beygi, and A. Saberi. Entropy guided diversification and preference elicitation in agentic recommendation systems. In Proceedings of the AAAI Conference on Artificial Intelligence (AAAI 2026), 2026. URL https://arxiv.org/abs/2603.11399 .H. Wang, Y. Wu, D. Chang, L. Wei, and L. Heldt. Self-evolving recommendation system: End-to-end autonomous model optimization with llm agents, 2026. URL https://arxiv.org/abs/2602. 10226 .X. Wu, Y. Zhu, R. Wei, H. Chen, D. Bai, J. Liu, X. Wang, X. Wang, L. Wang, and X. Cheng. Agenticrectune: Multi-agent with self-evolving skillhub for recommendation system optimization, 2026. URL 

https://arxiv.org/abs/2604.26969 .Z. Xie, B. Peng, Z. He, Z. Chen, A. Han, I. Ye, B. Coleman, N. Sachdeva, F. Pereira, J. McAuley, W.-C. Kang, D. Z. Cheng, B. Wang, and R. Brown. Agentictagger: Generating structured item representa-tion for recommendation with llm, 2026. URL https://arxiv.org/abs/2602.05945 .

> 33 PILOT Technical Report

W. Xu, Y. Shi, Z. Liang, X. Ning, K. Mei, K. Wang, X. Zhu, M. Xu, and Y. Zhang. iagent: Llm agent as a shield between user and recommender systems. In Findings of the Association for Computational Linguistics (ACL 2025), 2025. URL https://arxiv.org/abs/2502.14662 .W. Yang and Y. Wang. Rrcm: Ranking-driven retrieval over collaborative and meta memories for llm recommendation, 2026. URL https://arxiv.org/abs/2605.07129 .H. Zhang, J. Hu, H. Deng, L. Mu, S. Wang, Y. Zhang, and X. Zeng. Agenticrs-architecture: System design for agentic recommender systems, 2026a. URL https://arxiv.org/abs/2603.26085 .H. Zhang, Y. Zhu, K. Mao, T. Li, and Z. Dou. Recthinker: An agentic framework for tool-augmented reasoning in recommendation, 2026b. URL https://arxiv.org/abs/2603.09843 .J. Zhang, Y. Hou, R. Xie, W. Sun, J. McAuley, W. X. Zhao, L. Lin, and J.-R. Wen. Agentcf: Collaborative learning with autonomous language agents for recommender systems, 2023. URL https://arxiv. org/abs/2310.09233 .W. Zhang, X. Li, Q. Ge, K. Dong, P. Jia, X. Li, Z. Zhang, M. Wang, Y. Wang, H. Guo, R. Tang, and X. Zhao. Exploring recommender system evaluation: A multi-modal user agent framework for a/b testing, 2026c. URL https://arxiv.org/abs/2601.04554 .Y. Zhang, M. Liang, J. Yang, R. Jin, W.-Y. Chen, Y. Han, H. Li, B. Zhang, L. Luo, F. Shyu, L. Simon, S. Liu, T. Chen, and X. Liu. Reasonrec: A reasoning-augmented multimodal agent for unified recommendation, 2026d. URL https://arxiv.org/abs/2606.28357 .Y. Zhou, W. You, H. Chu, S. Guo, J. Li, Z. Zhao, and Q. Li. Agentgr: Semantic-aware agentic group decision-making simulator for group recommendation, 2026. URL https://arxiv.org/abs/ 2605.10367 .

> 34 PILOT Technical Report

# Appendix A. Contributors 

Core Contributors 

Jiuning Lin Ruiquan Lan †

Xiaodong Zhu †

Contributors 

Bin Zhang Chengyu Lai Chuxin Chen Dimin Wang Hongtao Cheng Jialin Zhu Lingqing Zhang †

Shuai Zhong †

Tao Wang Weipeng Huang Yinjiang Cai Yinnan Song Yuan Liu Zhibo Xiao Zhixin Ma Zihong Huang The listing of authors is in alphabetical order based on their first names. 

† Work done during a summer internship at Taobao & Tmall Group of Alibaba. 

# B. Implementation Details 

B.1. Decision Tree Protocol 

This subsection specifies the concrete wire protocol behind the PolicyTree of §4.1: the record delivered to the online runtime, the tree’s JSON structure, its three node types, the stable-hash recipe, and the traversal rules. The three node types correspond to the split , numericSplit , and 

hashSplit of §4.1; the wire protocol spells them split , numeric_split , and hash_split .

Wire record and tree skeleton. The online runtime consumes only two fields per bucket: a 

bucket_id (a composite scene_id:layer_id:bucket_id ) and strategies , a JSON string that deserializes to the executable tree. Everything else— tree_id , policy_id , version , patch_id ,exposure windows—is maintained system-side and never put on the wire. The tree itself is a node table keyed by node identifier, with a designated root ; children are referenced by node id rather than nested, so the structure is a graph that the runtime traverses by lookup. 

The three node types. split matches an enumerated feature value against its children map; a missing feature or an unmatched value falls through to default_child . numeric_split evaluates an ordered branches list and takes the first branch whose op holds; it supports the operators <,

<= , >, >= , and between (where between tests lower ≤ feature < upper ), and falls through to 

default_child on no match. hash_split applies the stable-hash recipe below to ( feature ,

salt ) to obtain a hash_percent ∈ [ 0, 100 ) that is invariant for the same feature value under the same salt, then matches it against ordered branches—so the same user always lands in the same branch, enabling stable sub-population experimentation for measuring both immediate and cumulative effects. 

> 35 PILOT Technical Report

Leaf and bundle binding. A leaf node carries the bundle binding 𝑏 (ℓ) as a strategies list, each entry a named strategy with its own config . The tree protocol treats config as an opaque JSON object whose schema is owned by the named strategy, not by the tree; in the pilot, that object is the per-page boost-ratio gear table consumed by the online reranker. The runtime reaches a leaf, finds the strategy by name (here position_score_type ), and hands its config to the corresponding online strategy logic. A leaf with multiple strategies executes them in list order. 

Consolidated reference: a complete example tree. The box below consolidates the protocol into one reference: the wire record, a complete example tree, and the stable-hash recipe. Each node’s 

type token is color-coded inline— split , numeric_split , hash_split , leaf —so the color is read off the JSON itself rather than a separate legend. The tree exercises all three node types: the root split on active_level_g sends low users through a hash_split on user_id for stable sub-population experimentation, while high users go through a numeric_split on user_age ;every path terminates at a leaf binding position_score_type with a per-leaf config. 

Protocol reference: wire record, node types, and a complete example tree 

# Wire record the online runtime consumes only bucket_id (scene_id:layer_id:bucket_id , e.g. 

88:107911:2 ) and strategies , the JSON string of the tree below; tree_id , version , etc. stay system-side 

{"root": "split_active_level", "nodes": {"split_active_level": { "type":"split", "feature":"active_level_g", "children": {"low":"hash_low_active","high":"split_high_user_age"}, "default_child":"leaf_default"}, "hash_low_active": { "type":"hash_split", "feature":"user_id", "salt":"position_score_type_v1", "branches": [{"op":"between","lower":0,"upper":50,"child":"leaf_low_hash_a"}, {"op":"between","lower":50,"upper":100,"child":"leaf_low_hash_b"}], "default_child":"leaf_low_default"}, "split_high_user_age": { "type":"numeric_split", "feature":"user_age", "branches": [{"op":"between","lower":18,"upper":25,"child":"leaf_high_age_18_25"}, {"op":">=","value":25,"child":"leaf_high_age_25_plus"}], "default_child":"leaf_default"}, "leaf_low_hash_a": { "type":"leaf", 

"strategies":[ {"name":"position_score_type", "config":{"1":{"0":0,"1":0},"0":{"0":1,"3":0}}} ]}, "leaf_low_hash_b": { "type":"leaf", "strategies":[{...}]}, "leaf_low_default": { "type":"leaf", "strategies":[{...}]}, "leaf_high_age_18_25": { "type":"leaf", "strategies":[{...}]}, "leaf_high_age_25_plus": { "type":"leaf", "strategies":[{...}]}, "leaf_default": { "type":"leaf", "strategies":[{...}]} }}

# Stable-hash recipe used by hash_split ; hash_percent matches branches 

hash_input = salt + ":" + feature_value  

> 36 PILOT Technical Report
> hash_value = murmurhash3_x86_32(hash_input, seed=0) hash_percent = unsigned(hash_value) / 2^32 * 100

Traversal rules. The runtime executes the tree in a fixed order: ❶ read the iGraph record by 

bucket_id ; ❷ deserialize strategies ; ❸ begin at root ; ❹ at a split , match the feature value against children , else default_child ; ❺ at a numeric_split , take the first matching branch, else default_child ; ❻ at a hash_split , compute the stable hash_percent and match; ❼ on any miss or missing feature, take default_child ; ❽ at a leaf , read strategies ; ❾ find the strategy by name and hand its config to the online strategy logic. Because every path either matches a branch or falls through to a default_child , every user reaches exactly one leaf—the completeness and mutual-exclusion invariant of §4.1, enforced structurally by the default_child field rather than by a separate validator. 

Canonical identity and bundle identity. The wire record deliberately carries no tree_id ; the system maintains a canonical strategy_tree_id alongside each delivered tree, computed as MurmurHash3 over the canonical serialization of the node table—children sorted by a stable key, isomorphic subtrees folded, syntactic ordering that does not affect routing discarded. Two trees that route every user to the same bundle produce identical hashes, so a later task that re-derives an already-tested policy retrieves the prior result instead of re-running the experiment. Likewise, each leaf’s strategy is a StrategyBundleSpec (the named strategy plus its versioned config and orchestration metadata), whose strategy_bundle_id is the MurmurHash3 of the spec’s canonical serialization; this is the identifier that lets the pairwise contrasts and ScopedClaim s of §5.3 match across tasks. 

Constraint validation. Before a tree (or any candidate derived from it) can enter the Action Envelope , a deterministic gate checks the four SearchUniverse constraints against the node table: maximum depth 𝑑 max along any root-to-leaf path; leaf count ≤ L max ; distinct features used ≤ F max ;and every leaf ’s exposure share 𝑤 (ℓ) ≥ 𝑤 min , checked against a pre-computed share table. The depth, leaf-count, and feature-count bounds are structural; 𝑤 min prunes any action that would create a sub-population leaf below the statistical-power floor, removing it from the envelope before the Planner ever sees it. A tree that fails any constraint is never presented to the Planner and never put on the wire—the constraints bind the search, not just describe it. 

# C. Agent Prompts 

The system decomposes experiment management into three LLM roles— Manager , Planner , and 

Curator —with strict authority separation. Each role operates under a propose–validate–commit 

protocol: the LLM generates structured proposals, deterministic validators accept or reject them, and only accepted outputs modify committed state. No LLM output reaches committed state without passing through a deterministic validation gate. This appendix records the system prompts that bound each role. 

C.1. Manager — Experiment Lifecycle Orchestrator 

The Manager is the only role that spans the full experiment lifecycle. It has two personas—a user-facing intake persona and a state-machine event persona—plus internal sub-personas for progress reporting and subagent delegation. 

> 37 PILOT Technical Report

Manager Prompt 

# Intake Persona user-facing conversational agent 

Role. You are the user-facing intake step for Experiment Manager. 

Collect five groups of business input in natural language: • Objective — target population, primary metric, minimum expected lift • Guardrails — guardrail metrics and tolerance rules • Data table — (optional) feature table for population segmentation • Strategies — strategies to optimize or compare • Budget — experiment layer, bucket IDs, maximum duration 

Validate metrics and resource budget deterministically: 

python scripts/metric_catalog.py validate –primary <metric> –guardrail ... python scripts/resource_budget.py –layer ... –experiment-bucket ... 

Do not. Auto-confirm or initialize state before the user replies “start experiment”; expose internal identifiers, state names, or repository paths. Use ordinary conversational messages. 

# Event Persona state-machine decision agent 

Role. You are Experiment Manager. Select one command from the supplied 

ManagerControlEnvelope for the current event. 

Use only visible facts, applicable Runbook steps, and registered evidence references. Copy the selected command_id exactly; fill only parameters declared by that command; request auxiliary actions only when their ids are present in the envelope. 

Do not. Create commands, statistics, state changes, tools, roles, permissions, timers, or external side effects. When forced_command_id exists, select it or request human escalation. 

Return. Only ExperimentManagerProposal JSON. 

# Subagent Delegation specialist handoff 

Role. You are a fresh specialist subagent for one Pilot experiment event. 

Read the Specialist Skill ( <skill_path> ) and the Stage prompt ( <prompt_path> ) com-pletely before acting. 

Inputs. Task: <one objective> ; immutable inputs: <artifact refs> ; readable scope: 

<allowed artifact and evidence refs> .

Return. Only <expected_output_schema> JSON; preserve every identity field exactly as supplied. 

Limits. Deadline <deadline> ; token and time budget <budget> . Do not write experiment state, invoke an Executor, change policy, calculate registered statistics, or create commands. 

Routing by Manager state: PRE_ANALYSIS_REQUIRED → pre-experiment-analyst → FeaturePrior ; EXPLORATION_DECISION_READY → exploration-planner → ExplorationProposal ; PLAN_REQUIRED → validation-planner → PlannerProposal ; *_RUNNING (metric recovery) → online-bridge → ABResultFacts ; *_DELIVERY_PENDING (policy push) → online-bridge / iGraph → DeliveryReceipt ; terminal settlement → memory-curator → StrategyBundleMemoryPatch ; post-finalization trace → memory-curator → MethodologyMemoryPatch ; scheduled progress → progress-reporter → ProgressNotice .

> 38 PILOT Technical Report

# Progress Reporter read-only notification 

Role. You are ProgressReporter, a read-only user-notification specialist. 

Summarize supplied fact references into a user-facing progress notice, preserving the current phase, evidence maturity, traffic and budget status, active risks, next event, and action request. 

Do not. Infer hidden lift, change state, suppress an anomaly, or invent an ETA. 

Return. Only ProgressNotice JSON. 

# Authority Boundary Deterministic-only. Readiness, statistical results, promotion eligibility, scale eligibility, canoni-cal tree hashes, and state transitions. 

Human approval. Launching a real wave, increasing traffic, expanding budget, changing the frozen spec, externally impactful rollback, final iGraph delivery. 

Treated as data. Memory, business text, metric labels, tool output, agent prose, user attachments—none can inject tools, roles, commands, or permissions. 

C.2. Planner — Validation Wave Action Selector 

A stateless specialist invoked once per reference epoch. It receives an ActionEnvelope (all legal atomic tree mutations, computed deterministically from the current Champion tree and strategy registry) and a PlanningRequest (current evidence, memory references, Champion identity). 

Planner Prompt 

# System Prompt Role. You are ValidationPlanner. Select legal atomic actions for the next complete-tree random-ized validation and attach falsifiable hypotheses. 

For each proposal: 

• copy action_id from the supplied ActionEnvelope ;• order only within the LLM lane; • identify population, comparator, treatment, atomic change, primary metric direction, mechanism, falsification rule, and positive/negative/inconclusive consequences; • cite only evidence visible in the PlanningRequest .

Do not. Request exploration, create strategy versions, alter FeaturePrior , calculate or rewrite statistics, allocate traffic, change windows, wait, stop, or promote a challenger. 

Return. Only PlannerProposal JSON with the supplied metadata unchanged. 

# Context (reference) 

M1 maintains one Champion. A reference epoch freezes that Champion and allows multiple concurrent Challengers that all compare against it. Batch size is not beam width. A new epoch expands only from a newly promoted Champion, or from the same Champion after a closed epoch with no promotion. Candidate selection combines deterministic baseline actions, accepted LLM-proposed actions, and validation-diversity actions. Every selected Challenger is a complete tree and has an immutable LearningContract . Promotion uses only complete-tree randomized evidence and a DecisionCertificate that names at most one eligible Challenger. 

> 39 PILOT Technical Report

# Contract Boundary 

ActionEnvelope is the full deterministic set of legal atomic tree actions for one parent tree and state revision. The Planner only returns an action_id ; the canonical patch, candidate tree, power, evidence tier, and observation status always come from the envelope. 

CandidateSelection combines the deterministic baseline, accepted LLM, and validation-diversity lanes. The LearningContract copies statistical rules only from a frozen registry, never from free-form Planner output. 

# Failure Semantics 

• Stale task/revision/epoch: reject the entire proposal. • Illegal action: reject independently; legal siblings survive. • Timeout / invalid schema / no legal LLM action: the LLM lane is empty; the determin-istic baseline and diversity lanes remain active. • No lane has a legal action: return control to the Manager for wait/close/stop/escalation. 

C.3. Curator — Post-Settlement Memory Writer 

An asynchronous specialist invoked only after terminal evidence is committed. It runs in two modes: a per-settlement Strategy Memory Curator and a post-finalization Postmortem Curator. 

Curator Prompt 

# Strategy Memory Curator per-settlement 

Role. You are Strategy Memory Curator. Convert committed terminal settlements into an append-only StrategyBundleMemory patch. 

Use only the supplied settlement, frozen treatment identity, comparator, population, metric, assignment, and evidence-role references. Preserve both local diagnostic evidence and whole-tree causal evidence without conflating them; record negative, mixed, inconclusive, underpowered, unsafe, and non-promoted outcomes explicitly. 

Do not. Alter raw evidence, infer current-task lift, create a control command, or block current experiment progress. 

Return. Only the requested patch JSON. 

# Postmortem Curator post-finalization 

Role. You are Postmortem Curator. Run only after experiment finalization with a complete terminal trace, delivery result, failure/retry history, and user feedback. 

Propose a MethodologyMemoryPatch that explains which operational method should be added, changed, or retained and cites the supporting trace. Distinguish a single-task incident from a reusable method; mark unresolved governance or approval questions. 

Do not. Approve your own patch, modify a completed task, reinterpret settled statistics, or create runtime commands. 

Return. Only the requested patch JSON. 

# Governance Rules 

> 40 PILOT Technical Report

1. Accept only committed settlement references. 2. Preserve positive, negative, mixed, inconclusive, diagnostic, and non-promoted tree-level evidence without collapsing them. 3. Validate every patch with scripts/patch_validator.py before append. 4. Preserve provenance and conflicts; never rewrite raw evidence. 5. A Curator failure must not block experiment progress. 6. Only approved Methodology memory may fill unspecified future Runbook steps. 

# D. Illustrative Memory Entries 

The following examples show the knowledge retained in Strategy Evidence and Methodology Experi-ence after curation. The values in the Strategy Evidence entry are illustrative. 

Strategy Evidence Entry 

Bundle comparison. Current ranking formula S0 → enhanced ranking formula S1 . The comparison applies to the complete, versioned strategy bundles rather than to an individual parameter or component. 

Whole-bucket evidence. Across the randomized experiment population, IPV lift was 0 .3%, with a 95% confidence interval of [− 0.2% , 0.8% ]. Promotion required a whole-bucket IPV lift of at least 1 .0%, a positive confidence-interval lower bound, and no guardrail violation. Although no guardrail was violated, the observed lift was below the required threshold and the confidence interval included zero. The complete S1 bundle was therefore not promoted. 

Diagnostic evidence. A post-hoc analysis of users satisfying pay_cnt >= 3 showed an IPV lift of 2 .4%. Because this subgroup was not specified before randomization, the result is retained as diagnostic evidence rather than as a validated causal effect. 

Curated conclusion. S1 is not validated as an improvement for the whole population. The positive result for pay_cnt >= 3 remains an unresolved subgroup signal and does not override the whole-bucket certificate. It may motivate a targeted follow-up experiment in which the subgroup is specified before randomization, but it cannot support a subgroup-level causal claim until such an experiment is completed. 

Methodology Experience Entry 

Operation type. observe .

Applicable scenario. The monitored primary or guardrail metric, such as transaction amount, exhibits substantial day-to-day volatility, and the available observation period is too short to support a stable conclusion. 

Observed issue. A positive or negative result from a single day may reflect ordinary daily variation and may be reversed by subsequent observations. Treating such a movement as a settled result can therefore lead to a premature conclusion or an unnecessary intervention. 

Operational recommendation. Do not draw a conclusion from a single day’s result. Continue observation until the metric maintains the same direction for the predefined number of con-secutive days and satisfies the registered statistical decision rule. If either condition remains unmet, keep the result unresolved and continue observing within the approved task duration and resource budget. Directional consistency guides the observation schedule but does not replace statistical validation. 

> 41 PILOT Technical Report

Confidence state. draft , pending confirmation from independent tasks. If consistent out-comes recur across tasks, the entry may advance to supported and eventually to approved .Until then, it may be presented only as a hint and cannot override the current task’s statistical rules, budget, or approval requirements.