---
title: "Agentic-DPO: From Imitation to Agentic Policy Optimization on Expert Trajectories"
title_zh: "Agentic-DPO: 从模仿到基于专家轨迹的智能体策略优化"
authors: "Yixiong Chen, Alan Yuille"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10601v1"
tags: ["query:ase"]
score: 10.0
evidence: 轻量级离线智能体策略优化方法
tldr: "LLM agent通常通过监督微调模仿专家轨迹，但仅学习动作序列而非决策能力。Agentic-DPO将专家轨迹转化为状态级偏好数据，每步采样当前策略的一步动作，与专家动作进行DPO对比优化，并引入策略保持增强防止模式混淆。无需在线环境或奖励模型，在多个基准上显著提升，9B模型tau-bench准确率从21.7%升至41.4%，媲美在线GRPO。表明专家轨迹可低成本支持离线策略优化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10601v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1433, \"height\": 390, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10601v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1413, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10601v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 392, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10601v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 986, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 591, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 539, \"height\": 200, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1410, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1447, \"height\": 158, \"label\": \"Table\"}]"
motivation: 现有SFT训练agent仅模仿专家动作序列，未能学习在状态中选择正确动作，而强化学习方法需高成本环境交互。
method: Agentic-DPO将专家轨迹转为状态条件偏好，每步用当前策略采样一步动作，以合理错误动作为负例，通过DPO目标对比优化，并引入PPA避免策略与模式混淆。
result: "在StableToolBench、tau-bench等任务上超越SFT，9B模型tau-bench准确率从21.7%提升至41.4%，匹配在线GRPO。"
conclusion: 专家轨迹可转化为状态级动作偏好，实现低成本离线策略优化，无需环境交互或奖励模型。
---

## 摘要
大型语言模型（LLM）智能体通常使用监督微调（SFT）从专家轨迹中训练，这会将多轮智能体行为视为普通文本模仿。这种方法简单且成本低，但只能学习模仿专家动作序列，而不能训练智能体在每个状态下针对可能的错误选择正确动作。缓解此问题的现有方法包括偏好学习或强化学习，但它们通常需要高成本的环境交互和奖励模型。我们提出了Agentic-DPO，一种轻量级离线智能体策略优化方法，它将专家轨迹转化为状态条件偏好监督。在每个专家动作状态下，Agentic-DPO从当前状态采样一步动作，将可能的错误动作视为负样本，并使用DPO风格的偏好目标将其与专家动作进行对比。为了避免在偏好学习中将策略和模式混在一起，我们引入了策略保持增强（PPA），它在保持专家策略固定的情况下，在多种模式下渲染相同的潜在轨迹。Agentic-DPO不需要在线环境交互、奖励模型或全轨迹学生探索。我们在StableToolBench、tau-bench零售和Mind2Web上进行了实验，Agentic-DPO在不同模型规模上持续改进智能体，超越了模仿学习。特别是，对于9B模型，它将tau-bench准确率从21.7%（SFT）提高到41.4%，在相同骨干网络下匹配在线GRPO，仅需步级交互且梯度步骤中无需环境交互。结果表明，当从演示转化为状态级动作偏好时，专家轨迹可以支持低成本智能体策略优化。Agentic-DPO代码已发布在https://github.com/Schuture/Agentic-DPO。

## Abstract
Large Language Model (LLM) agents are commonly trained from expert trajectories using supervised fine-tuning (SFT), which treats multi-turn agent behavior as ordinary text imitation. This recipe is simple and low-cost, but it only learns to imitate the sequence of expert actions, rather than training the agent to choose the right action against plausible mistakes at each state. Existing methods to mitigate this problem include preference learning or reinforcement learning, but they usually need high-cost environment rollouts and reward models. We propose Agentic-DPO, a lightweight offline agent policy optimization method that turns expert trajectories into state-conditioned preference supervision. At each expert action state, Agentic-DPO samples a one-step action from the current state, treats plausible wrong actions as negatives, and contrasts them with the expert action using a DPO-style preference objective. To avoid mixing both policy and schema in preference learning, we introduce Policy-Preserving Augmentation (PPA), which renders the same latent trajectory under multiple schemas while keeping the expert policy fixed. Agentic-DPO requires no online environment rollout, reward model, or full-trajectory student exploration. We conduct experiments across StableToolBench, tau-bench retail, and Mind2Web, where Agentic-DPO consistently improves agents at different model scales beyond imitation. In particular, it raises tau-bench accuracy from 21.7% (SFT) to 41.4% for a 9B model, matching online GRPO under the same backbone with only step-level rollouts and without environment interaction during gradient steps. The results suggest that expert trajectories can support low-cost agentic policy optimization when converted from demonstrations into state-level action preferences. Code for Agentic-DPO is released at https://github.com/Schuture/Agentic-DPO.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：大型语言模型（LLM）智能体（Agent）通常通过监督微调（SFT）从专家轨迹中训练，但SFT仅模仿专家动作的文本序列，无法学习在每个状态下**选择正确动作**以避免合理错误。现有强化学习（RL）或偏好学习方法虽能提升决策能力，但需要高成本的环境交互、奖励模型或全轨迹探索。
- **整体含义**：该文提出一种轻量级离线策略优化方法**Agentic-DPO**，旨在仅利用专家轨迹（无需在线环境或奖励模型）就能将模仿学习转化为状态级决策偏好训练，显著提升小规模智能体的决策鲁棒性。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将每条专家轨迹中的每个动作步骤视为一个**状态条件偏好对**——以专家动作为正例，从当前学生策略在该状态下采样的一步动作为负例，通过DPO-style损失进行对比学习。
- **关键技术细节**：
  - **Agentic-DPO损失**：
    - 对于每个专家状态 \(s_t\)，专家动作 \(u_t^+\)，从当前策略采样 \(K=4\) 个候选动作，去掉无效或等价于专家的动作后，选取学生对数概率最高的动作作为负例 \(u_t^-\)。
    - 损失函数：\(L_{\text{ADPO}} = -\mathbb{E} \left[ \log \sigma\left(\beta_{\text{eff}} \left( \log \frac{\pi_\theta(a^+|s)}{\pi_\theta(a^-|s)} - \log \frac{\pi_{\text{ref}}(a^+|s)}{\pi_{\text{ref}}(a^-|s)} \right)\right) \right]\)，其中 \(\beta_{\text{eff}} = \beta / \max(|a^+|,|a^-|)^\alpha\) 用于长度缩放（\(\alpha=0.5\)）。
  - **SFT anchor**：在偏好损失上叠加SFT损失 \(L_{\text{train}} = L_{\text{ADPO}} + \lambda L_{\text{SFT}}\)（\(\lambda=0.5\)），防止专家动作概率下降并保持语法有效性。
  - **Policy-Preserving Augmentation (PPA)**：通过**动作渲染重写**（如ReAct与JSON格式互换、工具重命名）和**状态上下文重写**（在历史中注入错误与恢复轨迹）生成同一潜在决策的不同表面形式，使损失梯度聚焦于决策差异而非格式差异。
- **算法流程**：
  1. SFT warm-up（冻结 π_ref）。
  2. 进入多轮refresh：每轮用当前学生采样负例，构建偏好数据集。
  3. 用PPA渲染后的三元组进行梯度更新（含ADPO和SFT anchor）。

### 3. 实验设计：数据集、Benchmark、对比方法
- **数据集/场景**：
  - **StableToolBench**：工具使用场景，含3,786个专家轨迹。
  - **τ-bench retail**：长程用户交互的零售任务，使用官方轨迹（同3,786）。
  - **Mind2Web**：网页GUI操作，使用官方静态动作序列（7,362个）。
  - **OOD泛化**：BFCL-v3（零样本），训练使用ToolACE语料。
- **对比方法**：
  - **SFT**（基础基线）
  - **PPA+SFT**（仅PPA增强的SFT）
  - **DFT**（奖励矫正SFT变体）
  - **ETO**（轨迹级DPO）
  - **GRPO**（在线RL，仅用于StableToolBench和τ-bench）
- **评价指标**：
  - StableToolBench：**Canonical accuracy** 和 **Perturbed accuracy**（9种扰动操作）。
  - τ-bench：任务**成功率**。
  - Mind2Web：跨任务/网站/域的**平均步骤成功率**。

### 4. 资源与算力
- **明确说明**：论文提到训练在 **4块 NVIDIA A6000 GPU** 上进行。
- **超参数**：β=0.008，λ=0.5，K=4，R=5个refresh轮，batch size=16。
- **未明确**：具体训练时长（如小时数）未给出；但指出Agentic-DPO每步计算成本约为SFT的1.6倍，而GRPO为13.6倍。

### 5. 实验数量与充分性
- **实验数量**：涵盖3个Benchmark、3种模型规模（Qwen3.5-2B/4B/9B 及 Gemma3-4B）。主要结果表展示9种方法×3组种子平均±标准差。
- **消融实验**：对SFT warm-up、SFT anchor、动作渲染PPA、恢复上下文PPA四个组件进行了拆分（Table 2）。
- **鲁棒性与泛化**：
  - 在StableToolBench上区分与PPA重叠的2种扰动和7种未见扰动，分别报告准确率（Table 3）。
  - 零样本BFCL-v3评估（Table 4）。
  - 数据缩放与负样本数量K的缩放分析（Figure 2）。
  - refresh轮数R的影响（Figure 3）。
- **充分性与客观性**：实验设计较全面，覆盖了不同任务类型、模型大小、训练组件、泛化场景，并报告了统计不确定性（标准差）。对比方法涵盖常见基线（SFT、DPO变体、在线RL），结果呈现公正。

### 6. 论文的主要结论与发现
- **核心结论**：Agentic-DPO在所有Benchmark和所有模型规模上**显著超越SFT**，且**超过或持平于在线GRPO**（如τ-bench 9B：Agentic-DPO 41.4% vs GRPO 40.0%），但**训练成本降低数倍**。
- **数据效率**：使用25%专家数据即可达到PPA+SFT全量数据性能。
- **稳定性设计关键**：移除SFT warm-up或SFT anchor导致性能崩溃或大幅下降；缺少PPA使规范-扰动差距扩大。
- **PPA的贡献**：动作渲染PPA和恢复上下文PPA共同提升了对未见扰动的鲁棒性。
- **大模型收敛更快**：9B模型仅需1次refresh轮即达最优，而小模型需更多轮次。

### 7. 优点：方法或实验设计上的亮点
- **轻量离线**：无需在线环境交互、无需奖励模型、无需全轨迹探索，仅需一步采样。
- **理论与实践结合**：提供命题1（SFT与ADPO梯度同向）、推论2（局部KL下降）以及硬负例扩展（命题3）的数学证明。
- **稳定设计**：SFT anchor + PPA有效解耦策略与模式，避免偏好学习被格式噪声主导。
- **实验覆盖全面**：三种不同任务领域（工具、长程对话、网页），多尺度模型，多种消融和泛化测试。
- **开源代码**：公开了GitHub仓库，促进可复现性。

### 8. 不足与局限
- **离线局限**：Agentic-DPO只能在**专家轨迹已有的状态**上构建偏好，无法探索学生长程行为导致的新状态（与在线RL的根本差异）。
- **依赖专家覆盖**：如果专家轨迹未覆盖关键决策状态，则训练信号不足。
- **采样成本**：虽然比RL低，但仍需每步从学生策略生成负样本（1.6×SFT成本），对于极大规模数据可能累计较大开销。
- **实验范围**：仅评估了2B–9B模型，未测试更大模型（如13B/70B）；仅在三个benchmark上进行，未涵盖需要大量探索的任务（如SWE-bench）。
- **超参数敏感度**：虽然提供了一个固定超参数集，但未系统研究β、λ、α等的最优变化对性能的影响（仅给出理论分析方向）。
- **模式混淆风险**：即使有PPA，如果特定模式在训练数据中出现频率不均，仍可能残留轻微的格式偏好。

（完）
