---
title: "SelfMem: Self-Optimizing Memory for AI Agents"
title_zh: "SelfMem: 面向人工智能代理的自我优化记忆系统"
authors: "Shu Yang, Junchao Wu, Derek F. Wong, Di Wang"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03726v1"
tags: ["query:ase"]
score: 9.0
evidence: 面向AI智能体的自优化记忆，实现自我改进
tldr: "当前AI智能体支持长上下文但仍需记忆系统，现有框架固定且需手动调参。SelfMem提供记忆工具和反馈信号，让智能体自主探索并优化记忆策略。在BEAM上，SelfMem在100K/500K/1M token下分别提升48.7%、40.8%、41.9%。SelfMem在不同需求下表现稳健，模型引导优化进一步提效，展示自优化记忆潜力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03726v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 798, \"height\": 572}, {\"url\": \"assets/figures/arxiv/2607.03726v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1617, \"height\": 1041}, {\"url\": \"assets/figures/arxiv/2607.03726v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 824, \"height\": 873}, {\"url\": \"assets/figures/arxiv/2607.03726v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1654, \"height\": 553}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03726v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1662, \"height\": 1419}, {\"url\": \"assets/tables/arxiv/2607.03726v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1670, \"height\": 1103}, {\"url\": \"assets/tables/arxiv/2607.03726v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1671, \"height\": 1406}]"
motivation: 现有记忆框架使用固定存储、检索和总结机制，在不同任务间缺乏灵活性且需手动调参。
method: SelfMem提供记忆工具和反馈信号，让智能体自主探索、评估并优化策略。
result: "在BEAM上SelfMem在100K/500K/1M token下分别提升48.7%/40.8%/41.9%。"
conclusion: 模型引导的策略优化进一步提升性能，证明了自优化记忆的鲁棒性和有效性。
---

## 摘要
尽管当前的AI代理能够支持越来越长的上下文窗口、工具使用以及长时任务的技能执行，但它们仍然需要记忆系统来有效利用历史经验。现有的记忆框架通常依赖固定的存储、检索和总结机制，这在不同的任务中可能显得僵化，且往往需要手动调整。为解决这一局限，我们提出SelfMem，一种自我优化的记忆框架。受先前关于自我改进AI工作的启发，我们遵循“授人以渔而非授人以鱼”的原则。SelfMem不为模型强制规定预定义的记忆策略或格式，而是提供一个带有记忆工具和反馈信号的环境，使代理能够探索、评估并完善自身的记忆策略。我们的结果显示，在100K至1M token的对话规模下，SelfMem在BEAM上的表现始终优于检索、压缩以及代理记忆基线。与最强基线相比，SelfMem在100K、500K和1M规模下分别将官方评分提升了48.7%、40.8%和41.9%。进一步的问题类型分析表明，该方法在多样化的记忆需求下具有广泛的鲁棒性，而我们的优化研究显示，模型引导的策略精炼进一步提升了性能。

## Abstract
While current AI agents support increasingly long context windows, tool use, and skill execution for long-horizon tasks, they still require memory systems to effectively leverage historical experience. Existing memory frameworks typically rely on fixed storage, retrieval, and summarization mechanisms, which can be rigid across different tasks and often require manual tuning. To address this limitation, we propose SelfMem, a self-optimizing memory framework. Inspired by prior work on self-improving AI, we follow the principle of "teaching an agent to fish rather than giving it a fish." Instead of forcing the model to follow a predefined memory strategy or format, SelfMem provides an environment with memory tools and feedback signals that allow the agent to explore, evaluate, and refine its own memory strategy. Our results show that SelfMem consistently outperforms retrieval, compression, and agent-memory baselines on BEAM across conversation scales from 100K to 1M tokens. Compared with the strongest baseline, SelfMem improves the official score by 48.7%, 40.8%, and 41.9% at 100K, 500K, and 1M, respectively. Further question-type analysis shows broad robustness across diverse memory demands, and our optimization study shows that model-guided strategy refinement further improves performance.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有 AI 智能体虽然支持长上下文、工具使用和技能执行，但在长时间交互中仍需要有效的记忆系统来利用历史经验。然而，现有的记忆框架通常采用固定的存储、检索和总结机制（如 RAG、MemGPT、MemoryBank 等），这些机制在不同任务间缺乏灵活性，且往往需要手动调整参数，难以自适应任务需求变化。
- **整体含义**：论文主张“授人以渔而非授人以鱼”——与其为智能体预定义固定的记忆策略，不如提供一个记忆工具与反馈信号环境，让智能体自主探索、评估和优化自身的记忆策略，从而实现适应性强、成本效益好的长期记忆管理。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：SelfMem 将记忆管理转变为一个由智能体控制的优化过程，而不是给智能体固定存储、更新或检索规则。它暴漏一组记忆工具（如读取、写入、审查、自测等）以及反馈信号（如响应性能、token 数量、成本、缓存复用等），让智能体自主决定记忆的内容、形式、更新时机以及哪些信息可通过转录检索。
- **关键技术细节**：
  - **Transcript Store（转录存储）**：原始对话历史作为不可变的只读数据源，以 SQLite 表存储（turns 表）。智能体只能通过只读工具（`conversation_manifest`、`conversation_sql_query`）检查转录，避免隐藏总结或预提取事实。
  - **Memory Workspace and Action Space（记忆工作空间与动作空间）**：智能体管理的记忆工作区可以包含任意结构（如用户画像、偏好列表、项目状态、时间线等）。动作空间包括：
    - 转录读取工具（manifest、SQL 查询、搜索、审计等）
    - 记忆读取工具（检查当前工作区）
    - 记忆写入工具（添加、替换、合并、存档、精确事实记录、时间线等）
    - 记忆审查工具（诊断缺失源支持、过时事实、矛盾等）
    - 自测工具（基于观察到的转录和工作区构建内部探测问题，无需隐藏标注）。
  - **反馈信号**：反馈来自工具和环境（如转录读取结果、工作区读取结果、写诊断、审查反馈、token 使用、延迟、缓存复用等），不包含隐藏的基准标注。智能体通过语言推理这些权衡并调整记忆行为。
  - **记忆构造流程**：初始化 M0 = ∅ → 智能体步骤 i 选择动作 ai，观察反馈 → 若 ai ∈ A_write，更新 Mi = Apply(Mi-1, ΔMi) → 可多次 inspect-write-review-revise 循环。
  - **记忆条件回答**：最终工作区 M* 与检索到的转录片段 Eq 一起提供给答案模型，M* 作为紧凑指南，Eq 提供确切事实。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：使用 BEAM 基准（Tavakoli et al., 2025），提供 100K、500K、1M token 三种规模的长对话历史，包含 100 个对话和 2000 个探测问题，覆盖信息提取、时间推理、多会话推理、事件排序等类型。
- **评估协议**：问题按顺序提问，之前模型生成的问答对附加到后续提示中，模拟多轮探测会话，支持提示缓存复用。
- **对比方法**：包括 RAG、Full Context（完整上下文）、Compression（压缩）、LoCoMo、MemoryBank、ReadAgent、MemGPT、A-Mem、Mem0。所有方法使用相同对话、问题和答案模型（GPT-5.4-nano 用于记忆构建和答案生成，GPT-5.4-mini 作为评判模型），仅改变历史到提示的转换方式。
- **评估指标**：官方得分（Score）、Pass 0.5（得分≥0.5 的问题比例）、成本（Cost）、缓存命中率、LLM 请求数、嵌入请求数。

## 4. 资源与算力

- **明确提及**：所有 LLM 调用使用 GPT-5.4-nano（记忆构建和答案生成）和 GPT-5.4-mini（评判）。使用确定性解码（温度 0）。未说明使用的 GPU 型号、数量、训练时长等。论文未涉及模型训练，仅进行推理调用。
- **注意**：论文没有提供具体算力资源信息（如 GPU 数量、训练时间等），因为 SelfMem 是一个基于 API 调用的框架，不需要训练。

## 5. 实验数量与充分性

- **实验数量**：在 BEAM 上进行了三组主要规模实验（100K、500K、1M token），每个规模下对比 10 种方法（包括 SelfMem）。此外进行了问题类型分析（10 个问题类型）和策略优化分析（8 轮迭代、不同训练集规模）。
- **充分性**：覆盖了多个上下文长度和广泛的问题类型，对比基线全面（包含经典 RAG、最近记忆方法如 Mem0、A-Mem 等），使用了统一评测框架确保公平。但仅使用 BEAM 一个基准，未在其他长期记忆基准上验证；优化实验仅在 100K 规模进行。
- **客观性**：所有方法在相同对话、问题和评判协议下评估，报告了成本、缓存等指标，结果透明。

## 6. 论文的主要结论与发现

- SelfMem 在所有 100K、500K、1M token 规模下均取得最佳官方得分和 Pass 0.5。与最强非 SelfMem 基线（RAG）相比，分别提升 0.165、0.141、0.134 得分，Pass 0.5 分别提高 17.0、14.9、14.3 个百分点（约 48.7%、40.8%、41.9% 的相对提升）。
- 更高的准确性不需要昂贵的记忆流水线：SelfMem 成本远低于 MemGPT、A-Mem、Mem0 等，同时性能更优。
- 问题类型分析显示 SelfMem 在 9/10、8/10、7/10 问题类型上最佳，在信息提取、知识更新、多会话推理、偏好跟随、总结、时间推理等类型上优势明显。
- 策略优化分析表明通过迭代反馈精炼可以进一步提升性能：从默认 held-out 得分 0.472 提升到最终策略 0.497，最佳策略达到 0.510。

## 7. 优点

- **方法创新性**：将记忆策略从固定管线转变为智能体控制的优化过程，暴露工具和反馈信号，让智能体自适应。符合“自我改进 AI”趋势。
- **性能优异**：在所有规模和问题类型上显著优于固定策略基线，且成本更低。
- **设计透明**：保留原始转录作为唯一真相源，记忆操作可审计，每步写入记录来源和理由。
- **鲁棒性**：在多样化记忆需求下表现稳健，不依赖单一策略。

## 8. 不足与局限

- **实验广度有限**：仅在 BEAM 一个基准上评估，未在 LongMemEval、LoCoMo 等其他长期记忆基准上验证；优化分析仅在 100K 规模进行，未扩展到 500K 和 1M。
- **模型与 API 依赖**：仅使用 GPT-5.4 系列模型，未在其他模型家族（如开源 LLM）上验证。结果可能受 API 特性和定价影响。
- **基线实现差异**：基线在高性能生产环境下可能有不同效率表现，论文虽尽力公平但难以完全消除实现差异。
- **策略优化分析受限于训练集大小和迭代次数**，最佳策略出现在中间阶段而非最大迭代，需要进一步理解其规律。
- **未涉及在线学习或多轮独立交互**：所有实验基于固定历史，未测试 SelfMem 在持续交互中动态更新的能力。
- **限制部分承认**：评估广度、工程实现差异等需要未来工作验证。

（完）
