---
title: "MemoHarness: Agent Harnesses That Learn from Experience"
title_zh: MemoHarness：从经验中学习的智能体操纵框架
authors: "Yue Huang, Wenjie Wang, Han Bao, Yuchen Ma, Xiaonan Luo, Yi Nian, Haomin Zhuang, Zheyuan Liu, Yue Zhao, Xiangliang Zhang"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.14159v1"
tags: ["query:ase"]
score: 8.0
evidence: 从自身执行中学习的自适应框架优化
tldr: 现有智能体框架固定使用单一全局控制层（harness），缺乏自适应能力。MemoHarness将harness分解为六个可编辑维度，通过双层面经验库存储和执行诊断及模式，为每个测试用例检索经验动态调整配置，无需额外反馈或搜索。在shell、代码生成和分析推理任务上，MemoHarness性能优于固定harness，并能选择性迁移到未见套件和模型，且缓存经验可维持低成本。研究表明执行经验是构建自适应harness的有效基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14159v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1611, \"height\": 958, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14159v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 715, \"height\": 340, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14159v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1611, \"height\": 627, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14159v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1621, \"height\": 516, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14159v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1646, \"height\": 1104, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14159v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1648, \"height\": 323, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14159v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 914, \"height\": 467, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14159v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1357, \"height\": 352, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14159v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1614, \"height\": 473, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14159v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 965, \"height\": 468, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14159v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 887, \"height\": 976, \"label\": \"Table\"}]"
motivation: 现有harness设计固定且无法自动优化，导致智能体行为缺乏适应性，亟需利用执行经验动态调整控制策略。
method: 将harness分解为六个控制维度，构建双层面经验库存储案例诊断和全局模式，在推理时检索相关经验动态调整各维度配置。
result: 在shell-agent、代码生成和分析推理基准上，MemoHarness优于固定harness，并部分迁移至未见套件和模型，成本可控。
conclusion: 执行经验可作为构建自适应agent harness的实用基础，比单一静态配置更灵活，为未来智能体自动化优化提供新方向。
---

## 摘要
智能体操纵框架是外部控制层，通过管理上下文、工具、编排、记忆、解码和输出处理，将基础大语言模型转变为可执行的智能体。虽然操纵框架的设计强烈影响智能体行为，但大多数自动改进方法优化的是更狭窄的工件，如提示、流水线或工作流，并且部署的智能体通常对所有情况重用单一的全局操纵框架。我们提出了MemoHarness，一种自适应操纵框架优化框架，它从自身的执行中学习。MemoHarness将操纵框架分解为六个可编辑的控制维度，在双层经验库中存储每个案例的诊断和提炼的全局模式，并使用检索到的经验将学习到的操纵框架适应到每个测试案例，无需测试时的标签、反馈或额外搜索。在我们对shell智能体、代码生成和分析推理基准的评估中，MemoHarness相比于我们对比的固定操纵框架有所改进，并显示出对未见套件和基础模型的选择性迁移。当检索到的经验大部分可缓存时，其额外的上下文也能保持成本竞争力。这些结果证明，执行经验是构建比单一静态配置更具适应性的智能体操纵框架的实用基础，而关于统计稳健性和组件归因的更广泛主张则留待未来工作。

## Abstract
An agent harness is the external control layer that turns a base LLM into an executable agent by managing context, tools, orchestration, memory, decoding, and output handling. While harness design strongly affects agent behavior, most automatic improvement methods optimize narrower artifacts such as prompts, pipelines, or workflows, and deployed agents usually reuse a single global harness for all cases. We introduce MemoHarness, an adaptive harness optimization framework that learns from its own executions. MemoHarness decomposes the harness into six editable control dimensions, stores per-case diagnoses and distilled global patterns in a dual-layer experience bank, and adapts the learned harness to each test case using retrieved experience without test-time labels, feedback, or additional search. In our evaluation across shell-agent, code-generation, and analytical-reasoning benchmarks, MemoHarness improves over the fixed harnesses we compare against and shows selective transfer to unseen suites and base models. Its additional context can also remain cost-competitive when much of the retrieved experience is cacheable. These results provide evidence that execution experience is a practical substrate for building agent harnesses that are more adaptive than a single static configuration, while leaving broader claims about statistical robustness and component attribution to future work.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前的 LLM 智能体（agent）依赖于一个固定的、全局唯一的“操纵框架”（harness）——即控制上下文构建、工具调用、推理编排、记忆管理、解码参数和输出处理的系统层。然而，不同任务在领域、推理深度、检索需求和输出格式上差异巨大，固定框架常常导致系统性误配，且现有的自动化优化方法多局限于提示或流水线，很少对完整的 harness 进行自适应、结构化的调整。
- **整体含义**：提出 MemoHarness，一个能从自身执行经验中学习的自适应 harness 优化框架，旨在使 harness 不再是一成不变的静态配置，而是能够根据每个测试案例的具体特点动态调整，从而提升智能体的任务成功率，并具备迁移能力。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将代理的完整控制层解耦为六个可编辑的功能维度，通过搜索阶段积累案例级和全局级的诊断经验，在测试阶段利用这些经验（无需额外标签或反馈）为每个新案例生成定制化的 harness 配置。
- **关键技术细节**：
  1. **六维操纵空间**（§2.3）：
     - D1 上下文组装（提示结构、示例等）
     - D2 工具交互（检索启用、top-k 等）
     - D3 生成控制（max tokens、温度、采样数）
     - D4 编排（单次调用→规划/执行/细化等拓扑）
     - D5 记忆管理（跨调用状态持久化、摘要、丢弃陈旧上下文）
     - D6 输出处理（提取答案、验证架构、回退）
  2. **双层面经验库**（§2.4）：
     - 案例级经验 \( \mathcal{E}_t \)：每次执行的轨迹、奖励、成本、诊断（成功/失败、主因维度、自然语言分析等）
     - 全局经验 \( \mathcal{G}_t \)：从失败簇中蒸馏出的跨案例规律，包括反复出现的现象、证据和预期修复效果
  3. **训练阶段搜索**（§2.5）：
     - 从最简 harness \( W_0 \) 出发（零示例、无工具等），确保所有组件均由经验证明。
     - 控制器基于当前经验和查询，从经验库中检索证据，提出下一个候选配置 \( W_t \)。
     - 在每个搜索用例上执行并收集轨迹、奖励和诊断。
     - **正确性优先排序**：按平均奖励降序选者，仅当奖励相同时才以 token 使用量作为副排序标准（lexicographic ordering）。
  4. **测试阶段案例适应**（§2.6）：
     - 对于每个未见测试用例 \( x = (u, \phi) \)，通过指令嵌入的余弦相似度检索最相似的成功和失败案例，以及相关的全局模式。
     - 控制器基于检索到的证据，将全局最优 harness \( W^* \) 调整为案例特定 harness \( W(x) \)。
     - 整个过程无需迭代搜索、梯度更新或测试时反馈。

### 3. 实验设计：数据集/场景、Benchmark、对比方法

- **三个主要基准**：
  - **Terminal-Bench**：长期壳代理任务（多步工具使用、文件编辑、进程管理），使用 80/20 分割（71 训练 + 18 评估用例）。
  - **LiveCodeBench**：单次代码生成（竞赛级编程问题）。
  - **FinanceAgent**：多步金融推理（需要检索和分析文档）。
- **外部迁移套件**（用于 RQ3）：MMMU（知识）、HumanEvalFix（代码修复）、StrongReject（安全拒绝）、Reasoning-Gym-Easy（可验证推理）、LawBench（法律推理）、SWE-Bench Pro（长期软件工程）。
- **对比方法**（Terminal-Bench 上的基线）：
  - OpenAI **Codex**（最强的终端导向基线）
  - Anthropic **Claude Code**
  - 开源 **OpenCode**
  - 基准提供的中性测试床代理 **Terminus**
- **跨模型迁移**（RQ4）：在 GPT-5.3-Codex 上搜索后，直接应用于 6 个额外模型：Claude-Sonnet-4.6、Gemini-3.1-Pro、Qwen3.5-397B-A17B、GLM-5、GPT-4.1、DeepSeek-V3.2。
- **成本对比**（RQ5）：在 Terminal-Bench 18 个测试用例上比较 token 使用量和美元成本（按公开价计算）。

### 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量或训练时长。搜索阶段使用 GPT-5.3-Codex（只能通过 API 访问），属于闭源模型。跨模型迁移实验也依赖 API 或开模型推理。成本分析中提及 token 数量和按公开价换算的美元成本，但未提供完整的硬件占用信息。

### 5. 实验数量与充分性

- **实验组数**：
  - 主要结果（RQ1）：1 组 Terminal-Bench 对比图（图 2）
  - 搜索演进（RQ2）：3 个基准上的 6 个检查点（图 3）+ 两个基准的逐轮曲线（图 4）
  - 跨数据集迁移（RQ3）：3 个搜索源 × 6 个外部套件 = 18 组对比（表 2）
  - 跨模型迁移（RQ4）：1 个搜索源 × 6 个目标模型 = 6 组对比（表 3）
  - 成本分析（RQ5）：5 个框架在 18 个测试用例上的 token/cost 对比（表 4）
  - 操作级诊断（附录 G）：约 17 种 shell 操作的 lift 分析（表 6）
- **充分性评价**：
  - 覆盖了多任务类型（shell、代码、推理）、跨数据集、跨模型、成本分析。
  - **不足**：作者在局限中明确指出：Terminal-Bench 的主结果仅使用 18 个用例的点估计，无置信区间或显著性检验；部分基线不是纯 scaffold 移植（系统级对比）；未完全隔离每个组件（经验库、全局模式、测试时适应）；成本分析依赖观察到的高缓存率（13.32/14.18 M 输入 token 可缓存），可能无法泛化；控制器和诊断操作采用启发式实现。

### 6. 论文的主要结论与发现

- **RQ1**：在 Terminal-Bench 上，MemoHarness 达到 0.806，优于最强基线 Codex（0.722）和其余基线（+0.250～+0.445）。
- **RQ2**：搜索能稳定提升质量，在三个基准上最终均优于初始基座（Terminal-Bench 0.722→0.806；LiveCodeBench 0.900→0.967；FinanceAgent 0.600→0.767）。长程代理任务受益最多，单次代码生成接近饱和时增益较小。
- **RQ3**：跨数据集迁移具有选择性：Terminal-Bench 学习的 harness 在 MMMLU、StrongReject、SWE-Bench Pro 上提升较大（+0.030、+0.030、+0.059），但某些套件无变化（HumanEvalFix、Reasoning-Gym-Easy），说明不会是通用万能模板。
- **RQ4**：跨模型迁移所有 6 个模型均显示提升，平均增益 +0.098，最大增益在 GLM-5（+0.233），最小在 GPT-4.1（+0.038）。表明学习到的控制决策具有一定可移植性。
- **RQ5**：MemoHarness 总成本 $6.89，低于 Codex ($10.28) 和 Claude Code ($9.51)，且任务成功率更高；但昂贵程度高于精度更低的 Terminus 和 OpenCode。前提是大多数检索上下文可缓存。

### 7. 优点

- **结构化的 harness 空间**：将黑箱 harness 分解为六个时间流程维度，使搜索和诊断具有可解释性和可编辑性。
- **双层面经验库**：同时存储案例级和全局蒸馏知识，不仅保存得分，还保存失败原因和维度交互信息，使搜索从“分数驱动”变为“诊断驱动”。
- **测试时无反馈适应**：无需标签、迭代或梯度，仅利用检索到的经验就能为每个案例生成定制 harness，实用性强。
- **正确性优先排序**：避免搜索跑偏到“便宜但错误”的配置。
- **跨任务、跨模型、跨数据集的选择性迁移能力**：证明学习到的控制决策并非过拟合于单一场景。
- **成本分析考虑缓存**：展示在实际部署中缓存经验能显著降低成本。

### 8. 不足与局限

- **统计稳健性不足**：主结果仅为点估计，无置信区间或显著性检验。
- **基线对比不纯粹**：部分基线（如 Claude Code）不能完全控制基础模型，因而是系统级比较而非纯 scafold 消融。
- **组件未完全隔离**：未分别消融经验库、全局模式和测试时适应对最终性能的贡献。
- **成本假设受限**：高缓存率（>90%）可能不适用于所有部署场景。
- **控制器和诊断为启发式实现**：虽确保可复现，但可能不是最优；未来可学得更通用的控制器。
- **Terminal-Bench 测试规模较小**：仅 18 个用例，需要更大样本验证。
- **未做在线经验积累**：目前仅在固定训练集上搜索，测试后不更新经验库。

（完）
