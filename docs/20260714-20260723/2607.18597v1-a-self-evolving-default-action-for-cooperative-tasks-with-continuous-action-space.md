---
title: A Self-Evolving Default Action for Cooperative Tasks with Continuous Action Space
title_zh: 连续动作空间协作任务中的自演化默认动作
authors: Shuangyao Huang
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18597v1"
tags: ["query:ase"]
score: 9.0
evidence: 多智能体强化学习中的自我演化默认动作
tldr: 多智能体强化学习在连续动作空间中的反事实信用分配面临挑战，现有方法因蒙特卡洛采样引入偏差且无法保证收敛。本文提出SAFE框架，利用智能体经验缓存中自演进的默认动作构造反事实基线，自然扩展至连续动作空间。该基线无偏地量化个体贡献，确保确定性策略梯度收敛到局部最优。在协作车辆任务中，SAFE显著优于现有模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18597v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1837, \"height\": 1248, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18597v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 839, \"height\": 224, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18597v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1480, \"height\": 899, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18597v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1672, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18597v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1849, \"height\": 693, \"label\": \"Figure\"}]"
motivation: 现有连续动作空间的反事实基线因蒙特卡洛采样引入偏差，无法保证收敛到局部最优。
method: 提出SAFE框架，以智能体经验缓存中自演进的默认动作构建反事实基线，无需额外模拟或先验知识。
result: 在协作车辆任务上，SAFE一致超越当前最优模型，并实现无偏策略梯度与收敛保证。
conclusion: SAFE为连续动作空间的多智能体协作学习提供了一种无偏且收敛的信用分配机制。
---

## 摘要
反事实信用分配在离散动作空间的多智能体强化学习（MARL）中已被证明有效，但其在连续动作协作任务中的扩展仍然具有挑战性。现有方法通过蒙特卡洛采样近似反事实基线，往往会在策略梯度中引入偏差，并且无法保证收敛到局部最优，因为采样动作可能未得到充分训练。为解决这些局限性，我们提出SAFE，一种新颖的MARL框架，其采用基于从每个智能体经验缓冲区中采样的自演化默认动作的条件反事实基线。该设计自然扩展到连续动作空间，无需依赖额外的模拟、奖励模型或环境特定的先验知识。该基线精确量化每个智能体的贡献，且不向确定性策略梯度引入偏差，确保收敛到局部最优。在协作车辆任务上的大量实验表明，SAFE consistently优于最先进模型。

## Abstract
Counterfactual credit assignment has proven effective in multi-agent reinforcement learning (MARL) for discrete action spaces, yet its extension to continuous-action cooperative tasks remains challenging. Existing methods that approximate the counterfactual baseline via Monte Carlo sampling often introduce bias into policy gradients and fail to guarantee convergence to local optima, as the sampled actions may not have been sufficiently trained. To address these limitations, we propose SAFE, a novel MARL framework that employs a counterfactual baseline conditioned on a self-evolving default action sampled from each agent's experience buffer. This design naturally extends to continuous action spaces without relying on additional simulations, reward models, or environment-specific prior knowledge. The baseline accurately quantifies each agent's contribution, and introduces no bias into the deterministic policy gradient, ensuring convergence to local optima. Extensive experiments on cooperative vehicular tasks demonstrate that SAFE consistently outperforms state-of-the-art models.

---

## 论文详细总结（自动生成）

# 论文详细总结：SAFE（Self-Evolving Default Action for Cooperative Tasks with Continuous Action Space）

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：多智能体强化学习（MARL）在**离散动作空间**中，反事实信用分配（如COMA）通过计算基线来量化个体贡献，效果显著。但在**连续动作空间**中，直接扩展（如蒙特卡洛采样）会导致：
  - 采样动作未充分训练，基线估计有偏，无法准确反映智能体贡献；
  - 策略梯度引入偏差，无法保证收敛到局部最优。
- **整体含义**：需要一种**无偏、收敛有保证**的连续动作空间信用分配方法。SAFE通过从经验缓存中采样“自演化默认动作”构造反事实基线，成功解决了上述挑战。

## 2. 方法论
- **核心思想**：利用每个智能体自己的**经验缓存**（replay buffer）均匀采样一个默认动作 \(\bar{a}_i(t)\)，替换当前动作 \(a_i\)，计算反事实基线 \(b_i = Q(s, a_{-i}, \bar{a}_i(t))\)。
- **关键细节**：
  - 集中式Critic（训练时使用全局状态和联合动作历史），分散式Actor（CTDE范式）。
  - 优势函数：\(A_i = Q(s,a) - b_i\)，用于更新策略。
  - 默认动作随训练动态演化：随着智能体行为收敛，经验缓存中高频动作逐渐代表其平均性能。
- **理论保证（引理0.1）**：
  - 通过将确定性策略视为随机策略（方差趋零）的特例，证明该基线对策略梯度无偏，且保证收敛到局部最优（基于Konda & Tsitsiklis的Actor-Critic收敛理论）。
- **算法流程**（文字简述）：
  1. 每个智能体以Actor网络输出动作（加噪声 \(\epsilon\) 探索）。
  2. 所有动作和环境交互，存储经验（包括联合动作、全局状态、团队奖励）到各智能体缓存。
  3. 训练Critic：拟合 \(Q(s,a)\)（含目标网络）。
  4. 对每个智能体，从其缓存中均匀采样一个默认动作 \(\bar{a}_i\)，计算基线 \(b_i\)。
  5. 计算优势 \(A_i\)，更新Actor（最大化 \(Q\)）和Critic（最小化TD误差）。

## 3. 实验设计
- **环境**：Highway-Env（Gym兼容），协作高速公路驾驶任务，受控车辆（绿色）需避免与障碍车辆（蓝色）碰撞并保持编队。
- **场景**：四种复杂度递增的配置：
  - 2V1O（2辆车，1个障碍）
  - 3V2O
  - 5V2O
  - 7V2O
- **对比方法**（benchmark）：
  - 离散动作方法：VDN、QMIX、IQL、COMA、MAPPO（使用离散元动作：左/右换道，加速/减速，空闲）。
  - 连续动作消融对比：SAFE_a=0（默认动作固定为0）、SAFE_batch_mean（采样32个动作求平均）、COMA_cont（蒙特卡洛采样连续COMA）、Centralized_critic（无信用分配，仅集中Critic）。
- **评估指标**：平均回合奖励（Average Episodic Reward），运行3次独立种子。

## 4. 资源与算力
- **GPU**：NVIDIA RTX 5060Ti，16 GB显存（文中明确写出）。
- **训练**：每个场景3个独立运行，但**未说明具体训练时长**（例如episode总数或小时数）。参数\(\epsilon\)退火在50,000个episode内完成，总episode约为175,000，但未给出时间开销。

## 5. 实验数量与充分性
- **主要实验**：4个场景 × 6种对比方法（SAFE vs. VDN / QMIX / IQL / COMA / MAPPO）+ 每个3个种子，共约 \(4 \times 6 \times 3 = 72\) 条学习曲线。
- **消融实验**：5种变体（SAFE_a=0, SAFE_batch_mean, COMA_cont, Centralized_critic）在3V2O场景下比较，3个种子。
- **参数研究**：不同采样数量K（1,30,50,80,100）在3V2O场景下比较，3个种子。
- **充分性评价**：
  - **优点**：场景复杂度覆盖较全（2~7辆车），对比了离散和连续基线的多个代表性方法，消融设计清晰（隔离了信用分配与连续适应）。
  - **不足**：
    - 仅在**单一任务**（highway驾驶）上验证，缺乏其他连续控制任务（如机器人、无人机编队）的泛化证据。
    - 未与最新的连续动作MARL方法（如MADDPG、QMIX变体用于连续、FACMAC等）直接对比，只对比了MAPPO和COMA的连续版本。
    - 未提供统计显著性检验（如置信区间、p值）。
    - 3V2O作为消融场景选择合理，但未在其他场景重复消融。

## 6. 主要结论与发现
1. **SAFE在所有场景中显著优于所有基准**，离散动作方法（VDN/QMIX/IQL）仅在2V1O简单场景中成功，其余均失败；COMA和MAPPO因on-policy采样效率问题持续低效。
2. **性能提升归因于信用分配机制**：消融实验显示，仅连续动作适应（如Centralized_critic、COMA_cont）无法达到SAFE水平。
3. **默认动作自演化特性验证**：图4显示，训练初期默认动作分布集中于0附近，后期智能体0/1偏向负转向（左躲），智能体2保持中立，反映了学习到的协作策略。
4. **单个采样优于多个平均**：参数研究中 \(K=1\) 表现最好，增加K反而引入额外偏差导致性能下降。

## 7. 优点
- **理论扎实**：严格证明所提基线不引入偏差且保证收敛（引理0.1），这在连续动作空间MARL中少见。
- **设计简洁实用**：无需额外模拟器、奖励模型或先验知识，仅利用现有经验缓存。
- **实验规范**：所有结果基于三随机种子，消融和参数分析全面，结论清晰。
- **可解释性**：通过默认动作的分布演化图直观解释模型行为。
- **性能突出**：在较复杂场景（7V2O）中依然能学习有效策略，展示良好扩展性。

## 8. 不足与局限
- **任务多样性不足**：仅测试了高速公路驾驶场景，未涉及更复杂的连续控制环境（如多机器人协作、Starcraft II等），泛化性待验证。
- **对比方法局限性**：未包含当代流行的连续动作MARL算法（如MADDPG、DOP、FACMAC、QMIX连续扩展），公平性可能受影响（尤其是MAPPO在连续动作上表现不佳的原因可能是其特性而非信用分配）。
- **实验统计严谨性**：未报告方差或置信区间，无法直接判断性能差异显著性；且每个场景运行次数仅3次，可能不够充分。
- **可扩展性未充分测试**：最大场景为7个智能体，实际大规模系统（>10）未涉及；GPU显存限制可能阻碍更大规模。
- **数学证明的实践限制**：证明依赖随机策略极限假设，实际训练中\(\epsilon\)退火可能不完全满足，且离散化采样默认动作可能仍存在微小偏差（但实验表明可忽略）。
- **计算开销**：虽然避免了额外模拟，但每次更新需从缓存中采样默认动作并前向Critic一次，相比无基线方法略有额外计算。

（完）
