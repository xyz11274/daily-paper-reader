---
title: "SelfOp: An Optimization Algorithm for Self-Improving Security Agents"
title_zh: SelfOp：一种用于自我改进安全智能体的优化算法
authors: "Saad Ullah, Yigitcan Kaya, Christopher Kruegel, Giovanni Vigna, Gianluca Stringhini"
date: 2026-09-19
pdf: "https://arxiv.org/pdf/2609.22792v1"
tags: ["query:agent-evolve"]
score: 9.0
evidence: 自动自我改进冻结安全智能体上下文的优化算法
tldr: LLM 智能体常用于漏洞挖掘、利用复现与补丁生成，但这类安全任务缺乏专家示范与可计算奖励。SelfOp 将上下文优化转化为链式法则启发的文本梯度下降，从单实例结果反向传播误差至评估器、轨迹与上下文产物。梯度经聚类、排序、过滤并按跨实例共识提交，收敛检测器自动停止。在 CyberGym 上，用不足 200 条样本使 GPT-5.4-mini 自我提升 17 分并超越 GPT-5.4 基线 6 分，技能还可跨模型迁移。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.22792v1/fig-001.webp\", \"caption\": \"\", \"page\": 6, \"index\": 1, \"width\": 720, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2609.22792v1/fig-002.webp\", \"caption\": \"\", \"page\": 7, \"index\": 2, \"width\": 333, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2609.22792v1/fig-003.webp\", \"caption\": \"\", \"page\": 13, \"index\": 3, \"width\": 1041, \"height\": 571}, {\"url\": \"assets/figures/arxiv/2609.22792v1/fig-004.webp\", \"caption\": \"\", \"page\": 13, \"index\": 4, \"width\": 1041, \"height\": 571}]"
motivation: 安全任务缺乏专家示范与可计算奖励，人工调优上下文需领域专家且难以扩展，自动化方法又依赖稀缺标注或强优化模型，易退化为昂贵试错。
method: 将上下文优化视为文本梯度下降：由单实例结果反向传播误差信号，再经聚类、排序、过滤与跨实例共识提交，并用收敛检测器自动停止。
result: CyberGym 上不足 200 条样本使 GPT-5.4-mini 提升 17 分并超越 GPT-5.4 基线 6 分，GPT-5.4 自身提升 18.5 分。
conclusion: SelfOp 不改模型权重与执行框架即可自优化上下文，优化后的技能可跨模型迁移，表明其学到的是通用任务知识而非模型特定模式。
---

## 摘要
大语言模型智能体正越来越多地用于安全任务：漏洞发现、漏洞利用复现和补丁生成。在模型层面改进它们需要专家示范或可计算奖励，而安全任务很少能提供这些：轨迹代价高昂，失败难以诊断，奖励稀疏且不可计算。因此，相关工作转向执行框架和上下文，但人工调优需要特定任务的专业知识，且难以扩展；自动化方法则依赖稀缺的真实标注、更强的优化器模型，或退化为昂贵试错的、无引导的“提议-评估”循环。我们提出 SelfOp，一种自动改进冻结的安全智能体任务上下文（指令、技能和参考文档）的算法，且不修改其执行框架和模型权重。SelfOp 将上下文优化建模为受链式法则启发的文本梯度下降：从单个实例的结果出发，它将误差信号反向传播经过评估器、智能体的轨迹以及塑造其行为的上下文产物，从而得到每个实例的文本梯度。梯度通过聚类、排序和过滤在多个实例间累积，并且仅在跨实例共识下才被提交。收敛检测器监控梯度信号本身，一旦上下文吸收了训练数据中的可泛化信息便停止，无需留出验证数据。我们在 CyberGym 上评估 SelfOp，这是一个真实世界漏洞复现任务的基准。在少于 200 个训练样本的情况下，SelfOp 使 GPT-5.4-mini（搭配 Codex）实现 17 个点的自我改进，足以超过前沿 GPT-5.4 基线 6 个点，并使 GPT-5.4 自身实现 18.5 个点的自我改进。优化后的技能还能跨模型迁移，这表明 SelfOp 优化的技能学到的是可泛化的任务知识，而非特定于模型的模式。

## Abstract
LLM agents are increasingly used for security tasks: vulnerability discovery, exploit reproduction, and patch generation. Improving them at the model level demands expert demonstrations or computable rewards, which security tasks rarely offer: traces are costly, failures hard to diagnose, rewards sparse, and non-computable. Efforts thus shift to the harness and context, but manual tuning needs task-specific expertise and scales poorly, while automated methods rely on scarce ground truth, stronger optimizer models, or unguided propose-and-evaluate loops that reduce to costly trial and error.   We introduce SelfOp, an algorithm that automatically improves a frozen security agent's task context (instructions, skills, and reference documents), without modifying its execution harness and model weights. SelfOp casts context optimization as chain-rule-inspired textual gradient descent: from a single instance's outcome, it propagates error signals backward through the evaluator, the agent's trajectory, and the context artifacts that shaped its behavior, yielding per-instance textual gradients. Gradients are accumulated across instances by clustering, ranking, and filtering, and committed only under cross-instance consensus. A convergence detector monitors the gradient signal itself and stops once the context has absorbed the generalizable information in the training data, without held-out validation data.   We evaluate SelfOp on CyberGym, a benchmark of real-world vulnerability reproduction tasks. With fewer than 200 training examples, SelfOp yields a 17-point self-improvement for GPT-5.4-mini (with Codex), enough to surpass the frontier GPT-5.4 baseline by 6 points, and an 18.5-point self-improvement for GPT-5.4 itself. The optimized skills also transfer across models, highlighting that SelfOp-optimized skills learn generalizable task knowledge not model-specific patterns.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：LLM 智能体已用于漏洞发现、利用复现、补丁生成等安全任务，但安全任务通常缺乏专家示范、可计算奖励和规范真值，导致模型训练、执行框架工程和上下文优化都面临资源稀缺问题。
- **核心矛盾**：
  - 模型级改进依赖监督数据、可计算 RL 奖励或更强教师模型，安全领域难以满足。
  - 执行框架工程需要领域专家，难以规模化。
  - 上下文优化虽最可行，但现有方法多为单次分析、验证集门控、依赖真实标注或更强优化器，容易退化为昂贵试错。
- **整体含义**：论文提出 **SelfOp**，在不修改冻结模型权重和执行框架的前提下，仅自动优化智能体任务上下文（指令、技能、参考文档），让安全智能体用少量任务结果实现自我改进。

## 2. 方法论

### 2.1 核心思想

- 将上下文优化视为 **链式法则启发的文本梯度下降**：
  - 前向：智能体在任务批次上执行，得到二值成功/失败信号。
  - 反向：从损失出发，经评估分数、智能体轨迹、上下文产物，逐层归因，生成每个实例的文本梯度。
  - 更新：跨实例聚类、排序、过滤，只在跨任务共识下提交上下文修改。
- 可学习参数 \(\theta\) 被限定为智能体技能，如 `SKILL.md` 与参考文档，不改模型 \(M\) 和框架 \(H\)。

### 2.2 关键技术细节

- **链式梯度计算**：
  - 公式：\(\partial L/\partial \theta_{\text{skill}} = \partial L/\partial s \cdot \partial s/\partial \tau \cdot \partial \tau/\partial \theta_{\text{skill}}\)。
  - 阶段 1：分解二值损失，判断失败发生在哪个评估标准。
  - 阶段 2：**skill-blind** 轨迹分析，只看轨迹与漏洞元数据 \(\mu_v\)，重构漏洞结构不变量 \(\hat{\phi}_v = \text{RECONSTRUCT}(\mu_v)\)，避免被当前技能误导。
  - 阶段 3：**skill-aware** 归因，将行为问题映射到具体上下文缺失或不足，并提出编辑建议。
- **梯度累积**：
  - 提取每任务中的原子 strengths 与 improvements。
  - 用 LLM 聚类为主题，计算综合分：
    \[
    \text{composite}(t)=\alpha\cdot \text{support ratio}+\beta\cdot w(t)
    \]
    论文取 \(\alpha=0.4\)，\(\beta=0.6\)。
  - 过滤：要求综合分非负，并满足最小支持率；论文设 \(\rho_{\text{str}}=0.30\)，\(\rho_{\text{imp}}=0.20\)。
  - **覆盖注释**：将改进主题分为 hard gradient（技能中缺失，允许新增内容）和 soft gradient（技能已有但执行不足，只允许强化或重写）。
- **收敛检测**：
  - 将每步 hard gradient 比例 \(\hat{p}=|g_{\text{hard}}|/K\) 作为信号。
  - 噪声底 \(p_0=0.073\)，统计量：
    \[
    z=\frac{\hat{p}-p_0}{\sqrt{p_0(1-p_0)/K}}
    \]
  - 若滚动 3 步的 \(z<1.0\) 连续出现 3 次，则停止优化，无需留出验证集。
- **结构策略**：用结构策略 \(\psi\) 约束技能语法、文件布局和引用格式，结构检查器保证编辑后仍可被智能体加载。

## 3. 实验设计

- **数据集/场景**：使用 **CyberGym**，真实世界漏洞复现基准，任务是给定漏洞描述生成 PoC。数据来自 OSS-Fuzz，附带漏洞描述、补丁提交、sanitizer 栈轨迹等元数据。
- **数据划分**：从 1,507 个实例中随机采样 224 个训练、40 个验证、80 个测试，共 344 个，约占 23%。批次大小 16，一个 epoch 14 步。
- **评估方式**：sanitizer 与差分测试，二值 pass@1，无 LLM judge 噪声。每任务限时 30 分钟。
- **对比方法/消融**：
  - D1 单次单发分析，类似 MCE/Meta-Harness/AgentFlow。
  - D2 每任务单阶段分析，类似 ACE/CoEvoSkills。
  - D3 链式分解但只给优化器最终阶段输出。
  - D4 链式分解并暴露全部三阶段输出，即 SelfOp 梯度计算组件。
  - 累积阈值：Loose、Slightly Strict、Strict。
  - 覆盖注释：在最佳累积配置上加入 hard/soft 标注。
  - 收敛检测：比较验证集选择与信号检测选择的 checkpoint。
  - 附加研究：batch size 32、无元数据、GPT-5.4 前沿模型缩放、跨模型技能迁移。
- **公平性处理**：ACE、MCE 原版在 CyberGym 上会过拟合单一参考 PoC，因此论文保留其优化算法，但提供与 SelfOp 相同的元数据监督，以隔离算法差异。

## 4. 资源与算力

- 论文**未报告 GPU 型号、数量或训练时长**；因为方法不训练模型权重，只做 LLM API 推理与智能体执行。
- 报告了 API 与执行成本：
  - CyberGym 完整单次评估约 **3,000 美元 API 费用**。
  - 测试集 \(n=80\) 每步评估约 **1,400 美元**。
  - 每任务最长 **30 分钟**，部分任务需数美元。
  - 使用 OpenAI Codex 作为冻结框架，GPT-5.4-mini 同时作为 agent 和 optimizer；GPT-5.4 用于前沿缩放实验。

## 5. 实验数量与充分性

- **实验组数**：约十余组配置，包括：
  - 梯度计算 4 组：D1–D4。
  - 累积阈值 4 组：无累积、Loose、Slightly Strict、Strict。
  - 覆盖注释 1 组。
  - 收敛检测与 checkpoint 选择 1 组，并测试 \(n=20/40/60/80
