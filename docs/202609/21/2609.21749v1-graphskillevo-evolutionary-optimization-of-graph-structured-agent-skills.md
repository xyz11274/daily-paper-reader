---
title: "GraphSkillEvo: Evolutionary Optimization of Graph-Structured Agent Skills"
title_zh: GraphSkillEvo：图结构智能体技能的进化优化
authors: "Rui Sun, Zhi Zheng, Zhenkun Wang, Zhichao Lu"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21749v1"
tags: ["query:agent-evolve"]
score: 8.0
evidence: 图结构智能体技能的进化优化
tldr: "现有LLM智能体技能优化多采用非结构化自然语言指令，缺乏工作流指导且搜索空间大，优化低效。本文提出图结构技能：节点为执行步骤与操作指导，有向边编码上下文转移；并构建GraphSkillEvo，以种群进化、变异和交叉算子优化图技能。在五个智能体基准上，其持续优于SkillOpt，GPT-5.4-nano准确率提升4.01%，GPT-5.4提升1.76%。该工作用结构化表示与进化搜索提升了智能体技能优化效果。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.21749v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 420, \"height\": 510}, {\"url\": \"assets/figures/arxiv/2609.21749v1/fig-002.webp\", \"caption\": \"\", \"page\": 1, \"index\": 2, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.21749v1/fig-003.webp\", \"caption\": \"\", \"page\": 1, \"index\": 3, \"width\": 512, \"height\": 512}]"
motivation: 现有技能优化把技能当作非结构化自然语言指令，缺乏工作流级指导且冗余，搜索空间过大导致优化低效。
method: 提出图结构技能，节点为执行步骤及操作指导，有向边编码上下文相关转移；并设计GraphSkillEvo种群进化框架，用变异和交叉算子优化。
result: "在五个智能体基准上，其持续优于SkillOpt，GPT-5.4-nano准确率提升4.01%，GPT-5.4提升1.76%。"
conclusion: 该工作以结构化图表示和种群进化搜索，为智能体技能优化提供更清晰、更高效的新范式。
---

## 摘要
技能可以通过提供任务特定的过程性指导来提升大语言模型（LLM）智能体的性能，而技能优化则通过迭代式改进进一步提升其效果。然而，现有技能优化方法通常将技能表示为非结构化的自然语言指令，这带来两个关键挑战：1）非结构化技能往往缺乏明确的工作流级指导，并包含大量冗余，使LLM难以执行；2）无约束的自然语言技能搜索空间巨大，导致技能优化效果不佳。为应对这些挑战，我们提出将技能表示为图结构化的自然语言产物。在图结构技能中，每个节点表示一个执行步骤及其操作指导，而有向边则编码步骤之间依赖上下文的转移关系。与非结构化技能相比，图结构技能能够提供清晰的工作流级指导。此外，所提出的图结构技能还能促进技能优化。基于这种结构化表示，我们提出GraphSkillEvo，一种面向图结构技能的、基于种群的进化优化框架，带有变异和交叉算子。通过维护多个候选技能并组合有效组件，GraphSkillEvo能够比纯基于LLM的迭代自我改进更广泛、更全面地探索结构化技能空间。在五个智能体基准上的大量实验表明，GraphSkillEvo持续优于强技能优化基线SkillOpt，在GPT-5.4-nano上平均准确率提升4.01%，在GPT-5.4上提升1.76%。我们的代码见 https://github.com/ruisun7/GraphSkillEvo。

## Abstract
Skills can improve the performance of Large Language Model (LLM) agents by providing task-specific procedural guidance, while skill optimization further improves their effectiveness through iterative refinement. However, existing skill optimization methods typically represent skills as unstructured natural-language instructions, creating two key challenges: 1) Unstructured skills often lack explicit workflow-level guidance and contain substantial redundancy, making them difficult for LLMs to execute; 2) the vast search space of unconstrained natural-language skills makes skill optimization ineffective. To address these challenges, we propose representing skills as graph-structured natural-language artifacts. In graph-structured skills, each node represents an execution step together with its operational guidance, while directed edges encode context-dependent transitions between steps. Compared to unstructured skills, graph-structured skills can provide clear workflow-level guidance. Moreover, the proposed graph-structured skill can also facilitate skill optimization. Building on this structured representation, we introduce GraphSkillEvo, a population-based evolutionary optimization framework with mutation and crossover operators for graph-structured skills. By maintaining multiple candidate skills and combining effective components, GraphSkillEvo enables broader and more comprehensive exploration of the structured skill space than purely LLM-based iterative self-refinement. Extensive experiments across five agent benchmarks demonstrate that GraphSkillEvo consistently outperforms the strong skill optimization baseline SkillOpt, improving average accuracy by 4.01% on GPT-5.4-nano and 1.76% on GPT-5.4. Our code is available at https://github.com/ruisun7/GraphSkillEvo.

---

## 论文详细总结（自动生成）

# GraphSkillEvo 论文中文总结

## 1. 论文的核心问题与整体含义（研究动机与背景）

- **背景**：LLM 智能体在真实任务中越来越普及，"技能"（skill）作为可复用的提示级自然语言产物，为智能体提供任务特定的过程性指导（工作流、领域知识、操作规则、输出约束）。技能的一个重要优势是可跨不同 LLM 复用，在模型快速迭代的背景下能保留任务能力而无需重建流程。
- **问题**：人工编写或一次性 LLM 生成的技能往往不完整、脆弱，因此出现"技能优化"研究。但现有方法（如 SkillOpt）把技能表示为**非结构化的自然语言指令**，由此产生两个核心挑战：
  - **技能执行困难**：优化后的技能常表现为冗长的清单/要点，只提供粗粒度工作流指导，LLM 难以判断当前阶段应参考哪条指导、下一步该做什么；对能力较弱的模型（如 GPT-5.4-nano）尤为严重。
  - **技能优化困难**：缺乏显式结构导致搜索空间庞大且冗余，相似工作流可用多种不同文本实现，优化器被迫探索大量无实质程序意义的表示变体。
- **整体含义**：作者类比"技能"与"流程图"在分步程序指导上的相似性，提出将技能形式化为**图结构自然语言产物**，并在此基础上设计种群式进化优化框架，为智能体技能优化提供更清晰、更高效的新范式。

## 2. 论文提出的方法论

### 2.1 核心思想：图结构技能

形式化地，图结构技能 `s = ⟨hs, gs⟩`：

- **全局指导 hs**：跨所有执行步骤共享的指令，如任务描述、通用原则、共享执行模板。
- **节点集 Vs = {v1, ..., vns}**：每个节点描述一个可复用执行步骤（如解析目标、检索证据、执行操作、验证答案），并包含该步骤所需的指令、规则与约束。
- **边集 Es**：通过 M 个工作流 `{(cm, pm)}` 指定。每个工作流针对任务中的特定情境，`cm` 为适用条件，`pm` 为节点有序序列，定义该情境下的执行路径；相邻节点构成有向边。不同工作流可共享节点但走不同路径。

**图结构技能的三个性质**：低冗余（共享指令只在可复用节点中写一次）、显式工作流指导（每条工作流给出有序步骤路径）、为优化提供更紧凑结构化的搜索空间。

### 2.2 GraphSkillEvo 进化优化框架

基于种群的进化计算框架，维护 N 个候选技能，四步流程：

- **Step 0 种群初始化**：除初始技能外，用初始化提示让 LLM 生成多样化的图结构技能；每个个体在完整验证集 Dval 上评估，得分为适应度。
- **Step 1 训练集执行**：每代从 Dtrain 采样小批量 B 个实例，每个技能在智能体 A 上执行，产生轨迹；保留最多 K 条失败轨迹 `F(t)i` 作为反思信息（`rx,s=0` 表示失败）。
- **Step 2 新技能生成**（每个新技能三个子步骤）：
  - **2.1 算子选择**：从四个算子中按轮询（round-robin）选择。
  - **2.2 父代选择**：选择概率 `p ∝ 1/(r + N)`，r 为适应度排名，N 为种群规模。
  - **2.3 技能生成**：变异算子使用单父代 + 失败轨迹；交叉算子使用双父代。生成公式：
    - 变异：`s̃(t+1)j = Agen(o(t)j, s(t)parent,j, F(t)parent,j)`
    - 交叉：`s̃(t+1)j = Agen(o(t)j, s(t)parent,j)`
- **四个进化算子**（图 2）：
  1. **全局指导变异**：基于反思信息修订技能的全局指导，保持图结构不变。
  2. **图结构变异**：基于反思信息修订节点与边（细化节点指令、增删可复用节点、调整工作流），保持全局指导不变。
  3. **全局指导交叉**：重组两个父代的有用全局指导，保留其中一个父代的图结构。
  4. **图结构交叉**：重组两个父代的节点与边，保留其中一个父代的全局指导。
- **Step 3 种群选择**：新技能在完整 Dval 上评估，从当前种群与新生技能中按适应度保留 Top-N：
  `P(t+1) ∈ arg max_{S⊆P(t)∪P̃(t+1), |S|=N} Σ_{s∈S} JDval(s)`
- 重复 Step 1–3 共 T 代，返回最终种群中适应度最高的技能。
- **图结构校验器**：每个新生成技能都经脚本校验是否符合图 schema（如工作流引用的节点必须已在 Vs 中定义、每个节点至少出现在一个工作流中），不合规者丢弃并重新生成。

## 3. 实验设计

### 3.1 数据集 / 场景（5 个基准）

| 基准 | 任务类型 |
|---|---|
| SearchQA | 基于证据的事实型问答 |
| SpreadsheetBench | 真实 .xlsx 工作簿的程序化操作（生成 Python 代码，最多 30 轮迭代） |
| DocVQA | 文档图像视觉问答 |
| LiveMathematicianBench (LiveMath) | 数学多项选择推理 |
| ALFWorld | 持久化文本家务环境的具身交互（每回合限 50 步） |

数据划分：训练/验证/测试三划分，统一用 split seed=42；各基准规模如 SearchQA 为 400/200/1400，SpreadsheetBench 为 80/40/280，DocVQA 为 107/53/374，LiveMath 为 35/18/124，ALFWorld 为 39/18/134。

### 3.2 评估指标

- SearchQA、DocVQA、LiveMath：精确匹配准确率。
- SpreadsheetBench：工作簿在所有必需位置与标准答案全部匹配才算正确。
- ALFWorld：交互上限内的通过率。

### 3.3 对比方法

1. **No Skill**：无技能执行。
2. **Human skill**：专家手写技能。
3. **LLM skill**：GPT-5.4 从任务描述一次性生成的技能。
4. **SkillOpt**：强技能优化基线（rollout 反思、文本编辑选择、技能更新、验证门控），4 epochs、rollout batch 40、反思 minibatch 8、merge batch 8、编辑预算 4（最小 2，余弦调度）、启用 slow update 与 meta-skill memory。

### 3.4 实验设置

- **LLM**：GPT-5.4 与 GPT-5.4-nano，均用 medium reasoning effort；同一设置下任务执行与技能优化使用同一 LLM。
- **Harness**：无 harness 与 Codex harness（通过 SDK 调用，每个任务独立本地工作空间，workspace-write 沙箱，禁用交互审批）；ALFWorld 在 Codex harness 下留空（不支持持久环境交互）。
- **优化参数**：种群 N=4，代数 T=5，每代采样 15 个训练实例，最多 5 条失败实例构成反思信息，四算子轮询。
- 所有报告结果为 **3 次重复技能优化运行的平均**。

## 4. 资源与算力

- **论文未明确说明使用的 GPU 型号、数量或训练时长**。全文没有硬件配置、计算集群规模、墙钟时间等描述。
- 论文仅报告了**优化过程中的 token 消耗量**（表 2）：
  - GPT-5.4：SkillOpt 总计 81.08M tokens，GraphSkillEvo 总计 61.94M（SkillOpt 约为其 1.31 倍）。
  - GPT-5.4-nano：SkillOpt 总计 103.54M，GraphSkillEvo 总计 75.94M（SkillOpt 约为其 1.36 倍）。
- 结论：**算力信息（GPU 型号/数量/时长）缺失**，只能从 token 消耗间接推断优化成本。

## 5. 实验数量与充分性

### 实验规模

- **主实验（表 1）**：5 基准 × 2 LLM × 2 harness = 共 14 个模型–harness–基准设置（ALFWorld 在 Codex harness 下留空），每项为 3 次重复平均。
- **优化曲线（图 3）**：GPT-5.4-nano 无 harness 下 3 个基准的验证分数–token 消耗曲线，3 次实验平均。
- **Token 消耗（表 2）**：2 模型 × 5 基准。
- **图结构表示效果（表 3）**：去掉显式工作流组织、保留全局指导与节点指令的非结构化对照，5 个基准。
- **消融实验（表 4）**：w/o graph structure、w/o mutation、w/o crossover 三个变体，3 个基准（SearchQA、Spreadsheet、DocVQA），3 次重复平均。
- **跨模型迁移（表 5）**：3 个基准，SkillOpt 与 GraphSkillEvo 各对比 baseline / direct / transferred。
- **显著性检验（表 7）**：GPT-5.4-nano 无 harness 下 4 个基准 × 5 次独立运行，单侧 Welch's t 检验。
- **案例研究（图 4）**：ALFWorld 的图摘录与三条代表性执行轨迹。
- 附录还提供了完整的算子提示词、初始化提示词、优化后的图结构技能示例及其非结构化对照。

### 充分性与公平性评价

- **优点**：覆盖 5 个异质任务（问答、表格、文档、数学、具身交互）、2 个 LLM、2 种 harness，多维度验证；消融实验直接检验图结构、变异、交叉三个核心组件；做了统计显著性检验与跨模型迁移测试；所有基线使用相同数据划分与相同 LLM，公平性较好。
- **不足**：
  - 消融实验只在 GPT-5.4-nano 且仅 3 个基准上进行，覆盖面有限。
  - 显著性检验中 SearchQA（p=0.0687）与 DocVQA（p=0.0888）仅在 0.05–0.10 边缘区间，未达 0.05 显著性水平。
  - LiveMath 在 GPT-5.4-nano 上 GraphSkillEvo 反而落后 SkillOpt 0.80%，说明并非所有任务类型都受益。
  - 每项主结果仅 3 次重复，样本量较小。

## 6. 论文的主要结论与发现

- **主结果**：在 14 个模型–harness–基准设置中，GraphSkillEvo 在 **13 个设置**上取得最优。
  - 相对无技能：GPT-5.4 无 harness 平均 +15.37%，GPT-5.4-nano 无 harness 平均 +21.86%，GPT-5.4 Codex harness 平均 +10.31%。
  - 相对 SkillOpt：GPT-5.4 无 harness 平均 +1.76%，GPT-5.4-nano 无 harness 平均 +4.01%，GPT-5.4 Codex harness 平均 +1.33%。
- **小模型与程序性任务受益最大**：GPT-5.4-nano 提升（4.01%）大于 GPT-5.4（1.76%）；SpreadsheetBench +10.60%、ALFWorld +3.73% 提升尤为显著，说明清晰工作流指导对需要程序化执行、尤其是与外部环境交互的任务帮助最大。
- **优化曲线**：SkillOpt 较早收敛，GraphSkillEvo 通过持续更新收敛到更优性能。
- **Token 效率**：GraphSkillEvo 在更强性能的同时总 token 消耗显著更低（约为 SkillOpt 的 76%）。
- **图结构表示的作用（表 3）**：移除图结构后 5 个基准分别下降 4.52、2.50、4.19、1.35、0.75 个百分点，说明全局指导 + 节点指令本身不足以获得图结构技能的完整收益。
- **消融结论（表 4）**：去掉图结构 71.52→64.08；去掉交叉 71.52→66.59（可视为多条并行 SkillOpt 式自我改进轨迹）；去掉变异降幅最大，71.52→54.50。说明执行反馈的局部精炼与跨候选重组互为补充。
- **跨模型迁移（表 5）**：用 GPT-5.4-nano 优化、部署到 GPT-5.4 的技能在所有 3 个基准上均优于无技能基线，且优于迁移的 SkillOpt 技能；SpreadsheetBench 上迁移的 GraphSkillEvo 技能达 71.78，甚至超过直接在 GPT-5.4 上优化的 69.40。

## 7. 优点

- **表示创新**：将技能显式形式化为"全局指导 + 可复用节点 + 情境化工作流（有向边）"的图结构自然语言产物，同时解决执行时工作流指导不足与优化时搜索空间冗余两个问题，思路清晰且可解释。
- **优化机制设计合理**：种群式进化 + 四个结构感知算子（全局/图结构 × 变异/交叉）分工明确——变异利用执行失败轨迹做局部精炼，交叉实现跨候选组件重组，突破纯 LLM 自我改进的单轨迹局限。
- **结构一致性保障**：引入图结构校验器，保证生成技能在演化全过程中格式良好、节点与工作流引用一致。
- **实验覆盖面较广**：5 个异质基准、2 个 LLM、2 种 harness，并包含消融、迁移、显著性检验、案例研究、token 分析等多维度分析。
- **效率与效果兼得**：在性能提升的同时 token 消耗显著低于 SkillOpt，实际应用成本更低。
- **可复现性**：提供代码链接，附录给出完整算子提示词、初始化提示词及优化技能示例，透明度高。

## 8. 不足与局限

- **算力信息缺失**：未报告 GPU 型号、数量、训练/优化墙钟时长，无法评估实际计算成本；仅有 token 消耗作为间接指标。
- **实验覆盖限制**：
  - 消融实验仅在 GPT-5.4-nano 的 3 个基准上进行，未覆盖全部基准与其他模型/harness。
  - Codex harness 下未评估 ALFWorld（适配器不支持持久环境交互），harness 维度覆盖不完整。
  - 主结果仅 3 次重复，统计稳定性有限。
- **显著性风险**：SearchQA 与 DocVQA 的 p 值落在 0.05–0.10 边缘区间，优势未达常规显著性标准；仅在程序性基准（Spreadsheet、ALFWorld）上 p<0.05。
- **并非普遍有效**：LiveMath 在 GPT-5.4-nano 上落后 SkillOpt 0.80%，说明对纯数学推理类任务，图结构工作流指导的收益可能有限甚至为负。
- **图结构依赖 LLM 与校验器**：技能生成与算子执行完全依赖 LLM 提示，可能继承 LLM 偏差；校验器只检查结构一致性，不检查语义正确性；节点/边的质量仍受提示设计与模型能力影响。
- **搜索空间仍受限于人工设定的算子与超参数**：算子种类、种群规模 N、代数 T、采样批量 B、失败轨迹预算 K 均为固定值，未做敏感性分析。
- **应用限制**：目前仅面向文本/代码型智能体任务；作者在结论中也指出未来工作包括与参数化优化方法结合、更丰富的图组合机制、跨领域图技能合并——这些尚未实现。

（完）
